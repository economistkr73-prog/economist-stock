# 분봉(1분봉) 시세 수집 로직 — 현행 정리 (검토 의뢰용)

> 작성 2026-08-05 · 대상 코드 기준일 2026-08-05 · 운영 중(2026-08-05 첫 자동 실행)
> **이 문서 하나만 읽고 검토할 수 있게** 코드를 인용해 옮겨 적었다. 레포 접근 없이 판단할 수 있다.
> 검토해 주었으면 하는 것은 맨 뒤 **§9 검토 요청 항목**에 모아 두었다.

---

## 0. 한 줄 요약

한국 주식 **1분봉을 우리 DB(`dt_min`)에 최근 10거래일치만 굴려 담는다.**
소스는 **키움 REST(주 소스)** + **네이버 모바일 차트 API(폴백·장중 실시간)** 둘.
평일 16:45 크론이 하루 한 번 「수집 → 만료 삭제 → 구멍 치유 → 분할 감지 → 적재율 감시」를 순서대로 돈다.

**왜 쌓아야 하나**: 네이버 분봉은 **최근 7거래일**까지만 준다(실측). 그 뒤로는 어떤 파라미터로도 못 받는다.
화면이 필요로 하는 보관 창이 10거래일이라 **지금부터 우리가 담지 않으면 영원히 사라진다.**

---

## 1. 시스템 환경 (제약의 출처)

| 항목 | 값 | 이것이 만든 제약 |
|---|---|---|
| 서버 | cafe24 웹호스팅 · PHP 8.4 · **SAPI=apache2handler** | `fastcgi_finish_request` 없음 → 긴 작업은 **자기호출 bg** 밖에 방법이 없다 |
| 응답 타임아웃 | **30초** | 20종목 × 1req/s = 40초 → 크론은 반드시 bg |
| DB | MariaDB 10.6 · `economist73` 스키마(127 표 공유) | 새 표는 접두어로 소속 표시 (`dt_`) |
| 크론 트리거 | cron-job.org → `cron_job.php?task=dt_min&k=…` 한 줄 | 잡 이력엔 bg 라 `queued…` 한 줄만 남음 |
| 키움 인증 | App Key/Secret (`env/kiwoom.inc` · 웹 루트 밖) + **IP 화이트리스트** | 서버 IP 등록 필수. 미등록 시 `8050 지정단말기 인증 실패` |

---

## 2. 데이터 모델

```sql
-- 종목 풀 (사용자가 화면에서 담는 감시 종목 · 상한 20)
CREATE TABLE dt_pool (
  code VARCHAR(10) NOT NULL COMMENT '6자리 순수 코드 (_AL 접미사 저장 금지)',
  name VARCHAR(64) NOT NULL DEFAULT '', mkt VARCHAR(8) NULL,
  active TINYINT(1) NOT NULL DEFAULT 1,
  sort_no INT NOT NULL DEFAULT 0,               -- 드래그 순서 = 우선순위
  init_status TINYINT NOT NULL DEFAULT 0 COMMENT '0대기 1진행 2완료 9실패',
  init_at DATETIME NULL, added_at DATETIME NOT NULL, removed_at DATETIME NULL,
  PRIMARY KEY (code), KEY ix_active (active, sort_no)
);

-- ★1분봉 원장. ts = 그 봉의 «시작» 시각 (KST)
CREATE TABLE dt_min (
  code VARCHAR(10) NOT NULL,
  ts   DATETIME    NOT NULL COMMENT 'KST · 봉 시작 시각',
  o INT UNSIGNED NOT NULL, h INT UNSIGNED NOT NULL,
  l INT UNSIGNED NOT NULL, c INT UNSIGNED NOT NULL,
  v BIGINT UNSIGNED NOT NULL DEFAULT 0,
  src TINYINT NOT NULL DEFAULT 1 COMMENT '1=키움 2=네이버',
  upd_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (code, ts),
  KEY ix_ts (ts)          -- 만료 삭제가 WHERE ts < ? 라 PK 선두를 못 탄다
);

-- 일자별 수집 상태 (치유 대상 판정 · 품질 추이)
CREATE TABLE dt_min_log (
  code VARCHAR(10) NOT NULL, d DATE NOT NULL,
  bars SMALLINT NOT NULL DEFAULT 0,
  status ENUM('ok','partial','none','holiday','fail') NOT NULL DEFAULT 'none',
  src TINYINT NULL, tries TINYINT NOT NULL DEFAULT 0,
  first_ts VARCHAR(5) NULL, last_ts VARCHAR(5) NULL,   -- 그날 첫/마지막 봉 HH:MM
  err VARCHAR(255) NULL, upd_at DATETIME NOT NULL,
  PRIMARY KEY (code, d), KEY ix_status (status, d)
);

-- 키움 접근토큰 캐시 (appkey/secret 은 여기 넣지 않는다)
CREATE TABLE dt_token (
  id TINYINT NOT NULL DEFAULT 1, token VARCHAR(1024) NOT NULL,
  expires_at DATETIME NOT NULL, upd_at DATETIME NOT NULL, PRIMARY KEY (id)
);
```

**읽기 전용으로 재사용하는 기존 표** (조인 관계 없음 · 종목코드 문자열만 공유)

| 표 | 쓰임 |
|---|---|
| `krx_amt` (전종목 일별 원장, 8년치) | **거래일 정의**(행이 있는 날 = 거래일) · 보관 창 계산 · 상장주식수(`list_shrs`) |
| `all_stock_info` | 종목명·현재가·등락률 · 거래일 판정 폴백(`uDate`) |

주요 상수
```php
Dt::POOL_MAX    = 20;      // 종목 풀 상한 (화면이 정한 수 — 좌측 262px 목록)
Dt::WINDOW_DAYS = 10;      // 보관 창 = 최근 10 «거래일» (달력 10일 아님)
Dt::BARS_FULL   = 381;     // 정규 1일 = 09:00~15:19 380봉 + 15:30 종가단일가 1봉
Dt::BARS_OK     = 375;     // 이 이상이면 status='ok' (거래 없는 분이 빠질 수 있어 여유)
Dt::UNITS       = [1,5,15];// 화면·API 가 허용하는 봉 단위
Dt::MAX_TRIES   = 5;       // 치유 재시도 상한
Kiwoom::TS_BASE = 'start'; // ★봉 시각의 의미 (§5)
Kiwoom::GAP_USEC= 1000000; // 1 req/s
```

---

## 3. 소스 두 개

### 3-1. 키움 REST (주 소스) — `ka10080 주식분봉차트조회요청`

```php
POST https://api.kiwoom.com/api/dostk/chart
headers: authorization: Bearer <token> / api-id: ka10080 / cont-yn / next-key
body:    { stk_cd, tic_scope:"1", upd_stkpc_tp:"1" }    // upd_stkpc_tp=1 → 수정주가
```
- **연속조회 루프**: 응답 헤더의 `cont-yn`/`next-key` 로 최대 8콜(초기 적재) / 2콜(당일). 한 콜 ≈ 900봉.
  중단 조건 = `cont-yn≠Y` · `next-key` 없음 · **가장 오래된 봉의 날짜가 `from` 이전**. 콜 사이 1초 sleep.
- **응답 파싱은 느슨하게** — 필드명이 바뀌어도 살아남게 후보를 여러 개 훑는다.
  실측 확정된 실제 이름: 목록 `stk_min_pole_chart_qry` · 시각 `cntr_tm`(`YYYYMMDDHHMMSS`)
  · 가격 `cur_prc`/`open_pric`/`high_pric`/`low_pric` · 거래량 `trde_qty`
- **키움 숫자는 부호가 붙어 온다**(`"+131900"`, `"-131600"`) → `abs()` 로 절대값이 곧 가격.
- **주문 API 봉쇄**: `TR_MAP` 화이트리스트(`ka10080`, `ka10081`)에 없는 `api-id` 는 예외를 던진다.
- 오류 처리: `429` → 지수 백오프 2회 · `401` → 토큰 강제 재발급 후 1회 재시도 · `>=400` 예외
  · **HTTP 200 인데 `return_code≠0`** 인 경우도 예외(키움은 200 으로도 실패를 알린다).
- 토큰: `dt_token` 캐시, **만료 10분 전** 재발급. `expires_dt`(14자리) 없으면 보수적으로 6시간.

```php
// 정규화된 한 봉으로 옮기는 자리
public static function mapBar(array $r): ?array {
    $tm = pick($r, ['cntr_tm','stck_cntg_hour','dt','tm']);   // 14자리
    if (strlen($tm) < 12) return null;
    $ts = strtotime("$y-$mo-$d $h:$mi:00");
    if (self::TS_BASE === 'end') $ts -= 60;      // 종료 기준이면 «시작»으로 당긴다
    $c = num(pick($r, ['cur_prc','close_pric','stck_prpr','clos_prc']));
    if ($c <= 0) return null;
    ... return ['t'=>date('Y-m-d H:i',$ts), 'o'=>$o?:$c, 'h'=>$hi?:max($o?:$c,$c),
                'l'=>$lo?:min($o?:$c,$c), 'c'=>$c, 'v'=>$v];
}
```

### 3-2. 네이버 모바일 차트 API (폴백 + 장중 실시간)

```
GET https://api.stock.naver.com/chart/domestic/item/{code}/minute?count=400
headers: 모바일 UA + Referer: https://m.stock.naver.com/
```
- 토큰 불필요·빠름. **가장 최근 거래일(=보통 당일) 하루치만** 걸러서 돌려준다.
- 필드: `localDateTime`(`YYYYMMDDHHmmss`) · `openPrice`/`highPrice`/`lowPrice`/`currentPrice`
  · **거래량 `accumulatedTradingVolume`**
  → 이름은 「누적」이지만 실측상 **분당 거래량**이다(§5 근거 참조). ★검토 요청 항목에 넣어 두었다.
- **한계**: 최근 7거래일까지만. 그래서 초기 적재(10거래일)와 오래된 구멍 치유는 키움만 할 수 있다.

### 3-3. 왜 SOR(`_AL`)을 안 쓰나 — ★거래량 기준을 하나로 묶는 결정

키움은 종목코드에 `_AL`(SOR 통합) / `_NX`(NXT) 접미사를 붙일 수 있다.
2026-08-04 삼성전자 정규장 381봉 실측:

| 기준 | 거래량 합 |
|---|---|
| KRX 단독 | **29,246,067** |
| NXT | 14,315,959 |
| `_AL`(SOR = KRX+NXT) | 43,555,914 |
| 네이버 | 29,239,968 (KRX 와 사실상 동일) |

→ `_AL` 은 네이버의 **1.49배**. 네이버가 **폴백이자 장중 실시간 소스**라 `_AL` 로 쌓으면
**한 종목의 같은 날 봉에 1.49배 다른 두 기준이 섞인다.**
`env/kiwoom.inc` 의 `KIWOOM_NO_SOR=true` → **KRX 단독**으로 고정. KRX 단독일 때 네이버와 분단위 **379/381 일치**.

---

## 4. 공통 정규화 — 소스가 무엇이든 이 문을 지난 것만 저장한다

```php
/**
 * R2 09:00~15:19  그대로 (380봉)
 * R3 15:20~15:29  → 15:30 으로 «병합» (OHLC 재계산 · v 합산)   // 동시호가 구간
 * R4 15:30        종가 단일가 1봉
 * R5 08:00~08:59 · 15:31~20:00 (프리마켓·시간외) → 버린다
 */
public static function normalize(array $rows): array {
    foreach ($rows as $r) {
        $m = 시(hh)*60 + 분(mi);
        if ($m < 540 || $m > 930) continue;               // 09:00~15:30 밖은 버린다
        if ($m > 919 && $m < 930) $t = 날짜 . ' 15:30';    // 15:20~15:29 → 15:30
        if ($c <= 0) continue;                            // 값이 없는 봉은 버린다
        if ($o <= 0) $o = $c;  if ($h <= 0) $h = max($o,$c);  if ($l <= 0) $l = min($o,$c);
        if (!isset($keep[$t])) $keep[$t] = [...];
        else { h=max, l=min, c=«나중 것», v+=v }           // 병합된 15:30
    }
    ksort($keep); return array_values($keep);
}
```

**★결측 봉은 만들지 않는다.** 거래가 없던 분은 봉이 없다. 0으로 채우면 없던 봉이 생겨 지표가 거짓말을 한다.
(실측: 삼성전자 하루 381봉 vs 바이브컴퍼니 2,171봉/10일 — 얇은 종목은 원래 봉이 적다)

**리샘플 1분 → N분** (조회 시점에 계산 · 저장하지 않음)
```php
// 라벨 = 그 구간의 «시작» (09:00, 09:05, …). 09:00 기준 경과분으로 그룹을 만든다.
$off = $mins - 540;  $g = intdiv($off,$unit)*$unit;  $gm = 540 + $g;
if ($gm > 915) $gm = 915;      // ★15:20~15:30 은 마지막 그룹(15:15)이 삼킨다
// 결측 그룹은 만들지 않는다 · 시가=구간 첫 봉 · 종가=구간 끝 봉 · h=max · l=min · v=합
```
검증: 1/5/15분 리샘플 봉수 381/76/26 이고 **거래량 합이 셋 다 동일**.

---

## 5. ★봉 시각의 의미 = 「시작」 (전 데이터가 여기에 매달려 있다)

`Kiwoom::TS_BASE='start'` — 한 곳에서만 정한다. 잘못 잡으면 **전 데이터가 1분씩 밀리고**
네이버 폴백분과 섞이면서 **조용히** 깨진다.

**근거 ① 소스 대조** (`php cron/dt_min.php job=diag code=005930`, 서버 실측)
```
대조 381분 · 같은 분 일치 381 · 1분 당겨 일치 94 · 거래량 일치 379
★ TS_BASE='start' 가 맞습니다     ★ 거래량 기준 일치
```
(작업 PC 에서의 초회 실측은 같은 분 288 : 1분 당겨 89 — 방향은 동일)

**근거 ② 구조** — 09:00 있고 08:59 **없음** / 15:19 있고 15:20 **없음**(동시호가) / 15:30 단일가 / 시간외 마지막 19:59.
종료 기준이라면 09:01~15:20 · 20:00 으로 떨어져야 한다.

진단 코드는 「그대로 겹칠 때(same)」와 「1분 당겼을 때(shift)」를 각각 세고
`shift > same*1.2` 면 `'end'` 로 바꾸라고 판정한다.

---

## 6. 크론 — `cron/dt_min.php` (하루 한 번, 4+1 단계)

레지스트리 등록(`cron_job.php`):
```php
'dt_min' => [
    'file' => 'cron/dt_min.php', 'bg' => true, 'cron' => '45 16 * * 1-5',
    'get'  => ['key' => 'econ-dt-min', 'job' => 'daily'],
],
```
시간 예산 900초 · `cron_bg_begin()`/`cron_bg_over()`/`cron_bg_finish()` 로 자기호출 bg.

### ★16:45 인 이유 (타이밍 의존이 둘 있다)
1. 「오늘이 거래일인가」를 **`krx_amt` 의 오늘 행**으로 판정하는데, 그 행은 **15:50 `dart_eod`** 크론이 넣는다.
   앞에 두면 매일 「휴장일」로 오판해 **그날 분봉을 조용히 건너뛴다.**
2. 바로 앞 `etf_update`(16:20)가 실측 **901.7초**(435 ETF × sleep 2초 → 16:35 종료)라 16:25 면 한복판에서 겹친다.

### 거래일 판정
```php
public function isTradingDay(?string $d = null): bool {
    if (주말) return false;
    if (SELECT 1 FROM krx_amt WHERE d=?) return true;
    if ($d === 오늘) {                                  // 폴백 — dart_eod 가 실패했을 때
        $n = SELECT COUNT(*) FROM all_stock_info WHERE DATE(uDate)=CURDATE();
        return $n > 100;
    }
    return false;
}
```

### 보관 창
```php
public function tradingDays(int $n = 10): array {   // krx_amt 에 행이 있는 날 = 거래일
    return SELECT d FROM (SELECT DISTINCT d FROM krx_amt ORDER BY d DESC LIMIT n) t ORDER BY d ASC;
}
public function windowFrom(): string {              // 이보다 옛 봉은 매일 지운다
    $days = $this->tradingDays();  return $days ? $days[0] : date('Y-m-d');
}
```
★`krx_amt` 가 비면 `windowFrom()` 이 **오늘**이 되고, prune 이 **어제까지를 전부** 지운다.
(8년치가 든 표라 실제로 빌 일은 없지만, 그 상태로 돌면 보관 창이 하루로 줄어든다)

### 단계

| 단계 | 하는 일 | 요점 |
|---|---|---|
| ① 수집 | 활성 종목의 **당일분** — `dt_fetch_save($code, $from=오늘, todayOnly=true)` | 종목당 1초 sleep · 예산 초과 시 「나머지는 치유 단계가 이어받는다」 |
| ② 만료 삭제 | `DELETE FROM dt_min WHERE ts < windowFrom()` · 60일 지난 로그 · 해지 종목 잔여 | ★**반드시 수집 다음** — 뒤집히면 수집 실패한 날 보관 일수만 줄어든다 |
| ③ 구멍 치유 | `holes(40)` 대상 재수집 (종목당 1회면 그 종목 창이 다 채워진다) | 키움은 약 1년 보관 → 며칠 놓쳐도 복구된다 |
| ④ 분할 감지 | `krx_amt.list_shrs` 전일 대비 **±20%** 초과 → `wipe()` 후 전량 재수집 | 수정계수를 곱하지 않는다 — 10거래일이면 5콜이라 다시 받는 편이 싸고 확실하다 |
| ⑤ 적재율 감시 | `collectedCount(오늘)*2 < 종목수` 면 `cron_fail_notify(priority 1)` | ★아래 설명 |

**⑤가 왜 필요한가** — 이 파일은 수집 실패를 **전부 자체 catch 로 삼킨다**(네이버 폴백을 위해).
그래서 예외가 안 올라가 중앙 실패 알림이 못 보고, bg 라 cron-job.org 눈에도 늘 성공이다.
20종목이 전멸해도 사람이 로그를 열어야만 안다 — **키움 IP 가 막혀 있던 동안이 실제로 그 상태였다.**
→ 「삼키는 크론은 결과를 세어 스스로 쏜다」.

### 한 종목·한 구간을 받아 저장하는 함수 (전 단계가 이것을 부른다)

```php
function dt_fetch_save(Dt $dt, Kiwoom $kw, string $code, string $from, bool $todayOnly = false): array {
    $rows = []; $src = null; $err = null;
    if ($kw->hasKey()) {
        try { $r = $kw->minute($code, 1, $from, $todayOnly ? 2 : 8);
              $rows = $r['rows']; $src = Dt::SRC_KIWOOM; }
        catch (Throwable $e) { $err = '키움: ' . $e->getMessage(); }
    } else { $err = '키움 인증키 없음'; }

    if (!$rows) {                                   // ── 폴백: 네이버(당일 위주)
        try { $nv = (new NaverFinanceAPI())->getMinuteOhlc($code);
              $rows = Dt::normalize($nv['success'] ?? []);
              if ($rows) { $src = Dt::SRC_NAVER; $err = $err ? $err.' → 네이버로 대체' : null; } }
        catch (Throwable $e) { $err = ...; }
    }
    if (!$rows) return ['bars'=>0, 'src'=>null, 'err'=>$err ?: '받아 온 봉이 없습니다'];

    $total = 0;
    foreach (Dt::byDay($rows) as $d => $day) {
        if ($d < $from) continue;
        $dt->barsUpsert($code, $day, $src);         // INSERT … ON DUPLICATE KEY UPDATE (한 방에)
        $n = count($day); $total += $n;
        $dt->logSave($code, $d, $n, Dt::statusOfBars($n), $src, $err,
                     substr($day[0]['t'],11,5), substr($day[$n-1]['t'],11,5));
    }
    return ['bars'=>$total, 'src'=>$src, 'err'=>$err];
}
```

### 치유 대상(구멍) 판정
```sql
SELECT p.code, k.d
  FROM dt_pool p
  JOIN (SELECT d FROM krx_amt WHERE d IN (…최근10거래일…) GROUP BY d) k
  LEFT JOIN dt_min_log l ON l.code = p.code AND l.d = k.d
 WHERE p.active = 1
   AND (l.status IS NULL
        OR (l.status IN ('partial','none','fail') AND l.tries < 5))
 ORDER BY k.d DESC, p.sort_no LIMIT 40
```

### 수동 job
```
php cron/dt_min.php job=diag code=005930    소스 대조(시각 기준·거래량 기준)
php cron/dt_min.php job=status              보관 창·종목별 적재 현황
php cron/dt_min.php job=init code=005930    한 종목 10거래일 초기 적재
php cron/dt_min.php job=heal                구멍만 치유
```

---

## 7. 조회 경로 (화면까지)

### 7-1. API — `stock/api.php?module=dt&action=series`
```php
case 'series': {
    $unit = (int)($_GET['unit'] ?? 1);
    if ($code === '' || !in_array($unit, Dt::UNITS, true)) $out([]);
    $to   = cleanDate($_GET['to'])   ?: date('Y-m-d');
    $from = cleanDate($_GET['from']) ?: $to;

    /* 장중 실시간 — 네이버 당일분을 얹는다(토큰 불필요·빠름).
     * 같은 분이 양쪽에 있으면 «DB(키움)가 이긴다» — 소스 섞임을 최소로. */
    $extra = [];
    $live  = !empty($_GET['live']) && $to === date('Y-m-d');
    if ($live) {
        $hm = (int)date('Hi');
        if ($hm >= 900 && $hm <= 1600) {
            $nv = (new NaverFinanceAPI())->getMinuteOhlc($code);
            $extra = Dt::normalize($nv['success'] ?? []);
        }
    }
    $out($dt->series($code, $unit, $from, $to, (int)($_GET['limit'] ?? 4000), $extra));
}
```

### 7-2. `Dt::series()`
```php
$win  = $this->windowFrom();
$to   = cleanDate($to) ?: 오늘;   $from = cleanDate($from) ?: $to;
if ($from < $win) $from = $win;      // 창을 넘는 요청은 «접는다» (에러가 아니다)
if ($from > $to)  $from = $to;
$limit = max(1, min(6000, $limit));  // 기본 4000

SELECT ts,o,h,l,c,v FROM dt_min
 WHERE code=? AND ts >= ? AND ts < DATE_ADD(?, INTERVAL 1 DAY)
 ORDER BY ts LIMIT {$limit}

// 장중 실시간 — DB 에 «아직 없는 분»만 얹는다 (같은 ts 는 DB 가 이긴다)
foreach ($extra as $r) { if (isset($bars[$t])) continue; $bars[$t] = …; }
ksort($bars);
return $unit === 1 ? $bars : self::resample($bars, $unit);
```

### 7-3. 프런트
```js
// style/dailychart.js — 단타 전용 어댑터. 기존 fetchMinute() 은 손대지 않는다(updash·갤러리 정본)
function fetchMinuteRange(code, unit, from, to, live) {
  var q = '/stock/api.php?module=dt&action=series&code='+code+'&unit='+(unit||1)
        + '&from='+from+'&to='+to + (live ? '&live=1' : '');
  return fetch(q).then(r=>r.json()).then(raw => raw.map(x => ({
      // KST 벽시계를 UTC 로 취급 — 09:00 이 09:00 으로 보인다
      time: Math.floor(Date.parse(x.t.replace(' ','T')+'Z')/1000),
      open:x.o, high:x.h, low:x.l, close:x.c, vol:x.v })));
}
```
```js
// 화면(mode=short) — 세션 메모리 캐시. localStorage 금지(시세는 오래 두면 거짓이 된다)
function ttlOk(at){ return (Date.now()-at) < (isOpen() ? 60000 : 3600000); }
function isOpen(){ 평일 && 09:00~15:35 }
function series(code, unit, from, to, live){
  var k = code+'|'+unit+'|'+from+'|'+to;
  if (!live && cache[k] && ttlOk(cache[k].at)) return Promise.resolve(cache[k].rows);
  return DailyChart.fetchMinuteRange(...).then(rows => { cache[k]={rows,at:Date.now()}; return rows; })
                                         .catch(() => []);
}
setInterval(function(){ if (!state.live || !state.code || !isOpen()) return; drawMain(); }, 30000);
```

화면 구성 — 메인 **1분·10거래일 전부 적재 후 창만 이동**(`[당일]`=300봉/`[5일]`/`[전체]`·사이는 휠)
· 보조 **5분·10거래일**(메인보다 한 단계 위) · 일봉 패널은 **기존 경로**(`stock_analysis_api.php?module=stock&action=daily`)를 그대로 읽는다.
**분봉과 일봉을 한 시계열로 합치지 않는다**(분봉은 거래대금 없음·수정주가 처리가 다름).

---

## 8. 운영 실적 / 현재 상태

- 서버 실측(2026-08-04): `job=diag` 같은 분 일치 **381/381** · `job=heal` 후 `dt_min` **7,450행**
  (005930 10일 3,748봉 · 049080 10일 3,702봉 · **구멍 0**)
- 이후 사용자가 종목을 담아 **7종목 24,593행**. 하루 정상치는 종목당 3,74x봉.
- 단위 테스트 `stock/tests/dt_test.php` **26건 통과** — 정규화(프리마켓/시간외 버리기·15:20~15:29 병합)·
  리샘플(라벨·OHLC·거래량 보존·마지막 그룹)·`cleanCode`(SOR/NXT 접미사 제거·인젝션 차단)·`statusOfBars`.
- 크론 등록 완료: cron-job.org 「키움단타데이터(일1회)」 `45 16 * * 1-5` Asia/Seoul.

### 이미 값을 치르고 확정한 것 (되돌리면 아는 사고가 재발한다)
1. 종목코드는 **언제나 6자리**로 저장. `_AL`/`_NX` 는 요청할 때만. (`Dt::cleanCode()` 가 문지기)
2. 거래일·보관 창은 **`krx_amt`** 로 판정. 휴장일 표를 새로 만들지 않는다.
3. **만료 삭제는 반드시 수집 다음.**
4. **주문 API 는 부르지 않는다** — `TR_MAP` 화이트리스트 밖은 예외.
5. **봉 시각 = 시작**(`TS_BASE`) · **거래량 기준 = KRX 단독**(`KIWOOM_NO_SOR=true`).
6. 분봉과 일봉을 합치지 않는다 · 일봉은 기존 경로 그대로.
7. 새 표는 `dt_` 접두어.

---

## 9. ★검토 요청 항목

아래는 **작성자가 직접 의심하는 것들**이다. 확증한 버그가 아니라 「이 판단이 맞나」를 물어보는 자리다.
①~④를 우선 봐 주고, 그 밖에 놓친 실패 모드가 있으면 지적해 주면 좋겠다.

### ① 치유(heal)가 **오래된 구멍을 영원히 못 메우면서 tries 도 안 오르는** 경로
`holes()` 는 `(종목, 날짜)` 를 주지만 `dt_fetch_save()` 는 **종목 단위**로 `from=창 시작` 부터 받는다.
키움이 살아 있으면 창 전체가 채워져 문제가 없다. 그런데 **키움이 실패해 네이버로 물러서면**
네이버는 **가장 최근 거래일 하루치만** 준다 → `bars > 0` 이 되어

- 대상이던 **옛 날짜(`$h['d']`)의 구멍은 그대로 남고**
- `if ($r['bars'] === 0)` 이 거짓이라 **`tries` 가 오르지 않는다**(`MAX_TRIES=5` 가 영영 안 걸린다)

즉 그 종목은 매일 한 콜씩 태우면서 같은 구멍을 무한히 재시도한다. 조용해서 티가 안 난다.
→ **날짜별 결과로 판정하도록 바꿔야 하나?** (요청한 `$h['d']` 가 이번에 안 채워졌으면 그 날짜에 `tries+1`)

### ② 휴장일 오판 — `isTradingDay()` 의 `all_stock_info` 폴백
평일 공휴일에는 `krx_amt` 에 행이 없다. 이때 폴백이 `all_stock_info.uDate = 오늘`, `COUNT > 100` 이면
**거래일로 판정**한다. 시세 갱신 크론이 공휴일에도 돌면서 `uDate` 를 건드리면 그렇게 된다.
그러면 전 종목 수집 시도 → 키움/네이버 모두 **전 거래일** 봉을 주고 `$d < $from` 으로 걸러져 **0봉**
→ 전 종목 `status='fail'` + `tries+1` → **⑤ 감시가 Pushover 로 오경보**.

- 폴백을 「오늘 갱신됐나」가 아니라 「오늘 **가격이 실제로 움직였나**」로 바꿔야 하나?
- 아니면 폴백을 아예 없애고 `krx_amt` 하나만 믿는 편이 나은가? (그 대신 `dart_eod` 실패 시 그날을 잃는다)

### ③ `Dt::series()` 의 `LIMIT 4000` — 여유가 190봉뿐이고, 잘리는 방향이 나쁘다
10거래일 × 381봉 = **3,810**. 기본 `limit=4000`(화면은 limit 을 안 보낸다) 과의 여유가 190봉이다.
그리고 `ORDER BY ts LIMIT n` 이라 한도에 닿으면 **가장 최신 봉이 잘린다** — 단타 화면에서 제일 보고 싶은 쪽이다.

**닿는 경로를 하나 찾았다**(코드상 추론 · 실측 아님):
`dart_eod`(15:50)가 실패해 `krx_amt` 에 **오늘 행이 없는 날** →
`isTradingDay()` 는 `all_stock_info` 폴백으로 통과해 수집은 정상 진행되지만,
`windowFrom()` 은 **어제로 끝나는 10거래일**을 가리킨다 → `prune()` 이 오늘 것을 지우지 않아
DB 에 **11거래일 = 4,191봉**이 남는다. 이 상태에서 `series(from=windowFrom, to=오늘)` 은
4,191 중 **오래된 4,000봉만** 읽고 **최신 191봉(오늘 오후분)을 버린다.**

- 이 추론이 맞나? (`prune` 은 수집 다음이고 `windowFrom` 을 그대로 쓰므로 11일이 되는 것은 맞아 보인다)
- 고친다면 `ORDER BY ts DESC LIMIT n` 후 뒤집기 / 창 크기에서 상한을 유도 / limit 상향 중 어느 쪽이 옳나?

### ④ 네이버 `accumulatedTradingVolume` 을 **분당 거래량**으로 쓰고 있다
필드 이름은 「누적」인데 봉의 `v` 로 그대로 넣는다. 근거는 실측뿐이다 —
분단위로 키움(KRX)과 379/381 일치했고, 1/5/15분 리샘플의 거래량 합이 모두 동일(29,239,968)했으며
KRX 원장 총량(29,246,067)과도 맞는다. **누적이면 이 셋이 모두 성립할 수 없다**고 판단했다.
- 이 추론에 구멍이 있나? 네이버가 어떤 조건에서 누적으로 바뀔 가능성은?

### ⑤ 그 밖에 봐 주었으면 하는 것
- **`normalize()` 의 15:30 병합이 입력 정렬에 암묵적으로 의존한다** — 병합된 봉의 종가를
  「나중에 순회된 것」으로 덮는다. 두 호출부 모두 사전 정렬(ksort/usort)돼 있어 지금은 맞지만,
  `public static` 이라 정렬 안 된 배열이 들어오면 조용히 틀린 종가가 된다. 함수 안에서 정렬해야 하나?
- **토큰 동시 발급** — 화면의 `pool_init`(사용자가 종목을 담는 즉시 10거래일 적재)과 크론이 겹치면
  각자 토큰을 발급해 `dt_token` 을 덮어쓴다. 키움이 신규 발급 시 이전 토큰을 무효화한다면
  진행 중인 쪽이 401 을 맞는다(재발급 재시도 1회는 있다). 잠금이 필요한 수준인가?
- **분할 감지가 하루 늦다** — `krx_amt.list_shrs` 전일 대비 ±20% 로 판정하므로,
  분할 당일 장중에는 옛 봉과 새 봉이 한 화면에 섞인다(다음 16:45 에 전량 재수집으로 해소).
  10거래일 창에서 이 정도 지연이 허용 가능한가? 무상증자·병합도 같은 규칙으로 묶어도 되나?
- **재시도·백오프** — `429` 지수 백오프 2회 · `401` 재발급 1회. 종목 간 1초 sleep.
  키움 실제 한도(1 req/s, 버스트 2) 대비 충분한가? 20종목 × 최악(치유·재수집 겹침)에서 예산 900초 안에 끝나나?
- **`dt_min_log.tries` 가 성공해도 초기화되지 않는다** — `tries=tries+0`. status 로 걸러지니 실害는 없다고 봤는데 맞나?

---

## 부록 A. 파일 지도

| 파일 | 역할 |
|---|---|
| `classes/Kiwoom.class` (365줄) | 키움 REST — 토큰·`ka10080` 연속조회·응답 파싱·TR 화이트리스트·소스 대조 진단 |
| `classes/Dt.class` (703줄) | DB 전담 — 스키마·거래일/보관창·종목 풀·UPSERT·구멍/삭제/분할·조회+리샘플·**정규화** |
| `cron/dt_min.php` (263줄) | 일일 배치 — 수집→삭제→치유→분할→감시 · 수동 job(diag/status/init/heal) |
| `stock/api.php` `api_dt()` | `module=dt` — pool_list/add/init/remove/sort · **series** · status · diag (전부 JSON) |
| `stock/index.php` `mode=short` | 화면(다크 3분할) — 메인 1분 · 보조 5분 · 일봉 패널 · LIVE 30초 |
| `style/dailychart.js` | 차트 공용 모듈 — `fetchMinuteRange()`(단타 전용 어댑터) |
| `stock/tests/dt_test.php` | 단위 테스트 26건 (순수 함수만 · DB 불필요) |
| `classes/NaverFinanceAPI.class` | `getMinuteOhlc()` — 네이버 모바일 차트 API |

## 부록 B. 용어

- **보관 창(window)** — 원장에 남겨 두는 최근 10 **거래일**. 그 밖은 매일 지운다.
- **구멍(hole)** — 보관 창 안인데 `dt_min_log` 가 `NULL/partial/none/fail` 인 (종목, 날짜).
- **치유(heal)** — 구멍을 다시 받아 메우는 단계.
- **동시호가** — 15:20~15:30. 체결이 15:30 한 번에 나므로 그 구간 봉을 15:30 으로 병합한다.
- **SOR / `_AL`** — 키움의 KRX+NXT 통합 호가. 거래량 기준이 달라 쓰지 않는다.
