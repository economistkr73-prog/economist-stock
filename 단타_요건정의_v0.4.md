# 단타 (단기매매) — 요건정의 v0.4 · **구현 완료본**

**작성** 2026-08-04 · **입력** `단기매매_요건정의_v0.3.md`(챗) + `분봉차트_현황.md`(실측) + 현재 스키마 대조
**v0.3 → v0.4** 접두어 `st_`→**`dt_`** · 크론 3개→**1개** · **장중 30초 자동 갱신 채택**(v0.3 은 제외였음) ·
보조 분봉 단위 규칙 확정(Q-10 해소) · 화면 위치 `stock_analysis.php?mode=short` → **`/stock/index.php?mode=short`**(메뉴 「단타」)

> 이 문서는 **이미 구현된 것**을 적는다. 불변 규칙 요약은 `CLAUDE.md` 의 「/stock 단타」 절,
> 크론 상세는 `CRON.md` §3.9 에 있다. 여기 있는 것은 **왜 그렇게 됐나**와 **남은 검증**이다.

---

## 0. v0.3 초안을 고친 곳과 이유 (현재 스키마 실측 대조)

| # | v0.3 | v0.4 | 왜 |
|---|---|---|---|
| 1 | 표 접두어 `st_` | **`dt_`** | 이미 `stock_financial`·`stock_daily_range`·`stock_price_range`·`all_stock_info` 가 있어 `st_` 는 그 축약처럼 읽힌다. 127표를 공유하는 스키마라 접두어가 곧 소속이다 |
| 2 | 종목코드 `VARCHAR(12)` (`_AL` 저장 여지) | **6자리만 저장** (`Dt::cleanCode`) | `_AL` 이 들어가면 `krx_amt`(CHAR(6))·`all_stock_info` 조회가 전부 어긋난다. SOR 접미사는 **요청할 때만** |
| 3 | 크론 3개 (daily 16:10 · heal 16:40 · adjchk 18:00) | **1개 (`dt_min` 평일 16:25)** · 안에 4단계 | 셋이 같은 시점에 매달린 순서 있는 일이다(`dart_eod` 와 같은 묶음 방식). 크론 사이트 줄도 하나만 는다 |
| 4 | 16:10 수집 | **16:25** | ★`krx_amt` 의 **오늘 행은 15:50 `dart_eod`** 가 넣는다(실측 `cron/dart_collect.php` eod). 그 앞에 두면 「오늘이 거래일인가」가 false 가 되어 **그날 분봉을 조용히 건너뛴다** |
| 5 | 거래일 판정 = `krx_amt` 단독 | + **`all_stock_info.uDate` 폴백** | 위 의존을 하나 더 받쳐 둔다. dart_eod 가 실패한 날에도 수집은 돈다 |
| 6 | 만료 삭제 PK 선두가 `code` | **`KEY ix_ts (ts)` 추가** | `DELETE … WHERE ts < ?` 가 PK 를 못 타 매일 전 표를 훑는다 |
| 7 | 장중 자동 갱신 **제외** | **30초 자동 갱신 채택**(기능으로 on/off) | 단타 화면인데 클릭해야만 갱신되면 쓰기 불편하다. 네이버 당일분은 토큰 불필요·빠르고 키움 한도를 안 쓴다 |
| 8 | 보조 분봉 단위 미정(Q-10) | **메인보다 한 단계 위**(1→5) · 10거래일 고정 ★2026-08-04 메인이 1분 고정이 되어 보조는 **5분 고정**이다 | 같은 단위·같은 기간이면 두 패널이 같은 말을 한다. 「메인=세부(1분·5일), 보조=맥락(5분·10일)」 |
| 9 | 화면 `stock_analysis.php?mode=short` | **`/stock/index.php?mode=short`** | 메뉴를 「포트폴리오」 앞 **단타** 로 넣기로 함(2026-08-04 사용자). 메뉴 단일 소스가 `pf_menus()` 라 화면도 같은 라우터에 둔다 |

---

## 1. 데이터 — 어디서 와서 어디에 쌓이나

```
① 초기 적재/치유   키움 ka10080(_AL·수정주가) ──┐
② 매일 당일분      키움 → 실패 시 네이버        ├→ Dt::normalize() → dt_min (1분봉)
③ 장중 실시간      네이버 당일 분봉             ─┘        └→ 조회 때 5·15분으로 리샘플(서버)
④ 일봉 패널        기존 action=daily (네이버 + krx_amt 실제 거래대금 병합) ← 새 경로 만들지 않음
```

| 표 | 내용 | 규모(20종목 기준) |
|---|---|---|
| `dt_pool` | 단타 종목 풀 (최대 30 · 소프트 삭제) | 30행 |
| `dt_min` | **1분봉 원장 · 최근 10거래일만** | 76,200행 (10MB 미만) |
| `dt_min_log` | 종목×날짜 수집 상태(`ok/partial/none/holiday/fail` · tries · 첫·끝 봉시각) | 60일 보관 |
| `dt_token` | 키움 접근토큰 캐시 (만료 10분 전 재발급) | 1행 |

**★보관 창이 10거래일뿐인 이유** — 네이버가 7거래일까지만 주고 그 뒤로는 어떤 파라미터로도 못 준다.
키움이 1년을 주므로 «놓친 날을 나중에 메우는» 것은 되지만, 화면이 쓰는 창은 10거래일로 못박았다.
장기 분봉 분석(요건 D9)이 필요해지면 **`dt_min` 을 늘리지 말고 별도 원장**을 새로 판다(캐시와 원장을 섞지 않는다 —
`stock_daily_range`↔`stock_price_range` 에서 이미 배운 것).

---

## 2. 정규화 규칙 (전 소스 공통 · `Dt::normalize`)

| 규칙 | 내용 | 검증 |
|---|---|---|
| R1 | `ts` 는 봉의 **시작** 시각(KST). 소스가 종료 기준이면 저장 전에 1분 당긴다 | `Kiwoom::TS_BASE` · **[V-1] 확정 = 'start'**(§5) |
| R2 | 09:00~15:19 그대로 (380봉) | `stock/tests/dt_test.php` |
| R3 | 15:20~15:29 → **15:30 으로 병합**(OHLC 재계산·v 합산) | 〃 |
| R4 | 15:30 종가 단일가 1봉 | 〃 |
| R5 | 08:00~08:59 · 15:31~ (NXT 프리/애프터·시간외) **폐기** | 〃 |
| R6 | 정규 1일 = **381봉** · 375봉 이상이면 `ok` | 〃 |

**리샘플(서버 PHP)** — 라벨은 구간 시작(09:00, 09:05 …). 마지막 그룹 `15:15` 가 15:15~15:19 + **15:30 을 삼킨다**.
결측 그룹은 **만들지 않는다**(0 으로 채우면 없던 봉이 생겨 지표가 거짓말을 한다).
★불변식: **봉 단위를 바꿔도 거래량 총합은 같다** — 테스트가 이걸 지킨다.

---

## 3. 화면 (`/stock/index.php?mode=short`)

```
┌ 좌 262px ─────┬──────────── 우상 55% : 메인 분봉(1분 고정) ───┐
│ 단타 종목     │ 당일전고·현재가 [당일][5일][전체] LIVE ＋지표 │
│ 현재가·등락률 ├──── 우하좌 : 일봉 ────┬──── 우하우 : 보조 분봉 ┤
│ 수집 배지     │ 일봉/주봉·기간·＋지표 │ (메인+1단계 · 10거래일) │
│ 드래그 정렬   │ SUE 공시 마커         │                        │
└───────────────┴───────────────────────┴────────────────────────┘
```
- 색은 차트 모듈의 dark 테마·차트설정 갤러리와 **같은 팔레트**. 화면과 차트가 겉돌면 눈이 다시 적응해야 한다.
- 십자선은 **분봉 두 패널끼리만** 잇는다(`DailyChart.linkCrosshair`). 일봉은 축 단위가 달라 무의미하다.
- **★일봉 패널의 도구모음은 공용 컴포넌트 한 벌이다**(2026-08-04 사용자 요청).
  옛 `[3개월][6개월][1년]` 자체 버튼을 걷어내고 공용 컴포넌트 둘로 바꿨다 —
  `DailyChart.periodBar`(일봉/주봉 · 160/240/480/전체 · ± · SUE 공시 N · ⛶)와
  `DailyChart.indicatorBar`(차트틀 ▾ · 차트저장 · ＋지표 · 적용중 칩) + 당일전고·현재가 칩 + 지표 값 범례.
  **차트틀·지표 키는 `short`**(`ChartFeat::SCREENS['short']['pref']`).
  ★2026-08-05 까지는 `updash` 였다 — 그 키를 함께 쓰던 상승종목분석 화면이 삭제되면서 옮겼다
  (저장된 행 이관은 `mig_updash_to_short.php`).
  ★**기간·높이는 화면(`short`)이 따로 기억한다** — 차트 폭이 달라서다(요건: 기간의 주인은 화면).
  ★데이터는 **1000영업일**을 한 번 받는다(480일·96주 버튼이 재조회 없이 성립).
  종목을 바꿀 때 `setData(rows)` 를 **fit 없이** 부른다 — fit 하면 볼 때마다 기간이 풀린다.
- 여러 날이 이어진 분봉은 **09:00 자리에 날짜**를 찍는다(`multiDay:true`) — 없으면 어디가 어제인지 모른다.
- **★메인은 1분봉 전용이다**(2026-08-04 사용자 지시로 확정). 단위 선택([1분][5분][15분])을 없앴다 —
  세부를 보는 자리라 1분이 아니면 이 패널의 값어치가 없고, 5·15분은 보조 패널이 이미 맡는다.
  옛 「10일×1분이면 자동으로 5분으로 강등」 규칙도 함께 사라졌다(그 조합 자체가 없어졌다).
  - **데이터는 보관 창 전부(10거래일)를 한 번에 싣는다**(HTS 방식 전체적재 · 2026-08-04 사용자 지시로 5→10).
  - **창(보이는 구간)만 움직인다** — `[당일]`=최근 **300봉** · `[5일]`=최근 5거래일 · `[전체]`=실린 10거래일 전부
    · **그 사이는 휠**. 창을 바꿀 때 **다시 받지 않는다**(`setViewDays` 만 부른다).
    ★「전체」는 3,810봉이라 캔들이 가늘다 — 그래서 **[5일] 을 사이에 뒀고 기본은 [당일]**이다.
    ★「5일이 몇 봉인가」는 **곱셈으로 정하지 않는다**(`barsForDays`) — 장중인 오늘은 덜 차 있고 반나절 장도 있다.
      그래서 창은 **봉이 실린 뒤에** 다시 센다.
  - 보조 패널(5분 · 10거래일)과 **기간이 같아졌지만 단위가 다르다** — 메인=세부(1분), 보조=맥락(5분).
  - 종목을 갈아 끼울 때 `setData(rows)` 를 **fit 없이** 부른다 — fit 하면 창이 매번 「전체」로 풀린다.
  - 도구는 일봉과 같은 한 벌이되 **분봉에 뜻이 있는 것만** — 당일전고 · 현재가 · 차트저장 · ＋지표.
    **기간 바는 붙이지 않는다**(일봉/주봉 전환과 「160일」 같은 라벨이 1분봉 자리에선 거짓말이 된다).
  - ★**「당일전고」는 분봉에서 «오늘 고가»다**(`todayHigh:'session'`). 일봉의 「직전 60봉 최고가」를 1분봉에
    그대로 쓰면 「직전 60분 최고가」가 되어 이름이 거짓이 된다 — 그래서 모듈에 모드를 하나 더 뒀다.
  - 지표·차트틀 키는 **`shortmin`**(일봉의 `short` 와 별개) — 분봉에 쓰는 지표는 일봉 지표와 주기가 다르다.
- 기능 스위치(차트설정 > 화면별 구성): `panel.short_daily` · `panel.short_sub` · `chart.minute_unit`(잠금) · `chart.live`.
  ★**끄면 DOM 도 fetch 도 사라진다** — 「배지만 지우고 뒤에서 계속 부르면 왜 느리지가 된다」.
- 세션 캐시: 메모리에만(localStorage 금지 — 시세는 오래 두면 거짓이 된다). 장중 60초 / 장 마감 뒤 세션 내내.
- **종목 상한 20 (`Dt::POOL_MAX`)** — 21번째를 담으면 막지 않고 **정렬 맨 아래를 빼고** 담은 뒤 무엇을 뺐는지 토스트로 알린다.
  상한을 정한 것은 크론이 아니라 화면이다(30종목이어도 매일 60초 · 최악 150초로 예산 900초의 1/4). 좌측 목록 262px 에
  종목당 2줄이라 **스크롤 없이 15~18개**가 한계고, 눈으로 좇는 자리라 그보다 많으면 목록이 「뒤지는 것」이 된다.
  빼는 기준이 「맨 아래」인 이유 — 순서는 사용자가 끌어서 정하므로 그 자체가 우선순위이고, 한 번도 안 옮겼다면
  `sort_no` 가 담은 순서라 자연히 «가장 먼저 담은 것»이 빠진다. ★해지와 같은 경로라 **그 종목의 봉도 함께 지워진다**(재등록 5초).

### 3.1 목록 탭 — 「단타 | 보유」 (2026-08-05 신설)

**보유 종목도 단타 화면에서 보고 싶다**는 요청에서 나왔다. 답은 **원장 하나 · 목록 둘 · 슬롯은 단타만**이다.

| | 단타 탭 | 보유 탭 |
|---|---|---|
| 원본 | `dt_pool`(내가 담은 것 · 상한 20) | `pf_position` `status <> 'closed'`(살아있는 포지션) |
| 담기·× 빼기·드래그 | 있음 | **없음** — 그 목록은 포트폴리오가 정한다 |
| 수집 | 매일 16:45 | **똑같이** 매일 16:45 (`Dt::targetCodes()` 합집합) |
| 초기 10거래일 | 「＋」 직후 `pool_init`(≈5초) | **단타 화면을 열 때** `held_init` 이 하나씩(≈5초) · 못 받으면 크론 ③ |
| 배지 | 「보유」면 초록 배지 | 「보유」(돈이 들어감) / 「편입됨」(자리만) · 눌러서 포지션 상세로 |

- ★★**보유를 `dt_pool` 에 «담지» 않는다.** 그 표는 상한 20 을 **FIFO 로 지우는** 표라
  (`poolAdd` → `poolEvictLast` → `poolRemove` → `DELETE dt_min`), 보유가 그 20칸에 섞이면
  **탐색하다 「＋」 한 번 누른 것이 내가 산 종목의 봉을 통째로 지운다.**
  게다가 보유는 이미 `pf_position` 이 정한 사실이라, 복사하면 **청산했는데 단타엔 남는**
  「따로 노는」 상태가 생긴다 — 참조로 두면 그럴 자리가 없다.
- **상한 20 은 그대로 둔다.** 그 숫자는 「단타 풀」의 것이지 「수집 대상」의 것이 아니고,
  탭으로 나누면 「한눈에 들어오는 수」 제약도 **탭마다** 걸린다(합이 아니다).
  실측 2026-08-05: 대상 **22종목** = 풀 11 + 보유 14 − 겹침 3. 크론은 11종목에 66.5초였다(예산 900초).
- ★★**상한이 찼을 때 FIFO 는 「보유 종목」을 먼저 뺀다**(`poolEvictLast` · 2026-08-05 사용자 선택).
  겹치는 종목(담기도 했고 보유이기도 한 것)은 슬롯을 먹지만 **빠져도 계속 수집**되므로 잃는 것이 없다.
  반대로 안 담긴 종목을 빼면 봉이 사라져 다시 받아야 한다 — **값이 싼 쪽부터 뺀다.**
  보유가 없으면 옛 규칙(정렬 맨 아래) 그대로. 무엇을 왜 뺐는지는 토스트가 말한다
  (「…을(를) 뺐습니다(보유라 계속 수집됩니다)」).
  ★그래서 **`poolRemove()` 는 보유 종목의 봉을 지우지 않는다** — 지우면 매일 받아서 매일 지우는
  밑 빠진 독이 되고 「보유」 탭이 눈앞에서 빈다. × 확인 문구도 갈라야 한다(「봉도 함께 지웁니다」가 거짓이 된다).
  ★실측 2026-08-05: 지금은 맨 아래(LG전자)가 마침 보유라 두 규칙이 겹치지만, 그 다음 후보에서
  **새 규칙 452280(보유) vs 옛 규칙 011070(안 담김)** 으로 갈린다.
- **초기 적재는 「단타 화면을 열 때」 화면이 당겨 온다**(`held_init` · 2026-08-05 사용자 선택).
  크론에만 맡기면 **편입한 날 분봉을 못 본다**(다음 16:45까지 기다린다). 고른 자리의 근거 —
  | 방식 | 왜 안 골랐나 / 골랐나 |
  |---|---|
  | `Pf::positionSave()` 에 훅 | ⊗ 편입 저장이 **5초 느려지고**, 키움이 막히면 **포트폴리오 저장까지** 흔들린다. 편입은 5개 화면에서 일어난다 |
  | **단타 화면이 열릴 때** | ◎ 훅이 **한 곳**뿐 · 포트폴리오 흐름을 안 건드림 · **실제로 볼 때만** 비용 · 「채우는 중」 배지가 그 자리에서 진행 표시가 된다 |
  | 크론에만 맡김 | ⊗ 편입 당일엔 분봉을 못 본다 |
  ★**한 번에 하나씩**(키움 1 req/s · 종목당 ~5초) · **한 세션에 종목당 한 번만**(`heldTried` — 실패를
  되풀이하면 화면 열 때마다 콜을 태운다) · 키움 키가 없으면 **아예 시도하지 않는다**(네이버는 당일치뿐).
  못 받은 것은 **③ 구멍 치유**가 맡는다 — 배지가 「받는 중」 → 「채우는 중」으로 돌아간다.
  ★`held_init` 은 **살아있는 포지션인지 확인**한 뒤에만 적재한다 — 아니면 이 문이
  「아무 종목이나 원장에 쌓는 문」이 되어 보관 창·용량 규칙을 우회한다.
  ★**일괄 동기 적재는 안 된다** — 14종목 × 5초 = 70초라 **30초 벽**(첫 백필은 크론을 한 번 돌려 해결했다).
- **청산해도 봉을 즉시 지우지 않는다** — 만료 삭제가 10거래일에 걸쳐 자연히 밀어낸다.
  방금 판 종목의 분봉이 복기엔 제일 쓸모 있다.
- ★**한 종목이 여러 포트폴리오에 있을 수 있다**(실측: 한국주철관 3곳 · 현대차 2곳) —
  목록은 **종목 단위로 접고** 배지에 개수를 적는다(18행이 아니라 14행).
- ★**미리보기 판정(`state.owned`)은 합집합**이다 — 보유 종목도 매일 수집되므로 미리보기가 아니다.
- ★**탭이 바꾸는 것은 「목록 열」 하나뿐**이다 — 차트·뉴스·고른 종목은 그대로 둔다.
- ⚠**포트폴리오에 ETF 가 있으면 빈 행이 된다**(단타 목록의 이름·현재가는 `all_stock_info` 에서 오고
  ETF 는 그 표에 없다). 실측 2026-08-05 기준 **결측 0** 이라 지금은 안전하지만, 생기면 시세 원천부터 정해야 한다.

---

## 4. API (`stock/api.php?module=dt`) — 전부 JSON

| action | 파라미터 | 반환 |
|---|---|---|
| `pool_list` | – | 풀 + 현재가·등락률 + 수집일수/봉수/구멍 + `held` + 보관 창·거래일 목록 |
| `held_list` | – | **살아있는 포지션**을 종목 단위로 접은 목록(+`pos_id`·`pf_n`·`pf_names`·`open`) |
| `pool_add` / `pool_init` | `code` | 담기(즉시) / 10거래일 초기 적재(≈5초). ★**보유 종목이면 거부**한다 — 이미 수집되므로 20칸을 먹을 이유가 없다 |
| `pool_remove` / `pool_sort` | `code` / `codes[]` | 해지(시세도 함께 삭제) / 순서 |
| `series` | `code,unit(1·5·15),from,to,live` | 봉 배열. **창을 넘는 요청은 에러가 아니라 접는다** |
| `status` | `code` | 보관 창 10일의 일자별 수집 상태 |
| `diag` | `code` | **[V-1][V-4]** 키움↔네이버 대조 |

종목 검색은 기존 `module=stock&action=search` 를 그대로 쓴다(새로 만들지 않는다).

---

## 5. 검증 결과와 남은 것

### ✅ 해소 — 2026-08-04 실측 (005930 · ka10080 실호출)

키를 넣고 서버 IP 를 키움 화이트리스트에 등록해 **서버에서도 정상 동작**한다([B-1] 해소).
처음엔 서버만 막혀 있어서 [V-1]·[V-4] 를 **작업 PC 에서 같은 요청을 재현해** 먼저 답했고, 뚫린 뒤 서버 진단이 같은 답을 냈다.

| # | 질문 | 답 | 근거 |
|---|---|---|---|
| **V-1** | 봉 시각이 「시작」인가 「종료」인가 | **시작** — `TS_BASE='start'` 그대로 | 네이버 381봉 대조 **같은 분 288 : 1분 당겨 89**. 구조로도 같다 — 09:00 있고 08:59 없음 · 15:19 있고 **15:20 없음**(동시호가) · 15:30 단일가 · 시간외 마지막 19:59. 종료 기준이면 09:01~15:20·20:00 이어야 한다 |
| **V-4** | `_AL`(SOR) 거래량이 네이버와 같은가 | **다르다 → `KIWOOM_NO_SOR=true` 로 바꿈** | 정규장 381봉 합계 **KRX 29,246,067 · NXT 14,315,959 · _AL 43,555,914**. KRX 단독이면 네이버(29,239,968)와 **분단위 379/381 일치** |
| – | 응답 필드명 | 코드의 첫 후보가 그대로 맞음 | 목록 `stk_min_pole_chart_qry` · 시각 `cntr_tm`(14자리) · `cur_prc`/`open_pric`/`high_pric`/`low_pric`(부호 붙음) · `trde_qty` |

**★V-4 를 「SOR 포기」로 답한 이유** — 네이버는 이 기능의 **폴백**이자 **장중 30초 실시간** 소스다.
`_AL` 로 쌓으면 같은 종목의 같은 날 봉에 1.49배 차이 나는 두 기준이 섞여 「거래량이 터졌다」가 소스에 따라 갈린다.
실제 체결 흐름(NXT 포함)을 잃는 값을 치르고 **화면 전체가 한 기준으로 말하는 것**을 샀다.
네이버를 걷어내고 전 구간을 키움으로만 채우게 되면 그때 `false` 로 되돌린다.

### ★[B-1] 키움은 **IP 화이트리스트**다 — 서버 IP 를 등록해야 한다

```
토큰 발급 실패: 인증에 실패했습니다[8050:지정단말기 인증에 실패했습니다]
```

키 문제가 아니다. 키움 「계좌 API KEY 관리 > IP 등록 및 현황」에 **등록된 IP 에서만 인증이 된다**(최대 10개).
처음엔 발급받은 PC 의 IP 만 들어 있어 **작업 PC 는 성공 · 서버만 실패**했다. 서버 IP `220.73.160.47` 을 더해 해소.

- **웹 서비스 IP 와 나가는 IP 가 같다**(실측 — `SERVER_ADDR`·ipify·ifconfig.me·icanhazip 모두 `220.73.160.47`). 하나만 넣으면 된다.
- 작업 PC 의 IP 도 **지우지 않는다** — 서버가 막혔을 때 같은 요청을 PC 에서 재현해 진단할 수 있다(이번에 그렇게 답을 냈다).
- 키는 **만료 1년**(2027-08-04) · App Key/Secret 은 **1회만 다운로드**된다.
- 유사 증상이 다시 나면 의심 순서: ①IP 목록에 현재 나가는 IP 가 있나 ②만료일 ③실전/모의 도메인(`8030`).

★막혀 있는 동안에도 **네이버 폴백으로 당일분은 쌓인다**(실측). 잃는 것은 키움 전용 둘뿐 —
⒜신규 종목의 10거래일 **초기 적재** ⒝7거래일보다 오래된 **구멍 치유**.

### 남은 것

1. ~~`env/kiwoom.inc` 에 appkey·secretkey~~ → **완료**(서버 배포까지).
2. ~~`job=diag`~~ → **완료**. 서버 실측 `대조 381분 · 같은 분 일치 381 · 1분 당겨 94 · 거래량 일치 379`.
3. ~~[B-1] 서버 IP 등록~~ → **완료**.
4. ~~초기 적재~~ → **완료**. `dt_min` 7,450행 · 005930 10일 3,748봉 · 049080 10일 3,702봉 · **구멍 0**.
5. **cron-job.org 에 한 줄 등록** — `https://economist.kr/cron_job.php?task=dt_min&k=econ-cron-j7k2` (평일 16:25) ← **남은 것은 이것뿐**
6. 하루 뒤 `job=status` 로 구멍이 없는지 확인.

★진단 자체에도 버그가 있었다 — `Kiwoom::diag()` 가 `$out += [...]` 로 결과를 담는데 **`+=` 는 왼쪽에 이미 있는 키를 덮지 않아**
위에서 초기화한 `'ok' => false` 가 그대로 남아, 대조가 성공해도 화면에는 늘 「대조 실패」로 나왔다(`array_merge` 로 고침).

### 미결 `[Q-n]`

| # | 질문 | 지금 | 결정 시점 |
|---|---|---|---|
| ~~Q-1~~ | 실계좌 키인가 모의투자 키인가 (`KIWOOM_MOCK`) | **실계좌 확정** — 모의 도메인은 `8030:투자구분(실전/모의)이 달라서 Appkey를 사용할수가 없습니다` 로 거절 | 해소 2026-08-04 |
| ~~Q-4~~ | 풀 상한 30 이 적절한가 | **20 으로 확정**(`Dt::POOL_MAX`) · 넘치면 **거부하지 않고 맨 아래를 빼고 담는다** | 해소 2026-08-04 |
| Q-6 | 3·30·60분봉을 열 것인가 | 1·5·15 만 (`Dt::UNITS`) | 필요할 때 |
| Q-8 | 조건식 판정(D9)을 10거래일 표본으로 검증 가능한가 | **불가** — 별도 백테스트 원장이 먼저 | v0.5 |
| Q-13 | 일봉 패널의 SUE 마커를 켤 것인가 | **켬**(기능으로 끌 수 있음) | – |
| Q-14 | 목록 시세를 실시간화할 것인가 | `all_stock_info`(평일 9회) 60초 재조회 | 필요할 때 |

**해소됨** — Q-7(키움 채택·v0.3) · **Q-9(swap 해석 → 「보조는 메인+1단계」로 대체)** · **Q-10(15분봉 자리 → 메인 단위·보조 자동)**

---

## 6. 이 기능이 건드리지 **않은** 것

`stock_analysis_api.php` 의 `case 'minute'` / `case 'daily'` · `NaverFinanceAPI` 기존 메서드 ·
`DailyChart.fetchMinute()` · 포트폴리오/관심종목/퀀트 로직 — **전부 무변경**.
새 경로만 더했다. 같은 종목이 포트폴리오와 단타에 동시에 있어도 서로 아무 제약이 없다(종목코드 문자열만 공유).
