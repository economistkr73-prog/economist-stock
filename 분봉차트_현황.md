# 분봉 차트 — 현재 구조·데이터 현황 (요건정의 입력용)

**작성 2026-08-04** · 코드 전수 확인 + 외부 API 실측(같은 날 장 마감 후 측정)
**목적**: 분봉 관련 요건을 새로 정의하기 전에, **지금 어떻게 가져오고 어디까지 되는지**를 한 파일로 파악한다.
(이 문서는 레포 접근 없이 읽어도 되게끔 코드 인용·실측 수치를 모두 본문에 포함했다.)

---

## 0. 30초 요약

| 사실 | 내용 |
|---|---|
| 어디서 오나 | **네이버 모바일 차트 API** `api.stock.naver.com/chart/domestic/item/{code}/minute` 1곳뿐 |
| 언제 받나 | **사용자가 종목을 클릭하는 순간** 서버가 실시간 크롤 → 그대로 전달 |
| **DB 저장** | ★**어디에도 저장하지 않는다.** 분봉을 담는 테이블은 이 스키마(127개 표)에 **없다** |
| 캐시 | 없음. 같은 종목을 두 번 누르면 네이버를 두 번 부른다 |
| 범위 | **당일 1분봉 381봉**(09:00~15:19 + 15:30). 과거일은 화면에서 못 본다 |
| ★숨은 여력 | API 자체는 `startDateTime`/`endDateTime` 으로 **최근 7거래일**까지 준다 — 현재 코드가 안 쓰고 있을 뿐 |
| 봉 단위 | **1분 고정.** 5분·15분봉을 주는 파라미터를 못 찾았다(`minuteUnit=5` 무시됨) |
| 쓰는 화면 | 상승종목분석(updash) 하단 보조차트 · 차트설정 갤러리 ⑤번 미리보기 — **둘뿐** |
| 일봉과의 관계 | 완전 별개 경로. 일봉은 다른 네이버 엔드포인트 + KRX 거래대금 병합, 분봉은 병합 없음 |

---

## 1. 전체 구조 (3계층)

```
[브라우저]  stock_analysis.php?mode=updash            ← 화면
   │        style/dailychart.js  DailyChart.fetchMinute(code)
   │            fetch('/stock_analysis_api.php?module=stock&action=minute&code=005930')
   ▼
[서버 PHP]  stock_analysis_api.php   case 'minute'    ← 얇은 통과 계층 (가공 거의 없음)
   │        classes/NaverFinanceAPI.class  getMinuteOhlc($code, 400)
   │            curl → https://api.stock.naver.com/chart/domestic/item/{code}/minute?count=400
   ▼
[외부]      네이버 모바일 차트 API                      ← 원천 (유일)

※ 이 경로 어디에도 DB 쓰기가 없다. MariaDB 는 「로그인 세션」 확인 외에는 관여하지 않는다.
```

**대비 — 일봉은 경로가 다르다**
```
fetchDaily → action=daily → NaverFinanceAPI::getDailyOhlc (fchart.stock.naver.com/sise.nhn?timeframe=day)
                          → + KrxAmt::series() 로 DB(krx_amt)의 «실제 거래대금 a» 를 있는 날만 병합
```
분봉에는 이 **거래대금 병합 단계가 없다**(네이버 분봉이 금액을 안 주고, DB 에도 분 단위 원장이 없다).

---

## 2. 외부 소스 — 네이버 모바일 차트 API (실측 스펙)

### 2.1 요청

```
GET https://api.stock.naver.com/chart/domestic/item/{6자리코드}/minute?count=400
Headers:
  User-Agent: Mozilla/5.0 (iPhone; CPU iPhone OS 16_0 like Mac OS X) AppleWebKit/605.1.15 ...  (모바일 UA 필수)
  Referer:    https://m.stock.naver.com/
```
- 인증 키 없음 · 무료 · 응답은 **클린 JSON 배열**(일봉 쪽 fchart 처럼 작은따옴표 비표준 JSON이 아니다)
- curl 타임아웃 10초, `CURLOPT_SSL_VERIFYPEER=false`

### 2.2 응답 원본 (한 항목)

```json
{
  "localDateTime": "20260804090000",   // YYYYMMDDHHmmss (KST)
  "currentPrice":  243000.0,           // 그 분의 «종가»
  "openPrice":     244500.0,           // 시가
  "highPrice":     244500.0,
  "lowPrice":      242000.0,
  "accumulatedTradingVolume": 931152   // ★이름과 달리 «그 1분의 거래량»(누적 아님 — 실측 확인)
}
```
★ `accumulatedTradingVolume` 은 이름이 오해를 부르지만 **분당 거래량**이다.
실측 09:00→09:01→09:02 = 931,152 → 316,635 → 145,106 으로 **감소**한다(누적이면 불가능).
현재 코드가 이 값을 그대로 거래량 히스토그램에 쓰는 것은 **옳다.**

### 2.3 ★실측 결과 (2026-08-04 · 삼성전자 005930)

| 확인한 것 | 결과 | 요건정의에 주는 의미 |
|---|---|---|
| `count=6` / `400` / `1000` | **셋 다 381행 동일** | **`count` 파라미터는 무시된다.** 코드의 `count=400` 은 사실상 장식 |
| 반환 구간 | `20260804090000` ~ `20260804153000` | 기본은 **최근 1거래일** 전체 |
| 행 수 | **381봉** | 09:00~15:19(380봉) + **15:30(종가 단일가) 1봉**. ★15:20~15:29 는 봉이 없다 |
| `startDateTime`·`endDateTime` 지정 | **동작함** — 0803+0804 = 762행 | ★**과거 분봉을 받을 길이 있다**(현재 미사용) |
| 얼마나 과거까지? | start 를 1개월/6개월/1년 전으로 줘도 **항상 2,605행 / 7거래일**(최초 20260727) | **보관 한도 = 최근 7거래일**. 그 이상은 어떤 파라미터로도 못 받는다 |
| `minuteUnit=5` | 무시(381행 그대로) | **5분봉 파라미터 아님.** 다분봉이 필요하면 1분봉을 **직접 리샘플링**해야 한다 |
| `.../minute/5` | 404 | 위와 동일 결론 |
| ETF (069500·091160·247540) | 정상 381행 | **ETF도 된다** — 화면이 주식/ETF 통합 목록인데 문제없음 |
| 없는 코드(000000) | **빈 배열** (에러 아님) | 실패 판정을 HTTP 코드로 못 한다 → 「빈 배열 = 데이터 없음」으로 취급 |

**참고로 같이 확인한 형제 엔드포인트** — `.../item/{code}/day?count=5` 는 **오늘 1행만** 반환:
`{"localDate":"20260804","closePrice":240000,"openPrice":244500,"highPrice":244500,"lowPrice":228000,"accumulatedTradingVolume":29246080,"foreignRetentionRate":46.61}`
(여기도 count 무시. 거래대금은 없음.)

---

## 3. 서버 계층 (PHP)

### 3.1 `classes/NaverFinanceAPI.class` → `getMinuteOhlc(string $code, int $count = 400)`

```php
$url = "https://api.stock.naver.com/chart/domestic/item/{$code}/minute?count={$count}";
$headers = ['User-Agent: '.$this->mobileUserAgent, 'Referer: https://m.stock.naver.com/'];
$res = $this->fetch($url, $headers);              // 공용 curl 헬퍼(타임아웃 10초·JSON 디코드)

// ① 가장 최근 날짜(maxDay)를 먼저 구한다 → ② 그 날 행만 남긴다  ※실측상 이미 하루치뿐이라 안전망 성격
// ③ 형식 변환 후 시간 오름차순 정렬
$out[] = [
  't' => 'YYYY-MM-DD HH:MM',                       // localDateTime 을 사람이 읽는 형식으로
  'o' => openPrice, 'h' => highPrice, 'l' => lowPrice,
  'c' => currentPrice,                             // ★종가 = currentPrice
  'v' => accumulatedTradingVolume,                 // ★분당 거래량
];
return ['success' => $out];   // 실패 시 ['error' => 메시지]
```
- 종목코드는 `preg_replace('/[^0-9A-Za-z]/','',...)` 로 세탁 (SSRF·인젝션 방지)
- **초 단위는 버린다**(`HH:MM` 까지만). 원본이 항상 `ss=00` 이라 손실 없음

### 3.2 `stock_analysis_api.php` — `module=stock & action=minute`

```php
// 부트스트랩: ob_start → env/cnt.inc → env/auth_fnc.php → require_login() → ob_clean → JSON 헤더
case 'minute': {
    $code = preg_replace('/[^0-9A-Za-z]/', '', (string)($_GET['code'] ?? ''));
    if ($code === '') { echo json_encode([]); return; }
    $api = new NaverFinanceAPI();
    $res = $api->getMinuteOhlc($code);            // ★count 인자 안 넘김 → 기본 400
    if (isset($res['error']) || !isset($res['success'])) { echo json_encode([]); return; }
    echo json_encode(array_values($res['success']), JSON_UNESCAPED_UNICODE);
    return;
}
```
**규칙**(이 API 전체 공통)
- 응답은 항상 `application/json; charset=utf-8`
- **빈/실패는 빈 배열 `[]` + HTTP 200**, 진짜 예외만 `{"error":"..."}` + 500
- ★**로그인 필수**(`require_login()`) — 미로그인 요청은 `/lg.php` 로 리다이렉트된다. 브라우저 밖에서 curl 로 검증할 수 없다는 뜻이다
- 받는 파라미터는 **`code` 하나뿐**. 날짜·기간·봉단위 파라미터가 **없다**

**응답 예시**
```json
[{"t":"2026-08-04 09:00","o":244500,"h":244500,"l":242000,"c":243000,"v":931152},
 {"t":"2026-08-04 09:01","o":243250,"h":243750,"l":240500,"c":241500,"v":316635}, ...]
```

---

## 4. 프론트 계층 (`style/dailychart.js` — 사이트 공용 차트 모듈)

### 4.1 데이터 어댑터

```js
function fetchMinute(code) {
  return fetch(API + '?module=stock&action=minute&code=' + encodeURIComponent(code),
               { credentials: 'same-origin' })
    .then(r => r.json())
    .then(raw => (Array.isArray(raw) ? raw : []).map(x => ({
      // ★KST 벽시계를 UTC 로 «취급» → 09:00 이 09:00 으로 보인다 (+09:00 을 붙이면 18:00 이 된다)
      time: Math.floor(Date.parse(x.t.replace(' ', 'T') + 'Z') / 1000),
      open: x.o, high: x.h, low: x.l, close: x.c, vol: x.v
    })));
}
```
★ **`'Z'` 트릭이 핵심**이다. 라이브러리(Lightweight Charts)는 UTC 로 축을 그리므로,
KST 시각을 UTC 인 척 밀어 넣어야 09:00 봉이 09:00 자리에 선다. 실제 UTC 오프셋을 주면 축이 9시간 밀린다.

### 4.2 차트 생성 옵션

```js
DailyChart.create('minuteChart', { theme:'dark', kind:'minute', resize:false });
```
`kind:'minute'` 이 바꾸는 것은 **시간축 표기 두 가지뿐**이다:

| 항목 | `kind:'day'`(기본) | `kind:'minute'` |
|---|---|---|
| 축 라벨 | 한글 날짜(`8월 4일`) | **`HH:MM`** |
| `timeVisible` | false | **true** |
| 툴팁 시각 포맷 | 날짜 | **시:분** |
| 그 외(캔들·거래량·테마·축 가두기 `fixLeftEdge`/`fixRightEdge`) | 동일 | 동일 |

### 4.3 분봉 차트가 **갖지 않은** 기능 (일봉과의 실질 차이)

생성 옵션에 아무것도 안 줬기 때문에 아래가 전부 꺼져 있다:

| 기능 | 분봉 | 이유 |
|---|---|---|
| `code` 옵션(=SUE 공시 마커) | ✗ | 안 넘김. 애초에 일 단위 이벤트라 분봉에 의미 약함 |
| `key`(사용자 지표·차트틀 저장) | ✗ | 안 넘김 → 지표·프리셋이 분봉엔 안 붙는다 |
| 기간 바(`periodBar`) | ✗ | 하루치 고정이라 고를 기간이 없다 |
| 높이 손잡이(`resize`) | ✗ (`resize:false`) | 위 일봉 블록의 손잡이 하나가 flex 로 둘을 함께 조절 |
| 주봉 전환·전체화면 | ✗ | 일봉 블록 전용 |
| 거래대금(`a`) | ✗ | 원천에 없음 |
| 자동 갱신 | ✗ | **종목 클릭 시 1회만** 로드. 화면의 LIVE 배지는 목록만 새로 그린다 |

---

## 5. 소비 화면 (전부 2곳)

### ① 상승종목분석 대시보드 — `stock_analysis.php?mode=updash`
```html
<div class="chart-block">        <!-- 위: 일봉/주봉 (메인) -->
  <div class="chart-host" id="dailyChart"></div>
</div>
<div class="chart-block bottom"> <!-- 아래: 분봉 (보조) -->
  <div class="chart-bar"><span class="lbl">분봉</span><span class="px">당일 09:00~15:30 · 1분</span></div>
  <div class="chart-host" id="minuteChart"></div>
</div>
```
```js
const FEAT_MIN = DailyChart.feats('panel.minute');            // 기능 스위치
if (!FEAT_MIN) { document.querySelector('.chart-block.bottom')?.remove(); }   // 꺼져 있으면 DOM 째 제거
...
const [news, d, m] = await Promise.all([
  fetch(`${API}?action=news&code=${s.code}`).then(r=>r.json()).catch(()=>[]),
  DailyChart.fetchDaily(s.code, 1000).catch(()=>[]),          // 일봉 1000영업일(≈4년) 한 번
  FEAT_MIN ? DailyChart.fetchMinute(s.code).catch(()=>[]) : Promise.resolve([]),   // ★꺼져 있으면 «조회 자체»를 안 함
]);
if (minute) minute.setData(m);
```
★ 설계 원칙: 기능을 끄면 **버튼만 지우는 게 아니라 네트워크 호출까지 없앤다**(「배지만 지우고 뒤에서 계속 부르면 왜 느리지가 된다」).

### ② 차트설정 갤러리 — `chart_gallery.php` 의 ⑤번 스타일 미리보기
```js
var c5 = DailyChart.create('c5', { theme:'dark', kind:'minute', screen:'', resize:true });
...
DailyChart.fetchMinute(code).catch(function(){ return []; })
```
갤러리는 사이트의 **살아있는 스타일 가이드**다 — 실제 사용처와 옵션이 어긋나면 안 된다는 규칙이 있다.

### 기능 카탈로그 등록 (`classes/ChartFeat.class`)
```php
'panel.minute' => [
    'g' => 'module', 'label' => '분봉 보조 차트', 'dep' => 'minute', 'def' => 0, 'ui' => 'panel',
    'desc' => '일봉 아래 당일 09:00~15:30 · 1분',
],
// 데이터 의존성 이름
'minute' => '분봉',
// 이 기능을 열어 준 화면 = updash 하나뿐
'updash' => [ 'data' => ['intraday','minute'], 'on' => ['overlay.intraday_ref','panel.minute'], ... ],
```
- 화면별 on/off 는 `chart_pref.features_json` 에 **카탈로그와의 차분만** 저장
- `'dep'=>'minute'` 은 「이 화면에 분봉 데이터가 있느냐」 — 없는 화면에선 체크칸이 비활성된다

---

## 6. DB — ★분봉은 저장되지 않는다

이 스키마(`economist73` · 127개 표)에 **분 단위 시세 테이블은 존재하지 않는다.**
`stock_analysis_api.php?action=minute` 호출은 **DB 를 한 줄도 읽거나 쓰지 않는다**(로그인 세션 확인 제외).

### 6.1 그래서 「없는 것」

| 없는 것 | 결과 |
|---|---|
| 분봉 원장 테이블 | 어제 분봉을 화면에서 볼 수 없다 |
| 분봉 캐시 | 같은 종목 재클릭 = 네이버 재호출(평균 수백 ms) |
| 분봉 수집 크론 | 7거래일이 지나면 그 데이터는 **영원히 사라진다**(네이버가 안 준다) |
| 분 단위 거래대금 | 원천에 없음 (일 단위 거래대금은 `krx_amt` 에 있음) |

### 6.2 참고 — 「일 단위」 시세는 이렇게 있다 (분봉 요건이 여기에 얹힐 수 있으므로)

| 테이블 | 내용 | 규모 |
|---|---|---|
| `krx_amt` | ★**전종목 일별 원장** `(code,d,amt,vol,o,h,l,c,mktcap,list_shrs,mkt,src)` · **원본주가** | 전종목 2024-08~ |
| `krx_daily` | KRX 전종목 일별(거래대금·시총·상장주식수) | 최근 10일만(prune) |
| `all_stock_info` | 전종목 **현재가 스냅샷**(TRUNCATE 갱신) | 2,759행 · 평일 9회 |
| `pf_daily` | 일봉 OHLCV · **수정주가** | 14종목 · 2021-02~ |
| `stock_daily_range` | 일별 **고·저만** | 332,593행 · 6개월 |
| `pf_sim_data` | 시뮬레이터용 일봉 통째 JSON | 20종목 · 각 10년 |

외부 소스는 3곳뿐이다 — **네이버**(일봉·분봉·현재가, 거래대금 없음) · **KRX 오픈API**(전종목 일별·거래대금) · **DART**(재무).
★ 새 데이터가 필요하면 「이미 있는 걸 쓸까 / 새로 받을까」를 먼저 판단하는 규칙이 있다(중복 수집 금지).

---

## 7. 제약·함정 (요건정의 시 반드시 고려)

1. **7거래일 절벽** — 네이버가 분봉을 최근 7거래일만 준다. 장기 분봉 분석은 **오늘부터 쌓기 시작해야만** 가능하다.
2. **1분 고정** — 5분·15분봉은 API가 안 준다. 필요하면 1분봉을 서버/JS에서 리샘플링(일봉→주봉 `resampleWeek` 와 같은 패턴).
3. **15:20~15:29 공백** — 종가 단일가 구간엔 봉이 없고 15:30 에 1봉으로 몰린다. 「분 인덱스 = 경과분」 가정을 세우면 어긋난다.
4. **거래대금 없음** — 분 단위는 `종가×거래량` 근사만 가능. (일봉 기준 근사 오차 실측: 중앙 0.99%, 두 날 대소 뒤집힘 0.79%)
5. **수정주가/원본주가** — 네이버는 수정주가 계열, KRX(`krx_amt`)는 원본. 분봉을 일봉과 이어 붙이면 액면분할 종목에서 어긋난다.
6. **로그인 필수 API** — 외부에서 curl 검증 불가. 서버 배포 검증은 SFTP 정본 grep 또는 로그인 세션으로 한다.
7. **응답 타임아웃 30초 / SAPI = apache2handler** — 크론으로 대량 수집을 붙이려면 `env/cronbg.inc` 의 **자기호출 bg** 패턴이 필수다(`fastcgi_finish_request` 없음). 크론은 루트에 새 파일을 만들지 않고 `cron_job.php` 의 `TASKS` + `cron/*.php` 로 넣는다.
8. **전종목 분봉 수집은 무거움** — 2,700종목 × 381봉 = 하루 약 103만 행. 종목당 요청 1회라 요청 수 자체가 2,700회다. 대상 축소(관심종목·포트폴리오·급등종목)가 현실적이다.
9. **`.class` 파일은 SFTP watcher가 못 잡는다** → 수동 업로드 필요. 일반 `.php` 는 자동.
10. **차트 모듈 수정 시 캐시버스트** — `style/dailychart.js?v=NN` 의 현재 값을 grep으로 확인한 뒤 **전 참조를 함께** 올린다.
11. **updash 의 정체성 제약** — 이 화면은 2026-08-02 사용자 결정으로 **「전략 무관 관찰 화면」**이다(오늘 뭐가/왜 오르나/어떤 흐름인가). 승률·신호칩 같은 **판정 기능은 싣지 않는다**(과거 폐기 이력 있음, 복원 금지).

---

## 8. 지금 못 하는 것 = 요건정의에서 결정할 것

| # | 질문 | 지금 | 가능한 선택지 |
|---|---|---|---|
| 1 | 과거 분봉을 보고 싶은가? | 당일만 | ⓐ API `startDateTime`/`endDateTime` 를 뚫어 **최근 7거래일**까지 즉시 확장(코드 소량) / ⓑ DB에 쌓아 **영구 보관**(표+크론 신설) |
| 2 | 5분·15분봉이 필요한가? | 1분만 | 1분봉 리샘플링(클라 or 서버). 봉 단위 선택 UI 필요 여부 |
| 3 | 장중 자동 갱신이 필요한가? | 클릭 시 1회 | 폴링 주기·부하(네이버 권장 폴링 7초, 다른 API 기준) |
| 4 | 어떤 종목을 쌓을 것인가? | 해당 없음 | 전종목(하루 103만 행·2,700요청) vs 포트폴리오/관심종목/급등종목 한정 |
| 5 | 분봉으로 무엇을 판정할 것인가? | 판정 없음(관찰만) | 판정을 넣는다면 **updash 가 아닌 다른 화면**이어야 한다(11번 제약) |
| 6 | 분 단위 거래대금이 필요한가? | 없음 | `종가×거래량` 근사 허용 여부 |
| 7 | 다른 화면에도 분봉을 열 것인가? | updash만 | `ChartFeat` 의 `SCREENS` 에 `panel.minute` 추가 + 해당 화면 `data`에 `'minute'` |

---

## 9. 파일 위치 요약

| 계층 | 파일 | 해당 지점 |
|---|---|---|
| 화면 | `stock_analysis.php` (`?mode=updash`) | `#minuteChart` · `FEAT_MIN` · `minute.setData(m)` |
| 화면 | `chart_gallery.php` | 스타일 ⑤번 `c5` |
| 공용 모듈 | `style/dailychart.js` | `fetchMinute()` · `kind:'minute'` 시간축 분기 |
| API | `stock_analysis_api.php` | `api_stock()` 의 `case 'minute'` |
| 크롤러 | `classes/NaverFinanceAPI.class` | `getMinuteOhlc()` · 공용 `fetch()` |
| 기능 카탈로그 | `classes/ChartFeat.class` | `panel.minute` · `DATA['minute']` · `SCREENS['updash']` |
| 설정 저장 | `chart_pref` 테이블 | 화면별 기능 차분(`features_json`) · 뷰 상태(`view_json`) |
