# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

**이코노미스트의 주식이야기** — PHP/MySQL 기반 개인 주식 분석 웹 애플리케이션.  
로그인 인증 후 ETF/주식 분석, 일간 뉴스 수집 기능을 제공한다.

## 기술 스택

- **백엔드**: PHP 8.4 (파일 단위 라우팅, 프레임워크 없음)
- **DB**: MariaDB 10.6.17, `economist73` 스키마
- **프론트**: Vanilla JS + ApexCharts/ECharts (`env/js/`, `style/`)
- **배포**: 웹 루트 직접 서빙 (Apache/Nginx 기준 `/` = `D:\Claude\www`)

## 부트스트랩 순서

모든 페이지는 맨 위에서 아래 순서를 따른다:

```php
require_once "./env/cnt.inc";      // DB 연결(mysqli + PDO) + 클래스 오토로더 + StockSummaryCache 초기화
require_once "./env/auth_fnc.php"; // 인증 함수
require_login();                   // 미로그인 시 /lg.php 리다이렉트
```

## 데이터베이스 연결 이중 구조

`env/cnt.inc`는 두 가지 연결 객체를 전역으로 제공한다.

| 변수 | 드라이버 | 용도 |
|------|---------|------|
| `$connect` | mysqli | 레거시 코드 |
| `$pdo` | PDO (utf8mb4, ERRMODE_EXCEPTION) | 신규 코드 — 반드시 이것 사용 |

새 쿼리는 항상 `$pdo` + Prepared Statement로 작성한다.

## 클래스 구조 (`classes/`)

확장자는 `.class`이며, `cnt.inc`의 `spl_autoload_register`가 자동 로드한다 (`/classes/{ClassName}.class`).

| 클래스 | 역할 |
|--------|------|
| `StockSummaryCache` | ETF 편입 요약 정보 전체를 최초 1회만 DB에서 읽어 메모리에 캐시 (Lazy Loading) |
| `StockRepository` | 종목 정보 / ETF holdings 조회 PDO 쿼리 집합 |
| `NaverFinanceAPI` | 네이버 금융 크롤링 |
| `UIHelper` | 정렬 아이콘 등 공통 HTML 생성 |
| `ChartHelper` | ECharts 트리맵 등 차트 HTML 렌더링 |
| `Stock_Analysis_Repository` | 주식 그래프/분석용 쿼리 |
| `data_processors` | 데이터 가공 로직 |

## 주요 페이지별 역할

| 파일 | 설명 |
|------|------|
| `etf_stock.php` | ETF/주식 분석 메인. `?mode=` 파라미터로 내부 라우팅 (`ef`, `si`, `eshl`, `elbs`, `slbe`, `gsnb` 등) |
| `stock_analysis_api.php` | 시세·차트 공용 데이터 API (`module=stock` top30·news·daily·minute / `ind`·`sue`·`band`). 소비자는 단타와 `style/dailychart.js` |
| `analysis_model.php` | 분석 모델 정의 |
| `daily_news.php` | 일간 뉴스 수집 및 표시 |
| `stock_thema_news.php` | 종목 테마 뉴스 |
| `cron_job.php` | **모든 크론의 유일한 진입점** (레지스트리+디스패처). 구현은 `cron/` — 아래 「크론」 절 |
| `rss_feed.php` | RSS 피드 출력 |
| `lg.php` / `gi.php` | 로그인 / 회원가입 |
| `classes/data_upload.php` | 데이터 수동 입력 UI |

## 인증 흐름

- `require_login()` → 세션에 `$_SESSION['usr_name']` 없으면 `/lg.php?url=...` 리다이렉트
- 로그인 성공 시 `acc_log_on()` → `tbl_users` 조회 + `password_verify()` + `tbl_users_log` 기록
- 기본 로그인 후 랜딩: `etf_stock.php?mode=si`

## `etf_stock.php` 다중 창 패턴

행 클릭 시 `openCommonFrames(rowId, stockCode, params, targets, curPhp)`를 호출해 여러 named window를 열어 종목 상세를 표시한다. `urlMap`에서 target 이름(`etf_t1`, `etf_d1`, `etf_d2`, `etf_d5`)과 `mode`를 매핑한다.

## 크론(배치)

**`CRON.md` 를 먼저 읽는다.** 전체표·잡별 상세(역할·소스·쓰는 테이블·실측 소요·이어받기)·외부 API 한도·테이블 역인덱스·신규 크론 체크리스트가 들어 있다.

구조 — **크론 파일을 루트에 새로 만들지 않는다.**

| 위치 | 역할 |
|------|------|
| `cron_job.php` | 유일한 진입점. `TASKS` 레지스트리가 "어떤 파일을 어떤 인자로 부를지" 결정 |
| `cron/*.php` | 실제 구현. env 는 `$_SERVER['DOCUMENT_ROOT']` 기준으로 읽는다 |
| `cron/_boot.php` | CLI 실행 시 `DOCUMENT_ROOT` 세팅 (각 파일 맨 위에서 require) |
| `env/cronbg.inc` | 30초 타임아웃 우회(자기호출 bg) + 로그 + 시간예산 공용 헬퍼 |

- 크론 사이트(cron-job.org)에 등록되는 URL은 **`cron_job.php?task=<이름>&k=…` 하나뿐**이다.
  모드·옵션을 바꿀 때 **크론 사이트를 건드리지 않고 `TASKS` 만 고친다.**
- 목록 `?task=list&k=…` · 실행 전 확인 `&explain=1` · bg 로그 `&log=1`
- 핵심 제약: 응답 타임아웃 **30초**, SAPI 가 **apache2handler**(`fastcgi_finish_request` 없음)
  → 30초 넘는 잡은 **`env/cronbg.inc` 의 자기호출 bg** 만 통한다 (옛 `Connection: close` 패턴은 무효).

## 환경 설정 파일

| 파일 | 내용 |
|------|------|
| `env/cnt.inc` | DB 자격증명 (`.gitignore`로 보호) |
| `env/e.fnc` | 공통 유틸 함수 |
| `env/inf.fnc` | 기타 정보 함수 |
| `env/prj.fnc` | 프로젝트별 함수 |
| `env/header.php` | 공통 HTML 헤더 + 상단 네비게이션 바 |

`env/cnt.inc`에는 DB 비밀번호가 평문으로 있다. 절대 커밋하지 않는다 (`.gitignore` 적용됨).

## CSS / JS

- `style/economist.css`, `style/economist.js` — 사이트 공통 스타일·스크립트
- `env/js/apexcharts.js`, `env/js/calender.js` — 외부 라이브러리 로컬 복사본

### 검색 자동완성 — ★사이트 공통 규칙 (2026-08-05)

**자동완성 목록이 뜨는 검색창은 예외 없이 키보드로 고를 수 있어야 한다** — `↓`/`↑` 이동 · `Enter` 선택 · `Esc` 닫기.
마우스로만 고를 수 있는 검색창을 새로 만들지 않는다(사용자 지적 — 단타 「종목 추가」가 그랬다).

- 단일본은 **`style/acnav.js`(`AcNav.attach()`)** 다. keydown 을 화면에 다시 적지 않는다.
  ```js
  AcNav.attach(inp, { box:'#dtSug', item:'.s', pick:function(el,i){…}, close:function(){…} });
  ```
- `/stock` 은 **`pf_acnav_js()`** 한 줄로 싣는다(once 가드). 다른 화면은 `<script src="/style/acnav.js?v=1">`.
- **하이라이트 클래스(기본 `on`)의 CSS 를 반드시 함께 둔다** — 없으면 이동은 되는데 눈에 안 보인다.
- 목록은 모듈이 **그때그때 DOM 에서 읽는다** — 검색 결과를 다시 그려도 모듈에 알릴 필요가 없고,
  화면의 `render()` 는 하이라이트를 신경 쓰지 않는다(그 일은 모듈 것이다).
- 아직 자기 구현으로 도는 곳 — `etf_stock.php`(종목 검색). **동작은 같다**(이미 화살표가 먹는다).
  그 파일을 손볼 일이 생기면 `AcNav` 로 옮긴다.
- ★모듈 파일 주석에 `</script>` 글자를 넣지 않는다 — 인라인으로 붙여 검증할 때 스크립트가 그 자리에서 끊긴다.

### 종목 목록 배지 — ★사이트 공통 규칙 (2026-08-05)

**관찰 화면의 종목 목록은 같은 배지를 같은 어휘·같은 색으로 단다** — ETF 편입 수(`.etfb`)·퀀트 배지(`.qb`/`.qbx`).
쓰는 곳: 단타(`/stock?mode=short`)의 두 목록 — 「상위 종목」(탐색)과 「내 목록」.

| 층 | 단일본 | 무엇 |
|----|--------|------|
| 판정 | `stock/lib/quant.php` (`quant_badge_many`) | 무엇이 매집형인가·박스가 어떤 상태인가 (krx_amt·krx_surge 원장) |
| 그리기·색 | `style/quantbadge.js` (`QuantBadge`) | HTML·CSS·억/조 표기(`eok`). CSS 는 모듈이 `<style>` 로 심는다 |

- 화면은 **고르기만** 한다 — 임계·어휘·색을 화면에 다시 적지 않는다(`Thr.class`·`ChartFeat` 와 같은 패턴).
- 좁은 목록(단타 사이드바)은 바깥 요소에 **`qb-sm`** 클래스를 준다(크기 규칙도 모듈 소유).
- 서버 입력은 `[code => ['price','rate','amtEok']]` — **`amtEok` 는 억원**(`all_stock_info.stock_vol_cap`).
  `krx_amt.amt` 의 **원 단위와 다르다**. 회전율은 거래대금÷시총이라 **저장하지 않는다**.

## /stock 포트폴리오 — ★불변 규칙 (2026-08-03)

**`요건정의서_v0.4_주식포트폴리오.md` 를 먼저 읽는다.** 아래는 그 문서가 낳은 여섯 규칙이며,
어기면 「화면끼리 다른 말을 하는」 상태로 되돌아간다. 전부 실측으로 값을 치른 것들이다.

1. **포트폴리오 화면에 탐색 판정(퀀트신호·퀀트경로·트리거)을 세우지 않는다.**
   여기는 *이미 산 종목*을 보는 자리라 오늘의 퀀트 판정은 편입 판단과 무관하다.
   예외는 둘뿐 — 「계단관통↓」(경보 = 판정의 *결과*)과 「편입 당시」(`pf_entry_snapshot` = *기록*).
   ★원장에서 **오늘 기준으로 계산한 시장 지표**(모멘텀 칩 등)는 공통층이라 허용. 금지 기준은
   「시장을 읽었나」가 아니라 「**판정을 가져왔나**」다.
2. **`krx_surge` 를 포트폴리오 로직에서 읽을 때는 반드시 `pf_position.surge_event_d` 를 경유한다.**
   `SELECT MAX(d) FROM krx_surge …` 패턴을 새로 만들지 않는다 — 그러면 경보가
   「내가 산 이유였던 박스」가 아니라 「가장 최근에 뜬 아무 박스」를 뜻하게 된다.
3. **임계값 리터럴을 새로 적지 않는다 — `classes/Thr.class` 를 경유한다.**
   문서 화면(검증·신호분석)의 *설명 문구*도 상수에서 보간한다. 손으로 적으면 상수를 고칠 때
   글이 조용히 거짓이 된다(실제로 「임계값은 전부 실측에서 왔다」가 틀린 문장으로 남아 있었다).
4. **`KrxAmt::invalidateSurge()` 에 `krx_surge_event` 삭제를 넣지 않는다.** 그 표는 append-only 다.
   `krx_surge` 는 크론이 매일 범위 삭제하는 **캐시**이고, 이벤트 표는 그걸 견디라고 따로 있다.
5. **차수 지연 판정 적용 3곳(`pf_load_calc` · 종목상세 · api payload)은 함께 고친다.**
   하나만 고치면 같은 종목이 화면마다 다른 판정을 낸다. `pf_calc_closed` 도 같은 3곳이다.
6. **새 테이블은 접두어로 소속을 밝힌다** — 포트폴리오 `pf_` · KRX 원장 계열 `krx_`.
   (economist73 은 127 테이블을 공유하는 스키마다.)

★ 자주 밟는 함정: `Pf::positionGet()` 은 `pf_stock`·`pf_portfolio`·`pf_rule_set` 과 **INNER JOIN** 이라
어느 하나라도 비면 **행 전체가 null** 이다. 컬럼 하나가 필요하면 단일 테이블로 읽는다.

## 차트 (일봉/주봉/분봉) — ★업무 규칙

**사이트의 모든 시세 차트는 `style/dailychart.js` 공용모듈로만 만들고, 반드시 `chart_gallery.php`(차트설정)에 정의된 구성 5종 중 하나를 기준으로 한다.** 임의 옵션 조합의 "맨몸 차트"를 새로 만들지 않는다.

| 구성 | 테마 | 특징 | 사용처 |
|------|------|------|--------|
| ① 기본형 | light | 캔들 + 거래량 | 재무분석(fund) |
| ② 포트폴리오형 | light | 가격선 + 체결 칩 | 포지션 상세·종목추가(박스 사다리) |
| ③ 시뮬레이터형 | light | 계단선 + 마커 텍스트(`chips:false`) | 시뮬레이터(sim) |
| ④ 분석형 | dark | 당일전고(`todayHigh`) + 현재가 | 단타(`mode=short`) 일봉 패널 |
| ⑤ 분봉 | dark | `kind:'minute'` | 분봉 뷰 · **단타(`mode=short`)** · 패턴분석(박스 상향돌파) 카드 우측 |

### 관심차트 그룹 — ★사이트 공통 규칙 (2026-08-07)

**보다가 괜찮은 차트를 그룹으로 묶어 두고 그 그룹만 따로 본다.** 단일본은 `classes/ChartFav.class`
(표 `chart_fav`·`chart_fav_item`) · API `stock/api.php?module=fav` · 지금 쓰는 곳은 패턴분석(불꽃형).

- **담는 단위는 「종목」이 아니라 「신호 한 건」**(`code` + `d`) — 한 종목이 여러 날 신호를 내므로
  종목으로 담으면 어느 날 그림인지 잃는다. 화면이 주고받는 키는 `code|d` 문자열이다.
- **스냅샷을 뜨지 않는다 — 참조만 갖는다**(`gift_item` 과 **반대**). 판정이 잡을 다시 돌려 바뀌면
  관심차트도 함께 바뀌어야 한다. 값을 복사하면 원본이 고쳐진 뒤 옛 판정이 따로 논다.
- **`src` 로 어느 화면에서 담았나를 남긴다**(`ChartFav::SRCS`). 화면을 늘리려면 거기 한 줄.
- **「따로 불러서 보기」는 주소다**(`&g=<id>`) — JS 로 숨기지 않는다. 북마크·공유되고
  결과 필터와 겹쳐 쓸 수 있다(「그 그룹 안의 손절만」).
- **그룹 목록의 단일본은 서버 응답**이다 — 담기·삭제·이름변경이 `groups` 를 통째로 돌려주고
  화면의 `renderGroups()` 가 필터 셀렉트·담기 셀렉트·관리 표를 **함께** 다시 그린다.
- ★★**「담기」 버튼을 두지 않는다 — 체크칸이 곧 담김이다**(2026-08-07 사용자 지시).
  체크=담기·해제=빼기이고 **버튼 없이 즉시 저장**한다(실패하면 체크를 되돌린다).
  버튼을 두면 「고르고 → 위로 올라가 누르고 → 다시 내려와 다음 페이지」가 되는데,
  버튼을 아래에 «하나 더» 다는 것은 왕복을 반으로 줄일 뿐 없애지 못한다.
  셀렉트는 둘이다 — **보기**(주소 `&g=`)와 **담을 그룹**(체크칸의 뜻을 정한다).
  체크의 초기 상태는 서버가 심은 `FL_MEMBER`(`code|d` → 그룹 id)에서만 나오고,
  담을 그룹을 바꾸면 그 그룹의 것으로 다시 그린다.
- 그룹을 지우면 담긴 것도 함께 간다(FK CASCADE) — 신호 자체는 그대로다.

**차트에는 축이 둘이다 — 위 표는 「스타일」이고, 「기능」(도구모음·레이어)은 `classes/ChartFeat.class` 가 원본이다.**

| 축 | 무엇 | 원본 | 화면 |
|----|------|------|------|
| 스타일 | 어떻게 그리나 (색·마커 모양·테마) | `style/dailychart.js` + 위 구성 5종 | 차트설정 > 스타일 5종 |
| 기능 | 무엇을 보여주고 조작하게 하나 | `classes/ChartFeat.class` (카탈로그) | 차트설정 > **화면별 구성** |

- 기능 목록을 **화면·JS 어디에도 다시 적지 않는다** — 늘리려면 `ChartFeat::FEATURES` 에 한 줄, 화면에 열어 주려면 `SCREENS` 에 한 글자. (Thr.class 와 같은 「원본 → 화면 표시」 패턴)
- 화면은 `ChartFeat::vals('<화면키>')` 로 구성을 읽고 `ChartFeat::script()` 로 JS 에 심는다 → JS 는 `DailyChart.feats('키')`. 안 심은 화면은 전부 켜진 것으로 본다.
- 저장은 `chart_pref.features_json` (카탈로그와의 **차분만**). ★이 표의 행은 지우지 않는다 — 축 행(`fund_day`)과 화면 행(`fund`)이 한 표에 살아서, `prefSave`/`presetDelete` 가 DELETE 하면 기능 구성이 함께 날아간다 (그래서 둘 다 `preset_id=0` UPDATE 로 바꿔 뒀다).
- 데이터 의존성이 없는 화면에서는 켤 수 없다(체크칸 비활성 + 「데이터 없음」). 런타임에 데이터가 없으면 **조용히 생략** — 빈 패널·오류 금지.
- **시간축·가격축은 「데이터 안」에 가둔다**(2026-08-03). 시간축 `fixLeftEdge`+`fixRightEdge` — 최신 봉이 항상 오른쪽 끝이고 휠 줌아웃은 **왼쪽(과거)만** 늘어나며 전부 보이면 거기서 멎는다(빈 여백 금지). 가격축은 **보이는 봉의 고·저 ±10%**(`padPct`)로 꽉 채우고, 라이브러리의 비율 여백은 되빼서 눈금에 영향이 없게 한다. **지표선은 가격축 스케일에서 제외**(멀리 있는 레벨 하나가 캔들을 눌러 납작하게 만든다) · **가격선(누적단가·자동매도가·다음매수가)은 범위를 넓혀서라도 포함**(내가 산 자리가 사라지면 안 된다).
- **★기간(조회 구간)의 주인은 「화면」이지 「차트틀」이 아니다**(2026-08-03). 차트틀은 여러 화면이 공유하므로 거기에 기간을 넣으면 재무분석에서 맞춘 240일이 시뮬레이터까지 따라간다 — 화면마다 차트 폭이 다른데. 우선순위 **①화면 기억(`chart_pref.view_json` 의 축별 `{index,bars}` · 축 전환하면 그 축의 기억으로)** → ②차트틀의 `view`(그 차트틀을 **처음 쓰는** 화면의 시작값) → ③화면 기본값 `defaultIndex`. 기간을 만지면 ①만 갱신되고 차트틀은 안 건드린다. 차트틀을 **고르는** 것은 명시적 행동이라 그때만 ②가 적용된다(그리고 ①이 된다).
- **차트 높이는 사용자가 끌어서 정한다**(2026-08-03). 차트 아래 가장자리의 손잡이(`.dc-rsz`)를 끌면 바뀌고 놓는 순간 **그 화면**이 기억한다(`chart_pref.view_json` 의 `{"h":…}` · 화면 행). 차트틀에 넣지 않는 이유 — 차트틀은 여러 화면이 공유하는 물건이라 한 화면에서 늘리면 다른 화면까지 늘어난다. 화면은 `ChartFeat::boot('<화면>', $pdo)` 한 줄로 `DC_SCREEN`·`DC_FEATS`·`DC_VIEW` 를 심는다(높이를 나중에 받아 오면 화면이 튄다). flex 로 자리를 나눠 갖는 배치(단타)는 `resizeTarget` 으로 늘릴 요소를 지정한다.
- **관찰용 기준선은 한 스위치다**(`overlay.intraday_ref`) — **직전고가**(`prevHigh` · **오늘을 뺀** 실린 봉의 최고가 점선 · 단타 1분봉) · **당일전고**(`todayHigh` · 직전 60봉 최고가 · 일봉 패널) · **현재가**(`curPrice`).
  ★**분봉의 기준선은 「직전고가」 하나다**(2026-08-05 사용자 지시로 당일전고·기간고가를 걷어냄). 물음이 **「오늘 저 선을 뚫었나」**라서 **오늘 봉을 계산에서 뺀다** — 넣으면 장중 신고가마다 선이 따라 올라가 **영영 안 뚫린다**. 실린 봉이 오늘치뿐이면(미리보기) 직전이 없어 **선을 안 그린다**.
  **창을 따라가지 않는다** — 휠마다 값이 달라지면 기준선이 아니다. 대신 **켜져 있을 때만 가격축에 포함**한다(지표선과 반대 규칙: 저건 안 켰는데 생기는 선이라 제외).
  ★★**LWC 4.1.3 은 autoscale 결과를 캐시한다** — 시리즈 `applyOptions`·priceScale `autoScale` false→true·`createPriceLine`/`removePriceLine` **넷 다 축을 다시 재지 않는다**(탐침 3회로 확인). `setData` 는 되지만 **마커를 지운다**. 유일한 답은 **`series.update(마지막 봉)`** — 캐시를 깨면서 마커를 지킨다. 그래서 봉→점 변환은 `mainPoint()` 단일본이다(적재와 재적용이 다른 모양을 만들면 마지막 봉만 어긋난다).
- **단일가 봉(09:00 시가·15:30 종가)의 거래량 왜곡은 「차트」가 아니라 「수식(사용자 지표)」에서 다룬다**(2026-08-05 사용자 판단). 대형주는 단일가 한 봉이 거래량 축을 다 먹어 나머지 380봉이 바닥에 붙는데, 차트에서 축을 손보는 방식은 **원복했다** — 차트는 원장을 있는 그대로 그린다.
  ★**어느 방식이든 봉을 «지우는» 선택지는 없다** — 지우면 ①1/3분·일봉 거래량 합이 어긋나 `krx_amt` 대조가 깨지고 ②그 시각의 체결이 없던 일이 되며 ③마지막 봉을 빼면 분봉의 종가가 **15:19** 가 되어 일봉 패널과 다른 말을 한다(장중엔 마지막 봉이 곧 현재가다). 첫 봉을 빼면 시가가 09:01 값이 된다.
  참고 — 분봉 `time % 86400` 이 그대로 KST 초다(09:00=32400 · 15:30=55800). 원장에 **시간외는 없다**(정규 1일 381봉 = 09:00~15:19 380 + 15:30 1 · `Dt::BARS_FULL`).
- **수평선(사용자가 긋는 지지·저항)의 주인은 「종목」이다**(2026-08-07 · 표 `chart_hline` · 기능 키 `draw.hline`).
  화면·축·기간 어디에도 매이지 않는다 — 재무분석에서 그은 선이 단타 분봉에도 같은 자리에 선다.
  같은 종목을 보는 차트들은 **한 배열을 나눠 쓴다**(`hlShare`) — 그래서 분봉에 그으면 일봉 패널에 바로 뜬다.
  ★배열을 갈아끼우지 않고 **내용만 바꾼다**(`splice`) — 참조가 갈리는 순간 화면끼리 다른 말을 한다.
  ★**라이브러리 `priceLine` 을 쓰지 않는다** — 잡을 수도 끌 수도 없다(히트 판정이 없다). 박스와 같은 HTML 층이라
  덤으로 **가격축 autoscale 에서 빠진다**(멀리 그은 선 하나가 캔들을 납작하게 만들지 않는다 · 지표선과 같은 규칙).
  ★**가격은 호가 단위로 스냅**한다(2,000/5,000/20,000/50,000/200,000/500,000 경계) — 없으면 62,412 처럼
  「있을 수 없는 값」에 선이 선다. 정확한 값이 필요하면 편집 모달의 가격칸에 적는다.
  ★도구 버튼은 **차트 안**(왼쪽 위)에 산다 — 도구모음에 두면 **기간 바가 없는 화면**(단타 분봉 패널)에서 못 긋는다.
  선 하나를 놓으면 **그리기 모드가 스스로 꺼진다**(계속 켜 두면 차트를 못 끌어 「갇힌」 느낌이 든다).
  ★기능을 꺼도 **선은 지워지지 않는다** — 「감추기」와 「지우기」를 섞지 않는다.
  ★**분봉 축에는 SUE 를 얹지 않는다**(같은 날 발견) — 공시일은 날짜라 초 단위 `time` 과 비교가 성립하지 않아
  **모든 공시가 마지막 봉에** 붙는다. 「뜻이 없는 축에는 안 나온다」는 지표의 축 규칙과 같다.
- **SUE 공시 마커는 모듈 소유다**(2026-08-03, 모든 차트). 화면은 `create({code})` 또는 `dc.setCode(code)` 로 **종목코드만** 넘기고, 받아오기(`?module=sue`)·「공시 다음 거래일」 스냅·기간 바의 「SUE 공시 N」 토글은 전부 `dailychart.js` 가 한다. 화면에서 마커를 직접 만들지 않는다. 계산 단일본 = `stock/lib/sue.php`(`pf_sue_marks`).

- 새 화면에 차트를 넣을 때: 위 표에서 구성을 고르고 **같은 create() 옵션 세트**를 쓴다. 기존 구성으로 안 되면 **갤러리에 구성을 먼저 추가**한 뒤 사용한다 (갤러리 = 살아있는 스타일 가이드 — 실제 사용처와 어긋나면 안 된다).
- **시간축은 넷이다 — `day`·`week`·`month`·`min`**(분봉, 2026-08-04 신설 · 단일본 `ChartIndicator::TFS`).
  축은 **지표 변수 세트·차트틀·`chart_pref` 축 행(`<키>_min`)을 함께 고르는 키**다. 분봉 차트는 만들 때부터 `_tf='min'` 으로 산다.
  변수 칸이 **비면 일 값**을 쓴다(주·월과 같은 규칙) — 분봉만 다른 값이 필요할 때만 채운다.
  ★**거래대금(`AMT`)은 원 단위**다 — 100억 = `10000000000`. 실제값은 `krx_amt.amt`(원), 없으면 종가×거래량 근사.
  ⊕**지표는 「십자선 값 표시」를 고를 수 있다**(2026-08-07 · `chart_indicator.hover` · 지표 관리 폼의 체크칸).
  켜면 마우스가 짚은 봉의 값이 **범례**에 뜬다(시각을 함께 적는다 — 날짜 없이 숫자만 바뀌면 「지금 값」으로 읽혀 거짓이 된다).
  끄면 늘 마지막 값. ★**차트가 아니라 지표의 성질이다**(사용자 판단) — 계단선(최고거래량 H/L)은
  「지나간 단계가 얼마였나」를 묻지만, 이동평균처럼 최근값만 보는 선은 따라다녀 봐야 숫자가 흔들리기만 한다.
  ★**선 위에 숫자를 띄우지 않는다** — 캔들과 겹쳐 오히려 안 읽힌다(차트 안에 글자를 넣지 않는 규칙과 같은 자리).
  ★기본 **끔** — 옛 지표의 범례가 어느 날 갑자기 마우스를 따라다니면 그것이 곧 「다른 말」이다.
  ⊕**지표는 「보여 줄 축」을 고를 수 있다**(2026-08-05 · `chart_indicator.tfs_json` · 지표 관리 변수표 머리글의 체크칸).
  ★**빈 값 = 전 축**이다(넷 다 켜도 빈 값으로 접어 저장) — 그래서 체크칸 이전의 옛 지표가 조용히 사라지지 않는다.
  판정 단일본은 `DailyChart.indTfOk(def, tf)` 하나뿐 — 그리기·칩·「＋지표」 모달이 전부 이것만 본다.
  안 고른 축에서는 **선도 칩도 목록에도 안 나온다**. ★다만 `applied`(적용 상태)에서는 **빼지 않는다** — 축을 되돌리면 살아나야 하고, 「차트저장」이 그 축 세트를 통째로 덮어쓰므로 빼면 저장이 지표를 잃는다.
- **차트틀 삭제는 지표 바 셀렉트의 「⚙ 차트 관리(삭제)…」 하나뿐**(2026-08-05 · `openChartManager()` → API `preset_del`).
  목록은 **축과 무관하게 전부** 보여 준다 — 지우려고 축을 옮기면 그 차트가 «적용»되는 부작용이 딸린다.
  ★지금 쓰던 차트를 지워도 **그려져 있는 지표는 그대로 둔다**(선택만 푼다) — 「그 축에 차트가 없을 때」와 같은 규칙.
- 화면 키(`key`)를 지정해 **사용자 지표·차트틀(차트저장)이 적용**되게 한다. 같은 업무 계열 화면은 키를 공유한다
  (포트폴리오 계열 = `position` · **관찰용 다크 일봉 = `short`** · 단타 메인 분봉 = `shortmin`).
  ★`short` 는 2026-08-05 까지 `updash` 였다 — 상승종목분석 화면이 삭제되면서 옮겼다(이관 = `mig_updash_to_short.php`).
  ★**공유되는 것은 지표·차트틀뿐이다** — 기간·높이는 화면이 따로 기억한다(`chart_pref.view_json`). 차트 폭이 화면마다 다르다.
- `dailychart.js` 수정 시 캐시버스트 `?v=` 를 **grep으로 현재 값 확인 후** 전 참조를 함께 올린다.

## /stock 단타 (`mode=short`) — ★불변 규칙 (2026-08-04)

**`단타_요건정의_v0.4.md` 를 먼저 읽는다.** 분봉을 처음으로 **DB 에 쌓는** 기능이라, 아래 규칙을 어기면
「어제 분봉이 사라졌다」·「거래량이 소스마다 다르다」로 되돌아간다.

1. **종목코드는 언제나 6자리로 저장한다.** 키움의 `_AL`(SOR)·`_NX` 접미사는 **요청할 때만** 붙인다.
   저장으로 새 나가면 `krx_amt`(CHAR(6))·`all_stock_info` 와의 조회가 통째로 어긋난다 — 문지기는 `Dt::cleanCode()`.
2. **거래일·보관 창은 `krx_amt` 로 판정한다** — 휴장일 표를 새로 만들지 않는다(퀀트·포트폴리오와 같은 규칙).
   ★그 오늘 행은 **15:50 `dart_eod`** 가 넣는다. 그래서 수집 크론이 **16:25** 다. 앞으로 당기면 매일 「휴장일」로 오판한다.
3. **만료 삭제는 반드시 수집 «다음»** (`cron/dt_min.php` 의 ②단계). 순서가 뒤집히면 수집 실패한 날
   삭제만 돌아 보관 일수가 조용히 줄어든다.
4. **주문 API 는 부르지 않는다.** `Kiwoom::TR_MAP` 이 조회 TR 화이트리스트고, 그 밖의 `api-id` 는 예외를 던진다.
   앱키·시크릿은 `env/kiwoom.inc`(웹 루트 밖·gitignore), **토큰만** `dt_token` 에 캐시한다.
5. **★키움 봉 시각은 「시작」이다 — `Kiwoom::TS_BASE='start'`(2026-08-04 실측 확정).** 한 곳에서만 정한다.
   근거는 네이버 대조(같은 분 288 : 1분 당겨 89)와 봉 경계(09:00 있고 08:59 없음 / 15:19 있고 15:20 없음 / 15:30 단일가).
   재확인은 `php cron/dt_min.php job=diag code=…`. 잘못 잡으면 전 데이터가 1분씩 밀리고 **조용히** 깨진다.
   **★거래량 기준도 하나로 묶는다 — `KIWOOM_NO_SOR=true`(KRX 단독)**. `_AL`(SOR)은 NXT 를 더해 네이버의 1.49배가 되는데
   (실측 43,555,914 vs 29,246,067), 네이버가 **폴백이자 장중 실시간** 소스라 한 종목의 하루 봉에 두 기준이 섞인다.
6. **분봉과 일봉을 한 시계열로 합치지 않는다.** 분봉은 수정주가·거래대금 없음, 일봉은 `krx_amt` 실제 거래대금 병합.
   화면에서 **별개 패널**로 두고 crosshair 도 **분봉끼리만** 잇는다.
7. **일봉 패널은 기존 경로(`stock_analysis_api.php?module=stock&action=daily`)를 그대로 읽는다** — 새 수집 경로를 만들지 않는다.
   같은 이유로 `DailyChart.fetchMinute()`(갤러리·미리보기 정본)도 손대지 않고 `fetchMinuteRange()` 를 따로 냈다.
8. **메인 분봉은 1분 고정**(2026-08-04) — **보관 창 전부(10거래일)** 를 한 번에 싣고 창만 옮긴다
   (`[당일]`=300봉 · `[5일]` · `[전체]`=전부 · 그 사이는 휠 · 「몇 봉인가」는 곱셈이 아니라 실린 봉을 센다).
   **보조는 메인보다 한 단계 위(5분) · 10거래일.** 기간이 같아도 **단위가 다르면** 두 패널이 다른 말을 한다(메인=세부·보조=맥락).
   ★분봉의 「당일전고」는 `todayHigh:'session'`(**오늘 고가**)다 — 일봉의 60봉 규칙을 쓰면 「직전 60분」이 되어 이름이 거짓이 된다.
9. **새 판정을 세우지 않는다** — 이 화면은 **보는 자리**다. 승률·신호칩 같은 것을 여기서 **만들지** 않는다.
   ★다만 **이미 있는 관찰 배지를 가져다 다는 것은 허용**이다(2026-08-05 사용자 지시로 두 목록에
   ETF 편입 수·퀀트 배지·시총·회전율을 붙였다). 금지 기준은 「배지가 보이나」가 아니라
   **「여기서 판정을 새로 내렸나」**다. 그래서 판정은 `stock/lib/quant.php`(서버 단일본),
   그리기·색은 `style/quantbadge.js`(모듈 단일본)가 맡고 화면은 **고르기만** 한다.
10. **새 표는 `dt_` 접두어.** `st_` 를 쓰지 않은 이유 — `stock_financial`·`stock_daily_range`·`all_stock_info` 가
    이미 있어 그것들의 축약처럼 읽힌다(계열 구분이 흐려진다).
11. **★「보는 것」과 「담는 것」을 섞지 않는다**(2026-08-05). 담기(`dt_pool`)는 조회가 아니라 **약속**이다 —
    매일 크론이 그 종목을 영구 수집하고, 20칸 중 하나를 먹고, 넘치면 **맨 아래를 봉째로 지운다**.
    그래서 목록을 훑는 클릭은 **아무것도 쌓지 않는다**(미리보기 = 네이버 당일 1분봉 + 일봉).
    원장 적재는 **「＋」를 누르는 명시적 행동**에만 붙는다.
    ★미리보기에서 **보조 분봉은 비고 [5일]·[전체]는 잠긴다** — 실린 것이 오늘치뿐이라 켜 두면 거짓말이 된다.
    빈 패널에는 **「왜 비었는지」를 적는다**(빈 칸은 고장으로 읽힌다).
12. **★★보유 종목을 `dt_pool` 에 «담지» 않는다 — 수집 «대상»만 넓힌다**(2026-08-05).
    목록은 탭 둘(**단타 | 보유**)이고, 수집 대상 단일본은 **`Dt::targetCodes()`**
    = `dt_pool`(active=1) **∪** 살아있는 포지션(`pf_position.status <> 'closed'`).
    크론의 네 단계(수집·치유·분할감지·감시)가 전부 그 하나를 본다 — 한 곳만 넓히면 어긋난다.
    ★**담으면 안 되는 이유 둘** ①`dt_pool` 은 상한 20 을 **FIFO 로 지우는** 표라
    (`poolAdd` → `poolEvictLast` → `poolRemove` → `DELETE dt_min`) 탐색하다 「＋」 한 번 누른 것이
    **내가 산 종목의 봉을 통째로 지운다** ②보유는 이미 `pf_position` 이 정한 사실이라
    복사하면 청산 뒤에도 남는 「따로 노는」 상태가 생긴다. **참조로 두면 그럴 자리가 없다.**
    ★**`prune()` 의 해지잔여 삭제에서 보유를 뺀다** — 옛날 단타에서 «해지»한 종목을 나중에 사면
    `dt_pool` 에 `active=0` 으로 남는데, 안 빼면 **수집 대상이면서 매일 봉이 지워지는** 밑 빠진 독이 된다
    (로그엔 성공으로 남아 티도 안 난다).
    ★**「보유」 탭에는 담기 칸·× 빼기·드래그가 없다** — 그 목록은 포트폴리오가 정한다.
    상위 목록·검색의 담기도 **보유 종목이면 막는다**(이미 수집되니 20칸을 먹을 이유가 없다).
    ★**초기 10거래일은 「단타 화면을 열 때」 화면이 당겨 온다**(`held_init` · 2026-08-05 사용자 선택).
    보유엔 「＋를 누르는 순간」이 없어 `pool_init` 자리가 없는데, 크론에만 맡기면 **편입한 날 분봉을 못 본다**.
    ★훅은 **단타 화면 한 곳**이다 — `Pf::positionSave()` 에 넣지 않는다(편입 저장이 5초 느려지고
    키움이 막히면 **포트폴리오 저장까지 흔들린다**. 편입은 5개 화면에서 일어난다).
    ★**한 번에 하나씩**(키움 1 req/s · 종목당 ~5초) · **한 세션에 종목당 한 번만**(실패를 되풀이하면
    화면 열 때마다 콜을 태운다) · 키움 키가 없으면 **아예 시도하지 않는다**(네이버는 당일치뿐).
    못 받은 것은 **크론의 ③ 구멍 치유**가 맡는다(배지가 「받는 중」 → 「채우는 중」으로 돌아간다).
    ★`held_init` 은 **살아있는 포지션인지 반드시 확인**한다 — 아니면 「아무 종목이나 원장에 쌓는 문」이 된다.
    ★일괄로 동기 적재하지 않는다 — 14종목 × 5초 = 70초라 **30초 벽**에 걸린다(첫 백필은 크론이 했다).
    ★**상한 20 은 그대로다** — 그 숫자는 「단타 풀」의 것이지 수집 대상의 것이 아니다
    (실측 2026-08-05: 대상 22종목 = 11+14−겹침3 · 크론 11종목 66.5초 → 22종목도 예산 900초에 여유).
    ★★**상한이 찼을 때 FIFO 는 「보유 종목」을 먼저 뺀다**(`poolEvictLast` · 2026-08-05 사용자 선택).
    보유는 빠져도 `targetCodes()` 에 남아 계속 수집되니 **슬롯만 비고 잃는 것이 없다** —
    안 담긴 종목을 빼면 봉이 사라져 다시 받아야 한다. **값이 싼 쪽부터 뺀다.**
    보유가 없으면 옛 규칙(정렬 맨 아래) 그대로이고, 무엇을 왜 뺐는지는 토스트가 말한다.
    ★★**그래서 `poolRemove()` 는 보유 종목의 봉을 지우지 않는다** — 지우면 매일 받아서 매일 지우는
    밑 빠진 독이 되고(`prune()` 의 해지잔여 규칙과 같은 이유) 「보유」 탭이 눈앞에서 빈다.
    × 로 뺄 때의 확인 문구도 갈라야 한다 — 「봉도 함께 지웁니다」가 보유에겐 **거짓말**이다.
    ★미리보기 판정 `state.owned` 는 **합집합**이다 — 보유 종목도 매일 수집되므로 미리보기가 아니다.
    ★**한 종목이 여러 포트폴리오에 있을 수 있다**(실측: 한국주철관 3곳) — 목록은 **종목 단위로 접는다**.

13. **★★장중 시세는 「보는 종목만 촘촘히 · 전종목은 성기게」로 나눈다**(2026-08-06).
    `all_stock_info` 를 채우는 크론의 **장중 정규 fire 는 09·11·13·15시 넷뿐**이라 목록 현재가가
    최대 **약 2시간** 낡았다. 차트는 초 단위로 도는 화면이라 **한 화면이 두 값을 말했다**.
    - 수집 대상(`Dt::targetCodes()` · 실측 34종목)만 **키움 `ka10095`(관심종목정보요청)** 로 받는다.
      단일본은 **`Dt::refreshQuotesLive()`** 이고, `module=dt` 의 `pool_list`/`held_list` 가 응답 전에 지난다.
    - ★**주기는 `Dt::TICK_SEC`(=10초) 한 숫자**다 — 목록 타이머 · 차트 덧대기 타이머 ·
      서버 신선도 창 · 「LIVE N초」 글자를 **함께** 정한다. 넷이 갈리면 다시 두 값이 된다.
      「LIVE 꺼짐」은 차트뿐 아니라 **목록 콜도** 멈춘다(배지 설명이 그렇게 적혀 있다).
    - ★**신선도 판정을 새로 적지 않는다** — `NaverFinanceAPI::staleCutoff()` 단일본을 키움 경로와
      네이버 경로가 **둘 다** 본다. 마감 뒤 규칙(`uDate < 오늘 15:30`)이 한 군데에 있어야 NXT 와 안 부딪힌다.
    - ★**`all_stock_info` 에 쓴다 — 별도 표를 만들지 않는다.** 따로 담으면 단타는 키움 값을,
      포트폴리오는 네이버 값을 보게 되어 지금보다 나쁘다. 섞여도 안전한 근거는 실측이다:
      **`_AL`(SOR)을 안 붙이면 KRX 단독**이라 거래량 기준이 네이버와 같다(규칙 5 와 같은 이유).
      덤으로 이 종목들의 `uDate` 가 늘 신선해져 **네이버 전종목 크론이 이것들을 건너뛴다**.
    - ★**시총·거래대금을 0 으로 덮지 않는다**(`IF(:v > 0, …, 기존)`) — 키움이 안 주는 종목이 있어도
      배지가 0 을 「없음」이 아니라 「0원」으로 그린다.
    - ★**휴장일·장 밖에서는 서버가 아예 안 나간다**(시각·요일 → `Dt::isTradingDay()`). 이 문지기가 없으면
      **토요일에 화면을 열어 둔 것만으로** 10초마다 콜이 나간다 — 마감 뒤 신선도 규칙은 거래일 안에서만
      뜻이 있어 휴장일엔 매 tick 이 「낡음」으로 잡힌다.
    - ★**전종목을 화면 타이머로 갱신하지 않는다** — 28콜 × 10초면 하루 1만 콜이라
      「사이트 전 주가의 단일 원천」을 IP 차단 위험에 올린다(CRON.md §4).
      **「상위 종목」 목록과 다른 탐색 화면(퀀트·스크리너)은 여전히 크론 주기**다.
    - 실패하면 **네이버로 폴백**(`refreshQuotes`)한다. 그때는 회전율이 다시 크론 주기로 남는다
      — 네이버 폴링 API 는 거래대금을 안 준다.
    - ★★**대상 종목은 «시세 넷»을 전부 키움에서 받는다**(2026-08-06 사용자 지시).
      소스가 갈려 있어 **같은 종목이 한 화면에서 세 값**을 말했다(실측: 1분봉 230,000 ·
      일봉 230,250 · 3분봉 246,000). 지금은 넷이 한 소스다:

      | 무엇 | TR | 단일본 |
      |------|----|--------|
      | 현재가·시세 | `ka10095` | `Dt::refreshQuotesLive()` |
      | 당일 1·3분봉 | `ka10080` | `Dt::todayBars()` |
      | 일봉 | `ka10081` | `Kiwoom::daily()` |

      ★**일봉 경로는 그대로다**(`action=daily`) — 규칙 7 을 어기지 않았다. **안에서 소스만 가른다**:
      대상이면 키움, 아니면 네이버. 전종목을 키움에 걸면 1 req/s 라 종목을 훑을 때마다 1초씩
      밀리고, 키움이 죽으면 **전 화면 차트가 함께 죽는다**.
      ★**과거 차트는 안 바뀐다** — 실측에서 키움·네이버 일봉의 과거 종가·거래량이 **전부 일치**했다
      (9/9). 어긋난 건 장중인 오늘 하나뿐이고 그건 받은 시점 차이다.
      ★`Dt::todayBars()` 에 **캐시(TTL=`TICK_SEC`)가 필요하다** — 한 tick 에 메인(1분)과 보조(3분)가
      각각 지나므로, 없으면 같은 종목을 두 번 받고 **두 번째가 1 req/s 에 걸려 1초 늦는다**.
      ★보조 분봉은 예전에 `live=false` 로 못 박혀 있어 장중 내내 **어제 종가에 멈춰** 있었다.
      서버는 진작 되고 있었다(`Dt::series()` 가 당일분을 «합친 뒤» resample 한다).
    - ★★★**실시간 갱신 상한은 15:30 «전»이다** (2026-08-06 실측으로 되돌림 · 대가를 치른 규칙).
      「15:30 종가 단일가까지 받자」고 15:35 로 뒀다가 **정반대 일**이 났다:
      ① 15:30:05 에 받은 값은 **종가 단일가 체결 «직전»** 값이다(실측 SK하이닉스 1,489,000 vs 종가 1,495,000)
      ② 더 나쁜 것 — 그 `uDate=15:30:05` 가 **마감 메우기를 통째로 무력화**한다.
      `staleCutoff()` 는 마감 뒤 「`uDate` < 오늘 15:30」인 것만 낡음으로 보는데(NXT 보호 규칙),
      15:30:05 는 그 조건 **밖**이라 영원히 신선으로 판정돼 **15:50 `dart_eod` 가 건너뛴다**
      → **종가가 틀린 채 굳는다**(실측 33종목 전부). 종가는 원래 설계대로 `dart_eod` 가 넣게 둔다.
    - ★★**덧대기의 «시간 창»을 부르는 쪽에 적지 않는다** — `Dt::todayBars()` 가 스스로 판단한다
      (09:00 이후 · 거래일 · **원장에 오늘 봉이 `BARS_OK` 미만일 때만**).
      `api.php` 에 `hm<=1600` 을 적어 뒀다가 **16:00~16:45 사이 45분** 동안 차트가 어제 봉을 보여 줬다
      — 게이트는 막는데 원장은 16:45 크론이 채우기 «전»인 구간이다.
      원장이 찬 뒤엔 저절로 콜이 멎으므로 창을 넓게 둬도 낭비가 없다.

★ 함정: **보관 창은 최근 10거래일뿐이다.** 그 밖의 분봉은 매일 지워지고, 네이버는 7거래일까지만 주며
그 뒤로는 **어떤 파라미터로도 못 받는다**. 장기 분봉이 필요해지면 「지금부터」 별도 원장에 쌓아야 한다.

★ 키움 `ka10095` 실측(2026-08-06 실호출로 확정 — 문서 대신 이 값을 믿는다):
경로 `POST /api/dostk/stkinfo` · 본문 `{"stk_cd":"005930|000660|…"}` · 목록 키 `atn_stk_infr` ·
**한 요청 100종목**(150 은 `return_code=5` 로 거절) · 34종목 실측 **1콜 0.078초**.
단위 — **`mac`(시총) = 억원 그대로**(「현재가 × `stkcnt`(천주) ÷ 1억 = `mac`」이 5종목 전부 비 1.0000) ·
**`trde_prica`(거래대금) = 백만원**(억원은 ÷100) · 숫자는 **부호가 붙어 온다**(절대값이 값, 방향은 `flu_rt`).

★ 키움 `ka10081`(일봉) 실측(2026-08-06):
경로 `POST /api/dostk/chart`(분봉과 같다) · 목록 키 `stk_dt_pole_chart_qry` · **한 콜 600봉**(cont-yn 으로 더) ·
필드 `dt`·`open_pric`·`high_pric`·`low_pric`·**`cur_prc`(종가)**·`trde_qty`·**`trde_prica`**·`trde_tern_rt`.
★★**`base_dt` 가 필수다** — 빼면 `[1511:필수입력 파라미터=base_dt]` 로 거절당한다(**분봉 `ka10080` 은 없어도 된다** — 둘이 다르다).
★**거래대금을 직접 준다** → 장중에도 실제값이다. `krx_amt` 의 오늘 행은 15:50 `dart_eod` 가 넣어서
그전까지는 「종가×거래량」 근사로 폴백하고 있었다.

## /stock 설정 > 시세 설정 — 시세 지도 (2026-08-06)

**시세가 어디서 와서 언제 갱신되는지**를 한 자리에 편 화면(`/stock/index.php?mode=quote`).
고치는 자리가 아니라 **보는 자리**다 — 손잡이는 크론(`cron_job.php` 의 `TASKS`)과 상수다.

- 카탈로그 단일본은 **`stock/lib/quote.php`** — 원천 표 12종 · 화면 5종 · 외부 API · 실측 탐침.
  화면(`pf_page_quote()`)은 그것을 **그리기만** 한다(`Thr.class`·`ChartFeat` 와 같은 패턴).
- ★**크론 시각을 화면에 적지 않는다** — `cron_job.php` 를 `CRON_REGISTRY_ONLY` 로 include 해
  `TASKS` 를 **부작용 없이** 읽는다(그 파일에 되돌아가는 가드가 있다). 손으로 옮겨 적으면
  크론을 옮기는 날 화면이 조용히 거짓말을 시작한다.
- ★**상수도 소스에서 읽는다** — `quote_const_from_file()`. 실제로 `QM_MAX_MB` 를 「300」이라 적었다가
  잡혔다(코드는 1600 · 표가 1.32GB 라 「가드 4배 초과」로 보였다).
- ★★**`all_etf_price`·`all_etf_holdings_info` 의 `uDate` 는 `DATE` 타입이라 시각이 없다**(실측).
  `all_stock_info.uDate`(`DATETIME`)와 달라서 「몇 시간 전」으로 재면 **밤새 안 받은 것처럼** 읽힌다.
  ETF 갱신 **시각**의 정본은 `data_update_status` 다.
- ★`data_update_status` 에 **죽은 키가 섞여 있다** — `all_stock_info`·`all_etf_price` 키는 2026-05 이후
  아무도 안 찍는다. 살아 있는 것은 `all_data_from_naver`·`all_data_from_nxt`·`auto_etf_naver_update` 셋뿐.
- ⚠**ETF 시세엔 구멍 메우기가 없다** — `refreshQuotes()` 도 NXT 경로도 `all_stock_info` 만 손댄다.
  `all_etf_price` 를 갱신하는 것은 `stock_news` 의 **정규 fire 뿐**(08·17·19시는 NXT 창이라 ETF 를 안 받는다)
  → ETF 는 **15:30 종가를 못 받고 15:05 값에 멈춘다**.

## 급등주 분봉 아카이브 (`qm_*`) — ★불변 규칙 (2026-08-05)

**`급등주_분봉아카이브_사양_v0.1.md` 를 먼저 읽는다.** 최근 두 달 「하루 10% 이상 오른 종목」의
급등일 전후 10거래일 1분봉을 **지워지지 않게** 쌓아 패턴을 분석한다. 진입점은 `cron/qm_collect.php`
한 파일(`job=probe|schema|events|work|verify|feat|status`) · 표는 `qm_event`·`qm_task`·`qm_bar`·`qm_feat`.

1. **`qm_*` 는 `dt_*` 와 조인하지 않는다 — 종목코드 문자열만 공유한다.**
   `dt_min` 은 **최근 10거래일만** 두고 매일 지우는 표다. 아카이브를 거기 넣으면 prune 이 집어삼킨다.
   반대로 **`qm_bar` 에는 만료 삭제를 붙이지 않는다** — 아카이브는 불변이다.
2. **아카이브에 네이버 폴백을 쓰지 않는다**(`dt_min` 과 다른 점). 네이버는 «가장 최근 거래일 하루치»만 주므로
   두 달 전 구간엔 무용한데, **폴백이 성공하면 `bars>0` 이 되어 목표 날짜가 비었는데도 성공으로 보인다**.
   키움이 실패하면 그 (종목,날짜)는 실패로 적고 `tries+1` 한다.
3. **★과거 분봉을 받을 때는 `base_dt` 를 반드시 넣는다**(2026-08-05 실호출 확정 · `Kiwoom::minute()` 5번째 인자).
   `base_dt=YYYYMMDD` 는 «그 날 마감까지의 900봉»을 준다. 없으면 언제나 **오늘부터** 되짚어서 콜이 **네 배** 든다
   (실측 5콜 vs 20콜). `base_dtm`·`strt_dt`·`qry_dt`·`end_dt` 는 **무시된다** — 이름을 바꿔 적지 않는다.
4. **성공/실패는 «날짜별»로 판정한다 — 종목 단위로 하지 않는다.**
   종목으로 판정하면 빈 날짜 구멍이 영영 안 메워지면서 `tries` 도 안 오른다.
   ★반대로 **지시(잠금)는 종목 단위**다 — 한 종목의 구간을 한 번에 받아야 콜이 줄어든다.
5. **부분 재수집을 하지 않는다.** `upd_stkpc_tp=1` 은 «받는 시점» 기준 수정주가라, 일부 날짜만 다시 받으면
   한 종목 안에 기준이 다른 봉이 섞인다 → 그 종목 `qm_bar` 를 **통째로 지우고 구간 전체를 다시 받는다**.
6. **과제(`qm_task`)에 없는 날짜는 저장하지 않는다.** 이벤트가 여럿인 종목은 `min(d)~max(d)` 사이에
   과제가 아닌 날이 딸려 오는데, 그것까지 담으면 용량이 추정을 훌쩍 넘는다.
7. **★`q_bars_full` 은 「날짜가 다 있나」지 「봉이 375개 넘나」가 아니다.** 봉 수로 재면 거래가 얇은 종목이
   통째로 「분석 불가」가 되어 표본이 조용히 대형주로 기운다(§9 가 금지한 생존편향). 얇음은 `q_halted` 가 신호한다.
8. **관리종목 필터는 걸 수 없다 — 「미적용」으로 명시하고 진행한다.** 127개 표를 전수 조사했으나 지정 여부를
   담은 컬럼이 없다. 추측으로 걸러 표본을 조용히 왜곡시키지 않는다.
9. **용량 가드 `QM_MAX_MB`(=300) 를 넘으면 수집을 멈춘다.** economist73 은 127개 표가 공유하는 스키마라
   조용히 호스팅 용량을 채우면 **다른 표가 같이 죽는다**.
10. **새 표는 `qm_` 접두어.**
11. **★★분봉 가격을 `krx_amt` 가격과 견주지 않는다.** 분봉은 `upd_stkpc_tp=1` 이라 **지금 기준 수정주가**,
    `krx_amt` 는 **그 날 있던 값**이다. 이벤트 뒤 무상증자·액면분할이 있으면 **상수배로** 어긋난다
    (실측 가온전선 ×1.7995 · RF머트리얼즈 ×1.9960 · 1,045건 중 57건).
    비율로 만드는 피처(종가/고가, 되돌림)는 멀쩡하지만 **두 소스를 «견주는» 피처는 조용히 거짓**이 된다.
    → 기준가는 **직전 거래일의 마지막 분봉 종가**에서 얻는다. 어긋난 건은 `q_px_basis=1` 로 **표시하고 남긴다**.
12. **★★`krx_amt` 의 「빈 날」은 셋이다 — 정지 / 결측 / 정상. `qm_day_kind()` 단일본으로 가른다**(2026-08-07 전수 실측).
    o·h·l 이 0/NULL 인 173,085행(3.6%) 중 **170,454행은 «거래정지일»**이다 — `vol=0`·`amt=0` 이고
    `c` 는 직전가를 이월한 값이다(원장 결함이 아니라 **시장 사실**). 진짜 결측은 **2,631행**뿐이고
    거의 전부 당일치 네이버 보강분이다. `vol=0` 인데 `h>0` 인 행은 **0건**이라 둘은 같은 날을 가리킨다.
    - **정지 → 건너뛴다**(그 날은 체결이 불가능했다) · **결측 → 판정 불가** · ⛔**신호를 통째로 빼지 않는다**
      (정지된 적 있는 종목이 표본에서 사라져 생존편향이 된다 · §9).
    - ★`krx_amt` 를 읽는 새 잡은 **`SELECT` 에 `vol` 을 반드시 넣는다** — 없으면 함수가 예외를 던진다.
    - ★**가장 비싼 실수는 「모른다」가 «−100%»로 새어 든 것**이다. 정지일의 `o=0` 을 시가로 읽어
      「−100% 갭 손절」을 만들면 손절 한 건이 아니라 **파산 한 건**이 표에 들어간다 —
      겨우 17~22건이 8년 평균을 **0.15%p** 끌어내렸다(n=10,377).
      **0 을 쓸 수 없는 자리에 0 이 들어가면 「몇 건인가」가 아니라 「얼마나 큰가」를 본다.**
    - ★고친 뒤 **일봉 브래킷이 분봉의 낙관·비관 경계 «밖»으로 나갔다** — 버그가 두 방법을 우연히
      일치시키고 있었다(`급등주_패턴정리_v0.1.md` 6-2절).
13. **「받아 온 봉이 0」 을 「그 날 봉이 없다」로 적지 않는다.** 키움은 장애 때 예외 없이
    **HTTP 200 + 빈 목록**(`return_code=0`)을 준다. **전무하면 실패**(재시도) · **일부만 비면 진짜**(거래정지).
    가르는 자는 **`krx_amt.vol`** — 그 날 거래가 있었으면 분봉도 있어야 한다.
    ★실패한 종목은 `ORDER BY tries, d` 로 **뒤로 보낸다** — 날짜순으로만 고르면 실패 종목이 늘 가장 오래된
    날짜를 쥐고 있어 장애가 지나갈 틈 없이 재시도를 소진한다(실측 124건이 그렇게 죽었다).

★ 함정: **종목명은 `all_stock_info` 를 SQL 조인해 붙이지 않는다** — 그 표는 `char(6) utf8mb3` 라
`krx_amt`(utf8mb4)와 조인하면 콜레이션이 어긋나 BNL 로 떨어진다(실측 15배). 따로 읽어 PHP 에서 붙인다.
★ **끝자리 `0` = 보통주 규칙은 5번째 자리의 문자를 배제하지 않는다** — `0004V0`·`0009K0`·`0126Z0` 은
전부 보통주다(신규상장·인적분할). 문자가 있다는 이유로 걸러 내지 않는다.
★ **SSH 에서 `php` 는 PATH 에 없다 — `/usr/local/php84/bin/php`** 를 쓴다.

## 패턴분석 (박스 상향돌파) — ★불변 규칙 (2026-08-09)

**관심차트에 담긴 365건을 되맞춰 얻은 정의**로 **매일 마감 뒤 같은 패턴을 쌓는** 화면
(`/stock/index.php?mode=boxbrk`). 판정 단일본 `stock/lib/boxbrk.php` · 적재 `cron/bx_scan.php`(표 `bx_cand`) ·
크론 `boxbrk`(16:20) · 담기는 `ChartFav`(`src='boxbrk'`).

★**옛 「패턴분석 (불꽃형)」(`mode=flame`·표 `qm_flame`·잡 `flamefill`)은 여기에 흡수돼 삭제됐다**
(2026-08-09 사용자 지시). 그 화면은 「불꽃형 전수 1,528건을 훑는」 자리라 다 보면 할 일이 없었고
**1회성 스냅샷**이라 새 신호도 안 쌓였다. `?mode=flame` 은 301 로 여기로 보낸다.

★★**옛 「패턴분석」(`mode=pattern`·5개 패턴 수기 사례 31건·`pf_page_pattern`)도 여기에 흡수돼
삭제됐다**(2026-08-10 사용자 지시 — 「대대적인 정리」). `?mode=pattern` 도 301. 그 화면이 하던 일은
이렇게 옮겨졌다: **패턴 지도**(5조건 판정 순서 — `BoxBrk` 상수에서 보간, 손으로 안 적음) ·
**특징·실측 표**(pick/auto 갈라 `bx_cand` 라이브 집계 — 판정 재계산 아님) · **실제/실패 사례**
(`<details>` 접힌 카드 — **auto 극단 D+5 상·하위 3건씩 동적 선별**. ★pick 은 사례에 안 쓴다 —
결과를 보고 담은 표본이라 사례로 쓰면 룩어헤드를 화면이 되풀이한다). 카드 렌더러 단일본은
**`pf_bx_card()`** — 목록과 사례가 같은 것을 그린다(사례는 DOM id 접두어 `cs` 로 가른다).
옛 5개 패턴의 **근거 표(트리거 실측)는 검증 탭 ④~⑥에 그대로**라 트리거 규칙은 무사하다.

★★**8년 재측정 잡 `job=bt8y`**(2026-08-10 신설 · 측정 전용 · 표 안 건드림 · 실측 **30초**).
잣대는 `dbrk` 와 글자 그대로 같다(종가 매수·5일 +15/−10·모호 낙관/비관·비용 0.20% 가정).
**최종 5조건의 8년 성적: n=4,426 — 비용 뒤 +0.01% vs 기준선(전체 신호 27,707건) −0.20% ·
승률 41.7 vs 41.4% · 중앙 −3.30 vs −2.28%(오히려 나쁨 — 더 크게 움직이는 무리라 평균만 끌림) ·
양(+)인 해 2/8 · 등급 A 도 승률 45.2%.** ⇒ 「기준선과 사실상 같다」 결론은 조건을 조인 뒤에도 불변.
기록은 **검증 탭 ⑨절**(측정치 리터럴 고정 — quantstat 원칙). **정의(BoxBrk 상수)를 바꾸면 `bt8y`
를 다시 돌리고 ⑨절 숫자를 갱신한다.** 신호분석 **2-d 절**이 이 신호(후보·등급·출처·알림)의 레지스트리다.

★함정(2026-08-10 실측): **서버에서 `bx_scan.php` 를 `php -l` 하면 segfault(139)가 난다** —
opcache+제3자 확장 조합의 lint 문제일 뿐 **실행은 정상**이다(옛 파일도 같았다). lint 는 `php -n -l` 로.

★**퀀트 > 최고 거래대금 목록은 「5조건 압축」이 기본값이다**(2026-08-10 사용자 지시 —
「하단에 박스상향돌파만, 다른 건 보여줄 필요 없어」). 그 날 신호 전체는 **`&bx=0`(「전체 보기」 칩)**
으로만 편다. 행에는 **📦등급 배지**가 붙는다. ★판정을 목록이 다시 하지 않는다 —
**`bx_cand` 를 읽기만** 한다. 그래서 **오늘 날짜는 16:20 적재 전까지 빈다**(잠정치엔 시고저가
없어 장중 판정 자체가 불가 — ③④⑤가 그 값을 본다. 빈 것은 결함이 아니라 구조다).
매집형/불꽃형·트리거 체계는 **그대로다** — 그 목록의 기존 판정을 바꾸지 않고 «곁에» 단 것이다.

★★**그 화면의 «얼굴»은 「📦 박스 상향돌파 추적」 카드다**(2026-08-10 2차 사용자 지시 —
「해당 패턴이 나왔을 때만 이 화면에 보여주고 피드백 받는 것」). 최근 45일(`KrxAmt::TRACK_DAYS`)의
`bx_cand` 후보를 **결과(익절/손절/D+5/사후 n/5 — 브래킷 실측)와 함께** 싣는다. 결과 칸이 곧 피드백이다.
0건이면 「없었다 + 마지막 후보 날짜」를 적는다(빈 칸은 고장으로 읽힌다).

★★★**경로 판정 «전면 재정의»**(2026-08-10 3차 사용자 지시 — 「신박스 기준도 박스상향돌파
기준으로만」 · 범위를 물었더니 **「사이트 전체」를 선택**했다. 트리거 실측·경보 근거 상실을 경고했고
그대로 진행). **경로 배지(新박스·박스안·돌파✓·계단지지·지지이탈↓…)는 박스 상향돌파(5조건 ·
`bx_cand`) 박스에만 붙는다.** 그 밖의 최고 거래대금 신호는 경로 칸이 `-`(bx-na)다.
- **구현은 단일본 한 곳** — `KrxAmt::boxStatusMany($sigs, bool $bxOnly = true)` 의 게이트.
  퀀트 목록·관심종목(트리거 포함)·어닝·단타 배지(`quant.php`)·알림 크론(`dart_collect`)이
  전부 이 함수라 한 번에 같은 기준이 된다. 화면마다 다시 걸지 않는다.
- ★**유일한 예외 = 포트폴리오 「계단관통↓」 경보**(`pf_stair_alert_map` → `$bxOnly=false`) —
  편입 시 못박은 기준 박스는 «기록»이고, 지지 구조 소멸을 놓치면 포지션이 다친다.
- ⚠**따라서 트리거(돌파확인·계단지지)는 박스 상향돌파 박스에서만 뜬다** — 검증 ④~⑥의 실측
  (+2.26/+2.40%)은 «전체 모집단» 기준의 기록이라 새 기준에서는 **참고치**다(화면·문서에 명시했다).
  5조건 부분집합에서의 트리거 성적은 잰 적이 없다 — 필요해지면 `bt8y` 를 넓혀 잰다.
- ★박스 «원장»(krx_surge·계단·`boxbrk_boxes`)은 **기질로 남는다** — 5조건 자체가 「이전 박스」
  (①·④) 위에서 정의되므로 원장을 5조건으로 좁히면 정의가 재귀로 무너진다. 좁힌 것은 **표시·판정
  대상**이지 원장이 아니다.
- 「🟢 매집형 박스 추적」 카드는 이 재정의로 모집단이 기준 밖이 되어 **삭제**했다
  (`accBoxes`·`surgeEventStates` 호출도 함께). 관측은 📦 추적 카드가 잇는다.
- `bx_cand` 는 16:20 적재라 **당일 박스의 경로는 마감 뒤에야 붙는다** — 장중 '-' 는 결함이 아니다.

1. **★★표가 담는 것은 두 가지다 — `src`.** `'pick'`(직접 고른 365건 = 정의 원본) · `'auto'`(매일 자동).
   **화면에서 반드시 갈라 본다**(「출처」 축). 섞어서 결과를 세면 **「이 패턴 승률 좋네」로 잘못 읽힌다** —
   `pick` 은 «고른 것»이라 승률 55.9% 인데 `auto` 는 8년 검정에서 기준선과 사실상 같다.
   ★`bx_fill_day()` 는 **그 날의 `auto` 만** 갈아 끼운다 — `pick` 을 다시 계산해 지우면 안 된다.
2. **왜 그 경고가 필요한가 — 2026-08-09 실측.** 옛 갤러리는 카드에 신호일 **이후** 55거래일 차트와
   결과 배지를 함께 띄웠고, 거기서 담긴 365건의 승률 우위는 **「모양」이 아니라 「결과」에서 왔다**.
   근거 셋 — ①모델이 재현한 상위 24%는 승률 43.6%로 전체(49.1%)보다 **나쁘다** ②8년 기간 밖에서
   상위 10% +0.40% vs 기준선 −0.25%(승률 44.1 vs 42.8로 사실상 같다) ③**같은 «모양» 칸 안에서**
   담은 것−안 담은 것이 +1.7~+22.2%p 로 갈리고, **모양이 가장 «안» 닮은 4분위에서 담은 것이 가장 좋았다**(68.4%).
   ⇒ 「검증된 신호」처럼 쓰지 않는다. 알림 본문의 「미검증」 한 줄도 지우지 않는다(아래 10).
   ★오염 방지는 이제 **다른 자리**가 맡는다 — 오늘 것은 사후 5거래일이 안 차 **자연히 「사후 미완」**이고,
   나중에 갈라 읽을 자는 `chart_fav_item.made_at` 과 신호일 `d` 의 간격이다.
3. **★★점수(z)를 «담는 기준»으로 쓰지 않는다** — 2026-08-09 사용자 지시로 **적재 컷을 없앴다**
   (「z값은 이상해, 빼자」). 정의는 **박스 조건 셋**이 하고, 점수는 **표시·정렬·알림 등급**에만 쓴다.
   ★z 는 「담긴 것과 얼마나 닮았나」이지 **「오를까」가 아니다**(불꽃형 1,225건 중 담은 348건을 되맞춘 것).
   ★왜 뺐나 — 학습은 **불꽃형 «안에서»** 했는데 지금 모집단은 박스 조건으로 걸러진 **다른 무리**다.
   그 결과 **직접 고른 것의 z 중앙값이 −0.40** 이라 컷(0.223)을 넘는 것이 **21%뿐**이었다.
   결정적 증거는 씨어랩 189330 — **같은 종목의 두 박스 돌파가 갈렸다**: 05-29(z −0.27)는 조건을 다
   통과하고도 컷에서 빠졌고, 화면에 있던 07-09 도 z +0.129 로 «컷 아래»인데 오직 `src='pick'` 이라
   남아 있었다. **화면이 앞뒤가 안 맞았다.**
   ★**알림은 여전히 등급으로 좁힌다**(`ag=B` = A+B · 실측 하루 1.6건) — 안 그러면 하루 12.4건이 다 울린다.
   ★화면에 **D 칩을 반드시 둔다** — 없으면 A+B+C 가 전체와 안 맞아 조용히 거짓말하는 화면이 된다.
   라벨은 「컷 아래」가 아니라 **「D 그 밖」**이다(컷이 없으므로 «걸러진 것»이 아니다).
3. **정의는 손으로 그리지 않는다 — 되맞춘 것이다.** 사전 피처 11축 로지스틱(교차검증 AUC 0.750).
   계수·평균·표준편차는 `BoxBrk::W` 에 **한 벌**로 산다 — 하나만 고치면 점수가 조용히 다른 뜻이 된다.
   축 어휘는 `boxbrk_axes()` 가 원본이고 화면은 읽기만 한다(`Thr.class`·`ChartFeat` 와 같은 패턴).
4. **★계단(`stepsAbove`)은 종목 «이력 전체»에서 센다** — 학습이 그랬다. `boxbrk_scan()` 이 기간을 자르면
   계단이 평균 9.6개에서 2~3개로 줄어 **같은 종목·같은 날인데 점수가 달라진다**(그래서 종목 묶음이 80개다).
5. **모집단에 「20평비 20배↑」를 걸지 않는다** — 담기 취향은 오히려 20평비가 **낮은** 쪽이다.
   불꽃형 여부는 **배지로만** 구분한다(그래서 점수에서 20평비를 뺐다 — 안 빼면 모집단을 넓히는 순간 쏠린다).
6. **하드 배제는 점수 «밖»에서 — 이제 등락 하나뿐이다**(2026-08-09 사용자 지시로 바뀜).
   `CHG_MAX` **25% → 28%**(「상한가 «근처»만 뺀다」) · **「종가위치 ≥95%」 배제는 없앴다**.
   ★없앤 이유 — 그 규칙은 「담은 적 없다」에서 뽑았는데 정작 **사용자가 박스로 인정하는 날**을
   걸러내고 있었다(하나마이크론 **2026-01-27** · 4,197억 · 종가위치 99% → 지금은 등급 A 로 나온다).
   「안 담았다」가 「나쁘다」는 뜻은 아니다 — 그 화면에서 그 날을 아예 안 봤을 수도 있다.
   실측 복귀 **+151건**(종가위치 95%↑ 88건 · 등락 25~28% 65건).
6-2. **★신호는 다섯 조건이다** — ①**이전에 100억↑ 박스가 있고**(`PRIOR_BOX_AMT`) ②**그 날 박스가
   새로 생기며**(거래대금 직전 119거래일 최고 & 100억↑) ③**기존 박스에 붙어 있고**
   (현재 박스 저가 ≤ 기존 박스 고가 × 1.10 · `BOX_GAP_MAX`) ④**신규 박스가 직전 3개 박스보다 «위»**
   (저가는 직전 박스와, **고가는 직전 3개 전부**와 견준다 · `REQUIRE_HIGHER_BOX`·`ABOVE_LAST_N_BOX` ·
   판정 단일본 `boxbrk_higher_box()`) ⑤**그 날 종가가 시가보다 7%↑**(`OC_MIN`).
   ★③은 「너무 높이 뜬 것」만 막았지 **아래로 내려선 것은 못 막았다** — 에스씨엠 109610
   (기존박스 대비 −23.5% · 등락 −2.15%) · 광무 029480(−41.8%)이 그랬다(auto 1,141건 제거).
   ★★그리고 ④를 **「가장 최근 박스 하나」로만 두면 또 샌다**(같은 날 다시 잡혔다) — 박스가 한 단
   «내려선 뒤» 그 위에 생기면 통과한다. **야스 255440** 이 그랬다: 직전 박스보다는 위(−5.2%)인데
   그 앞 박스들의 H(≈12,300·≈9,900)보다 한참 아래(신규 H ≈5,800)였다. **직전 3개 H 전부**와
   견주도록 고쳤다(auto 223건 추가 제거).
   ★**N=3 인 것은 차트가 옛 단계를 정확히 3개 연장해 그리기 때문**이다(`chart_indicator` id=4 의 `ext:3`) —
   「화면에 보이는 계단 전부보다 위」와 같은 말이 된다. 「n일 고점」 안을 안 쓴 이유도 이것이다:
   화면에 안 그려지는 기준은 「왜 이건 되고 저건 안 되나」를 눈으로 확인할 수 없다.
   ★★⑤는 **「등락」과 다른 자**다 — 갭으로 뜬 뒤 하루 종일 흘러내린 봉은 등락이 플러스여도
   **시가→종가가 음수**다. 돌파의 「그 날」은 장중에 밀어올린 봉이어야 한다(auto 979건 제거).
   ★③이 없으면 「돌파」가 아닌 것이 섞인다 — 두산 2026-05-29 는 현재 박스 저가가 기존 박스 고가보다
   **+194.8%** 위였다(573,000 → 1,689,000). 돌파가 아니라 «완전히 다른 자리에서 새로 생긴 것»이다.
   실측으로 auto **386건**이 여기 걸려 빠졌다(1,241 → 855 · 하루 3.5건).
   ★기준 0.10 은 **사용자 선택**이다. 더 조이면(현재L < 기존H = 박스 «안») 취향엔 더 맞지만
   (pick 승률 55.1→58.0%) 자동 표본이 474건으로 62% 줄고 **자동분 성적은 오히려 나빠진다**
   (42.2→39.3% · +0.39→−0.20%). 여기서도 취향과 성과는 따로 논다.
   ★「기존 박스」는 **가장 최근 이전 박스**(10억↑ · 차트에 선이 그려지는 그것)다 — 눈에 가장 가까운
   계단이라 화면과 말이 맞는다. 값은 `m_gap_pct`·`m_prev_h` 로 담아 카드에 「기존박스 대비 %」로 적는다.
   ★박스를 «그리는» 하한은 10억(`STEPAMT` · 차트 지표와 같은 값)이라 **게이트 하한과 다르다** —
   그냥 두면 「이전 박스」 문턱이 신호 문턱의 1/10 이 된다. `boxN`(10억) / `boxN100`(100억)로 갈라 둔다.
   ★`stepsAbove`(점수 축)와 `m_box_n`(화면 「박스 N벌」)은 **10억 그대로** — 저건 «그려지는 계단»을
   세는 것이고 점수도 10억으로 학습했다.
7. **`ChartFav::SRCS` 에 옛 `'flame'` 을 남겨 두지 않는다** — 목록에 있으면 API 가 그 값을 받아들여
   **새 행이 옛 출처로 들어가고** 화면에서 영영 안 보인다. 담긴 365건의 `src` 는 `job=migrate` 가
   `'boxbrk'` 로 UPDATE 했다. `pf_flame_fav_js($src)` 가 담기 JS 의 단일본이다.
7-2. **★1분봉은 두 번에 나눠 받는다**(`bx_minfill`) — 1차 신호 당일 «−5 ~ 신호일»(그 날 카드가 비지 않게) ·
   2차 사후가 찬 뒤 «−5 ~ +10» **통째로 다시**. 2차가 그 구간을 지우고 다시 넣으므로 급등주 규칙 §5
   (부분 재수집 금지)를 지킨다 — 매일 하루씩 «덧붙이면» 한 종목 안에 수정주가 기준이 섞인다.
   ★사후가 **이미 찬** 행은 1차를 건너뛰고 곧장 2차로 간다(안 그러면 콜이 그대로 두 배).
   ★`base_dt` 를 반드시 넣고(급등주 §3) **네이버 폴백을 쓰지 않는다**(§2). 실측 **행당 ~8콜** ·
   하루 예산 `mincalls`(기본 150) · 봉은 `qm_bar` 에 넣는다(급등주 아카이브와 같은 표 · 만료 삭제 없음).
8. **★16:20 인 이유** — 오늘 행은 15:50 `dart_eod` 가 넣는다. 앞으로 당기면 매일 「그 날 봉이 없다」로
   조용히 0건이 된다. 잡이 **직전 거래일도 다시 잰다**(13:05 KRX 확정값이 잠정치를 덮으면 점수가 바뀐다).
9. **새 표는 `bx_` 접두어.**
10. **알림(2026-08-09 사용자 지시로 추가)** — `job=daily` 끝에 **새로 뜬 후보만** Pushover.
    발송·중복방지는 **`stock/lib/alert.php` 단일본**(`pf_alert_log` 한 장부)을 그대로 쓴다 —
    여기에 따로 표를 만들면 「이미 쐈나」에 답하는 장부가 둘이 된다.
    ★중복키는 **(종목, 날짜)**이고 **등급을 넣지 않는다** — 확정값에 B→A 로 올라간 것을 다시 쏘면 두 번 온다.
    ★0건이면 **안 보낸다** · 실패는 **삼킨다**(적재가 본업) · 등급 하한은 **`TASKS` 의 `ag`**(기본 B).
    ★**본문 끝의 「성과 미검증」 한 줄을 지우지 않는다** — 그리고 그 자르기를 **`bx_alert()` 안에서** 한다.
    `pf_alert_send()` 가 10줄에서 자르므로 그냥 끝에 붙이면 **후보가 많은 날에 조용히 사라진다**
    (가장 필요한 날에 없어진다).
    ★알림은 표본을 **오염시키지 않는다** — 후보가 뜬 그 날 쏘므로 사후가 아직 없다.

★ **「박스」 축**(2026-08-09) — **차트에 계단이 몇 «벌» 그려지나** = `1 + LEAST(3, m_box_n)`.
실측 1벌 6 · 2벌 22 · 3벌 27 · 4벌↑ 1,403.

★★**창으로 세지 않는다 — 같은 날 한 번 틀렸다.** 처음엔 「앞 55거래일 «안»의 박스 봉 수」로 셌는데
화면과 달랐다. 지표(`chart_indicator` id=4)의 H선·L선에 **`"ext":3`** 이 걸려 있어 **옛 단계를 3개까지
«연장»해 그리기** 때문이다 — 박스 봉이 화면 밖이어도 **선은 보인다**. 실측 하나마이크론 2026-05-15 는
직전 박스가 **72거래일 전(창 밖)**이라 필터는 「1개」라 했지만 화면엔 계단이 **2벌** 있었다.
⇒ **`m_box_n`(이전 박스 «총» 개수)만 본다.** 창은 관계없다.

★★**박스 창은 `BoxBrk::STEPWIN`(직전 120봉)이고 신호 창 `WIN`(직전 119봉)과 «일부러» 다르다.**
박스는 **화면에 그려지는 계단**과 맞춰야 하는 값이라 지표 수식
(`현재_거래대금 > highest(거래대금,120)[1]`)을 그대로 따른다. 신호 창은 8년 백테스트와 같은 자라 못 바꾼다.
대가는 `stepsAbove` 가 ±1 흔들리는 것(z 변화 ±0.04 · 잡음 안)인데, 화면과 맞추는 쪽을 골랐다.

★★**「뚫을 박스가 없는」 신호는 담지 않는다**(`BoxBrk::MIN_PRIOR_BOX` · 2026-08-09 사용자 지시).
「박스 상향돌파」인데 뚫을 박스가 없으면 이름이 성립하지 않는다 — 실측 달바글로벌 2026-05-12
(2025-05-22 상장 · 이력 297봉 동안 첫 폭발)처럼 계단이 신호일 것 하나뿐인 건이 auto 10건 있었고 지웠다.
★**`pick` 은 지우지 않았다**(6건 남음) — 사용자가 «고른 사실»이라 판정으로 지울 것이 아니다.
재계산·정리는 `job=boxn`.

★ 화면의 축은 **여섯**이다 — **날짜 · 출처 · 등급 · 유형 · 박스 · 결과**. 옛 화면의 칩 여덟(거래정지·분봉이 가름·
원장 결측·상한가 제외…)은 **필터에서 내리고 카드 배지로만** 남겼다 — 하루 몇 건짜리 목록에서 그것들로
거르는 일은 없다. ★「날짜·관심차트」는 **범위**이고 나머지 넷은 그 안의 **분해**다. 그래서 칩의 숫자는
범위만 걸고 센다 — 고른 등급까지 걸어 세면 「A 0건」 같은 자기모순이 화면에 뜬다.

★★**재백필은 분봉 진행을 «건져» 간다**(`bx_fill_day` 의 `$keepMin`) — 그 날 auto 를 통째로 갈아
끼우므로 그냥 지우면 `min_stage`·`has_min` 이 0 으로 돌아가 **이미 받은 봉을 다시 받는다**
(행당 ~8콜 · 한 번에 수천 콜). 봉 자체는 `qm_bar` 에 그대로 있으니 되살리기만 하면 된다.

★ 실측 메모(2026-08-09 최종): `bx_cand` **828건** = pick **84** + auto **744**(하루 3.3건).
**모든 행이 5조건을 만족한다** — `job=boxn` 재검사에서 위반 **0건**(auto·pick 모두).
★`pick` 을 세 번 정리했다 — **조건을 더할 때마다 안 맞는 것이 생긴다**(365 → 294 → 120 → **84**).
관심차트 항목도 함께 지워 **항목 84 = 매칭 84**(고아 0).
⇒ **정의를 바꾸면 `job=boxn`(auto) 과 `job=purgepick`(pick) 을 «함께» 돌린다.** 한쪽만 돌리면
표가 두 가지 뜻을 갖는다.
★**분봉 백로그**가 남아 있어 `mincalls` 를 **400** 으로 뒀다(새 것 3.3건×~8콜 ≈ 27콜 + 밀린 것 ≈ 373콜).
그대로 150 이면 밀린 것을 못 채운다.

★**`job=boxn`(정기)은 pick 을 «지우지 않는다»** — 「고른 사실」이라 판정으로 지울 것이 아니다.
지우는 것은 **`job=purgepick`** 뿐이고 **사용자가 시켰을 때만** 부른다(2026-08-09 에 71건 지웠다:
①이전 100억 박스 없음 13 · ③너무 멀리 뜸 58 · 365 → 294).
★★**`chart_fav_item` 도 함께 지운다** — 안 지우면 그룹 건수는 그대로인데 화면엔 안 보여
「숫자가 새는」 상태가 된다(`ChartFav::groups()` 가 item 을 센다). 지운 뒤 검증: 그룹 294 = 매칭 294.
★지우기 «전»에 목록을 찍는다(`go=1` 없이 부르면 미리보기) — 되돌릴 원본(`qm_flame`)이 없어서
그 로그가 유일한 기록이다.
★★**집계 문구에 분모를 손으로 적지 않는다** — 「63/365」라고 박아 뒀다가 71건을 지운 뒤
조용히 거짓말을 시작했다. 세는 값에서 뽑는다. **0건인 날이 흔하다** — 시장 거래대금이 5월 65.8조/일 →
8월 32.1조/일로 반토막이라 「직전 119거래일 최고」를 넘길 수가 없다(**버그가 아니라 시장 상태**).
판정 검증은 「종목 훑기 vs 날짜 훑기」 두 방법이 **175 vs 175 · 차이 0건 · 점수 차이 0건**으로 일치했다.

## /gift 선물 발송 관리 — ★불변 규칙 (2026-08-03)

**`선물관리시스템_요건정의서.md` 를 먼저 읽는다.** 명절(설/추석) 선물 발송을 회차 단위로 관리한다.
구성은 `gift/index.php`(라우터+화면) · `gift/api.php`(module+action) · `gift/export.php`(엑셀) · `classes/Gift.class`(DB 전담).

1. **`gift_item` 은 스냅샷 표다.** 고객명·연락처·주소·물품명·단가를 **값으로 복사**해 둔다.
   확정된 회차는 이후 고객·물품이 바뀌어도 변하면 안 된다. `customer_id`/`product_id` 는 참조용일 뿐이다.
2. **파생값(`total_qty`·`amount`)은 항상 서버가 다시 계산한다.** 화면 계산은 미리보기다.
   `total_qty = 1 + extra_qty` · `amount = unit_price × total_qty`.
3. **물품은 삭제하지 않는다 — `is_active=0` 으로만 내린다.** 고객도 소프트 삭제다(이력 보호).
   단가를 고쳐도 **확정 회차는 불변**이고, 작업중 회차만 물어본 뒤 일괄 반영한다.
4. **작업중(`draft`) 회차는 동시에 하나뿐이다.** 연도·명절 중복은 `uniq_key` 로 막고 `기타`만 꼬리를 붙여 여러 번 허용한다.
5. **「오프라인 전달」은 고객의 성질이지 주소의 결과가 아니다**(2026-08-03 · `gift_customer.is_offline`).
   주소가 있어도 직접 전달하는 고객이 있다. 다만 **주소가 없으면 택배가 불가능하므로 `customerSave()` 가 저장 시점에
   `is_offline=1` 로 못박는다**(화면이 0 을 보내와도 서버 판정이 이긴다). 사람이 켜고 끄는 것은 **주소가 있는 고객뿐**이다.
   발송구분 기본값은 `Gift::defaultDelivery($addr, $offline)`
   — **오프라인이면 「일괄」**, 아니면 주소 있으면 「택배」·없으면 「일괄」. 이 판정을 화면에 다시 적지 않는다.
   되돌리기(`fixDelivery`)는 **「택배」인 행만** 건드린다 — 사용자가 고른 「별도」를 덮어쓰면 안 된다.
6. **인명록(`tbl_contact`) 접근은 `Gift` 의 「인명록 연동」 구역 한 곳뿐이다.** 다른 데서 그 표를 읽지 않는다.
   인명록에는 **우편번호 칸이 없어** 주소만 온다 → 택배로 보내려면 화면에서 우편번호를 채워야 한다.
7. **고객 분류 그룹(`gift_group`)은 이름이 아니라 `gift_customer.group_id` 로 잇는다**(2026-08-03).
   인명록(`tbl_contact.group_name`)은 문자열이라 이름을 바꾸면 전 행을 UPDATE 해야 했다 — 같은 실수를 반복하지 않는다.
   그룹 삭제는 **`ON DELETE SET NULL`** — 그룹만 사라지고 고객은 미분류로 남는다. 미분류는 별도 행이 아니라 `group_id IS NULL` 이다.
   **그룹 관리는 별도 메뉴가 아니라 고객 명단 화면의 모달**(`#m-grp`)이다 — 쓰는 일이 드물어 탭을 차지할 값어치가 없다.
   순서는 **끌어서 옮긴다**(`gfSortable()` · 놓는 즉시 `group/reorder` 로 1..N 재부여). 숫자를 손으로 적는 칸은 없앴다.
   ★드래그 **포인터 캡처는 움직이는 행이 아니라 `tbody` 에 건다** — `insertBefore` 로 행이 DOM 에서 잠깐 떨어지면
   그 안의 캡처가 풀려 드래그가 끊긴다. `gfSortable(tbody, key, onOrder)` 는 범용이라 다른 표에도 그대로 쓴다.
   ★그룹은 **고객 명단에만 있고 회차·엑셀에는 아직 없다**. 회차에 넣으려면 `gift_item` 에 `group_name` 스냅샷을 더해야 한다(규칙 1).
8. **확정 차단 조건 3가지**(물품 미지정 · 택배인데 주소/우편번호 없음 · 대상 0건)는 `Gift::invalidItems()` 단일본이다.
   화면 하이라이트·집계 「미완성」·확정 검사가 모두 이 함수를 본다 — 조건을 화면에 다시 적지 않는다.
9. **새 표는 `gift_` 접두어.** (economist73 은 127 표를 공유하는 스키마다.)

★ 함정: **엑셀 원본의 「단가」 칸에는 합계 금액이 들어 있다**(50,000×2=100,000).
가져오기가 `단가 = 합계 ÷ 총지급` 으로 되돌린다 — 이 규칙을 지우면 금액이 제곱으로 부푼다.

- **엑셀은 PhpSpreadsheet 가 아니다.** 이 서버에 composer 가 없어 `nw/report.php` 의 순수 PHP OOXML 라이터를
  `gift/export.php` 에 가져왔다(`gx_` 접두어 · ZipArchive 없으면 SpreadsheetML 폴백). 라이터를 고칠 일이 생기면 둘 다 본다.
- 우편번호는 길이 둘이다. **화면에서 사람이 고를 때는 Daum Postcode**(무료·키 불필요·`schedule.php` 와 같은 지연 로딩),
  **서버가 주소만 갖고 찾을 때는 `classes/Zipcode.class`**(카카오 로컬 → 카카오 키워드 → 네이버 지오코딩 3단 폴백).
  실측 49건 중 48건 성공. ★건물명으로 찾은 건(`src=kakao-keyword`)은 단지·동이 갈릴 수 있어 **「확인 필요」로 따로 보고**한다 —
  못 찾은 건은 **빈 칸으로 남긴다**(틀린 우편번호를 채우는 것보다 낫다).
  ★배치로 돌 때 **실패한 id 를 `skip` 으로 넘긴다** — 안 그러면 그 사람이 목록 맨 앞에 남아 같은 조회를 되풀이한다.
- 고객 명단의 **정렬은 전부 표 머리글을 눌러 고른다**(2026-08-03 · 셀렉트·필터 셀렉트를 걷어냄).
  머리글 하나가 두 상태를 오간다 — 고객명 `name`/`namedesc` · 그룹 `group`/`ungroup` · 주소 `addr`/`noaddr` · 전달 `ship`/`offline`.
  `gift_sort_th()` 로만 만들고 `gift_cust_url()` 이 **검색·그룹 필터를 유지한 채 sort 만 바꾼다**. 기본은 `ship`.
  ★전달 기준은 **주소 유무가 아니라 오프라인 체크**다 — 주소가 있어도 직접 전달하는 고객은 아래로 내려가야 한다.
  툴바에 남은 것은 **검색칸(Enter)과 그룹 필터** 둘뿐이다.
- **`gift_customer.source_type` 은 삭제했다**(2026-08-03 사용자 지시). ★**`source_ref_id` 는 지우면 안 된다** —
  인명록 가져오기의 중복 방지(「등록됨」 배지)가 그 값을 쓰고, 채워져 있다는 것 자체가 「인명록에서 왔다」는 뜻이라 출처도 남는다.

## 서버 환경 (cafe24 웹호스팅)

| 항목 | 버전 |
|------|------|
| PHP | 8.4 |
| DB | MariaDB 10.6.17 |
| 서버 IP | 220.73.160.47 (uws8-wpm-151) |
| SSH/SFTP 접속 | `economist73@220.73.160.47` (port 22, 비밀번호 인증) |
| 배포 | `.vscode/sftp.json` SFTP 자동업로드. remotePath는 chroot 경로 `/economist73/www` |

## 모바일 감지

`cnt.inc`에서 `$mobile` 변수(0/1)를 전역 세팅한다. User-Agent 기반이며 페이지별로 레이아웃 분기에 사용한다.
