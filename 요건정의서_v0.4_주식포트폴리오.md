# 주식 포트폴리오 시스템 — 요건정의서 v0.4 (구현 반영판)

> 기준: v0.3 + **M1~M6 · M5-2 실제 구현**(2026-08-03) + 현행조사보고서(같은 날)
> 성격: **v0.3 은 계획, v0.4 는 사실이다.** 구현하면서 v0.3 과 달라진 것을 여기서 정본화한다.
> 표기: `✅` 구현·배포·검증 완료 · `⏳` 미구현 · `⊗` 기각 · `⚠` 결정 필요 · `⊸` 보류

---

## 0. v0.3 → v0.4 — 무엇이 달라졌나

M1~M6 을 구현했고, **여섯 군데가 계획과 다르게** 됐다. 전부 구현 중 측정으로 드러난 사정이다.
(일곱 번째 — `accBoxes` 이벤트 전환은 착수했다가 **되돌렸다**. §3.4)

| # | v0.3 계획 | v0.4 실제 | 왜 |
|---|---|---|---|
| 1 | `surge_event` 테이블 | **`krx_surge_event`** | `pf_`/`krx_` 접두어로 소속을 밝히는 레포 규칙(CLAUDE.md). krx_surge·krx_surge_day 와 한 식구 |
| 2 | `position_entry_snapshot` | **`pf_entry_snapshot`** | 같은 이유 (127 테이블 공유 스키마) |
| 3 | `tracking_*` 를 `krx_surge` 에 컬럼 추가 | **별도 테이블 신설** | ★`krx_surge` 는 캐시다 — 크론이 매일 `DELETE … WHERE d >= ?` 로 <b>범위 삭제</b>한다(§3.2) |
| 4 | `entry_trigger` 를 `pf_alert_log` 에서 부분 백필 | **백필 없음(NULL)** | 실측 `kind='trig'` **0건** — 채울 재료가 없다(§2.4) |
| 5 | `threshold_registry` **테이블** | **`classes/Thr.class` 상수** | 정본이 둘이 되는 것을 피했다. 임계는 사용자 설정이 아니라 연구 결과라 배포와 함께 간다(§5) |
| 6 | (없던 결정) | **`surge_event_d` 60일 상한** | 실측: 무제한이면 18건 중 10건이 <b>반년 넘은 남의 박스</b>를 기준으로 물려받는다(§2.2) |

---

## 1. 시스템 구조 (v0.3 §1 유지 · 실제로 적용됨)

두 섹션(**탐색** = 발견·검증 / **포트폴리오** = 운용) + 공통층(1-a 시장 · 1-b SUE).
시각 분리(신호일 vs 오늘) 유지.

### 1.1 D8 경계 — 확정 문구

> 금지되는 것은 **탐색 판정**(2-a 신호 · 2-b 경로 · 2-c 트리거)이 포트폴리오 화면에 서는 것이다.
> 원장(`krx_amt`)에서 **오늘 기준으로 계산한 1-a 지표**(모멘텀 칩 등)는 공통층이므로 허용.
> 기준은 「시장을 읽었나」가 아니라 「**판정을 가져왔나**」다.

✅ **V1(보유 종목상세의 「퀀트 : 최고 거래대금」 칸) 제거 완료** — 렌더 실측으로 포트폴리오 전 화면에서 0건, 퀀트 섹션(관심종목·퀀트)에만 남음.

### 1.2 예외는 둘뿐

| 예외 | 무엇 | 근거 |
|---|---|---|
| ④ 계단관통↓ | 2-b 판정을 **입력**으로 삼아 만든 포트폴리오 경보 | §2.3 |
| 「편입 당시」 | 편입 순간에 찍어 둔 **기록**(다시 계산 안 함) | §2.5 |

---

## 2. 편입 경계 ✅

### 2.1 원칙 (유지)

탐색 → 포트폴리오 이전은 편입 시점 1회, **참조가 아니라 값**. 이후 탐색 갱신은 흘러들지 않는다.

### 2.2 기준 박스 고정 ✅

`pf_position.surge_event_d`(DATE NULL) — 편입 시점에 못박은 `krx_surge.d`.
매 요청 `SELECT MAX(d) FROM krx_surge` 하던 3곳을 전부 교체했다.

**백필 규칙** — `Pf::SURGE_BACKFILL_MAX_DAYS = 60`(달력일):
편입일(=`started_at`, 없으면 첫 매수일) 이전 **60일 안**의 최근 확정 신호(`src='k'`)만 인정. 밖이면 NULL.

> ★ **실측이 이 상한을 낳았다** (18 포지션): 6·36·47일 **3건** / 105~186일 9건 / 273일 1건 / 신호 이력 없음 5건.
> 상한이 없으면 10건이 반년 넘은 박스를 「내가 산 이유」로 물려받는다 — 그 박스를 보고 산 것이 아니다.

**결과 — 경보 총량이 크게 바뀌었다** (배포 전 대조표로 예측 → 배포 후 렌더로 확인):

| | 계단관통↓ |
|---|---|
| M3 이전 (최신 박스) | **9건** |
| M3 이후 (편입 박스) | **1건** (한국주철관 id=11 · 편입 6일 전 박스) |

**딸림 규칙**

| 상황 | 처리 |
|---|---|
| `surge_event_d = NULL` | 계단관통↓ **판정하지 않음**. 종목 상세에 「**기준 박스 없음**」 칩 (목록엔 안 붙임 — 훑는 화면이라) |
| 보유 중 새 신호 발생 | 기준은 **안 바뀐다**. 새 신호는 탐색 섹션에서만 쓴다 |
| 신규 편입 | 화면이 보여 준 신호(`&sed=`)를 그대로 받는다. 안 실려 오면 같은 60일 상한으로 파생 |

★ 실려 온 `sed` 도 **실재하는 확정 신호인지 DB로 확인**한다(주소로 아무 날짜나 넣을 수 있다).

### 2.3 유일한 예외 — 정정 ✅

> v0.3 §2.3 은 「계단 구조(floors)의 일일 갱신은 계속」이라 했다. **그것은 성립하지 않는다.**
> `boxStatusMany` 의 계단 재료는 `d < 기준신호일` 인 과거 신호뿐이라(KrxAmt.class),
> 기준을 편입 시점으로 고정하면 **계단 구조도 그 시점에 동결**된다.
> ⇒ 갱신되는 것은 **그 뒤의 봉(가격)뿐**이다. 예외 조항 자체가 사라졌고, §2.1 과 완전히 정합적이다.

### 2.4 편입 3축 ✅

| 축 | 컬럼 | 값 | 신규 편입 | 백필 |
|---|---|---|---|---|
| 발견 경로 | `pf_position.source` | quant/earn/fund/watch/'' | 기존대로 | (이미 있었음) |
| 사다리 방식 | `pf_position.ladder_method` | `ruleset_pct` \| `quant_ladder` | 폼의 `buy_mode` 에서 **직행** | 레벨 행 유무 → 18건 전부 `ruleset_pct` |
| 매수 자리 | `pf_position.entry_trigger` | `breakout` \| `step_support` \| `none` | 탐색 화면이 `&trig=` 로 실어 옴, 없으면 `none` | **하지 않음 → NULL** |

★★ **NULL 과 `'none'` 은 뜻이 다르다** — NULL = 「기록하지 않던 시절이라 모른다」(옛 18건) / `'none'` = 「폼을 거쳤는데 트리거가 없었다」(신규). 이 레포의 「값 없으면 0이 아니라 null」 원칙(`pf_trade_score`) 그대로다.

★ `ladder_method` 는 **새로 만든 값이 아니다** — `pf_watchlist.adopt_bm` 에 이미 있었고 편입 때 끊기고 있었다(현행조사보고서 §1.3).

**편입 다리** — 트리거·신호일을 싣는 화면:

| 화면 | `&trig=` | `&sed=` |
|---|---|---|
| 관심종목(관제탑) | ○ (트리거 판정 시 `'k'` 키를 함께 정해 둔다 — 한글 라벨 파싱 금지) | ○ |
| 퀀트 목록 | ✕ (이 화면은 트리거를 판정하지 않는다) | ○ (`$day`) |
| 재무분석·수동 | ✕ | ✕ → 폴백 파생 |

### 2.5 편입 스냅샷 ✅ — `pf_entry_snapshot`

포지션당 1행(`INSERT IGNORE`). **신규 편입에서만** 찍고 수정 저장은 건드리지 않는다.

```
position_id  PK      snapshot_at  DATE
quant_signal ENUM(accumulate|neutral|flame|none)
flame_reason ENUM(by_volume|by_return|both) NULL
box_path     VARCHAR(40)      floors_count INT NULL
box_levels   VARCHAR(120)     -- 편입 시점 계단 가격(내림차순·콤마) ← 결정 ③
signal_date  DATE NULL        ret_20d      DECIMAL(10,6) NULL
sue_latest   DECIMAL(10,4)    def_ver      INT
```

- 조립 = `stock/lib/entry.php` `pf_entry_snapshot_build()`. 판정은 **기존 함수 그대로**(임계는 `Thr`).
- 라벨이 아니라 **조건으로** 되짚는다 — 한글 라벨을 파싱하면 문구를 바꿀 때 기록이 깨진다.
- `def_ver = PF_DEF_VER`(현재 **1**) — 임계를 바꾸면 올린다. 옛·새 스냅샷을 같은 잣대로 섞어 세지 않기 위한 표식(D7).
- **과거분 백필 없음.** 되짚어 만든 값은 「그때 그랬다」가 아니라 「지금 보니 이렇다」다. 옛 포지션은 「**기록 이전**」으로 표시.
- 화면 표기는 **무채색**(`.ent-b`) — 퀀트 배지와 같은 색이면 살아 있는 판정으로 읽힌다.

✅ **`box_levels` 추가됨**(결정 ③ · §9) — 계단 **가격**까지 값으로 든다. `floors_count`(개수)만으로는
「어느 자리가 계단이었나」를 되짚을 수 없어 결국 `krx_surge` 를 다시 읽어야 했다 — 그건 §2.1 이 금지한 「참조」다.

### 2.6 관심종목·보유 동시 존재 ✅ 허용 (v0.3 §4.6 유지)

편입해도 관심종목 행이 남는다(2026-08-03 사용자 규칙). 같은 종목을 다른 포트폴리오에도 담을 수 있어야 하므로.

✅ **정해짐**(결정 ② · §9): `pf_alert_eod()` ①(관심종목 트리거 알림)은 **보유 종목에도 계속 보내되**
문구에 **`<보유중>`** 을 박는다. 빼면 다른 포트폴리오에 담을 기회를 놓치고, 그냥 두면 「새 후보」와 구별이 안 된다.

---

## 3. 탐색 섹션

### 3.1 신호 수명 ✅

`krx_surge_event` (PK `d,code`) — **append-only**. 11,464행 · 0.05 MB.

```
tracking_until DATE  = 신호일 + KrxAmt::TRACK_DAYS(45 달력일)
tracking_state ENUM(active|closed|superseded)
```

현재 상태(2026-08-03): active **241** / closed **11,100** / superseded **123** · 종목당 active 중복 **0**.

- 동기화 = `KrxAmt::syncSurgeEvents()`. `rebuildSurge()`·`warmSurgeAll()` 끝에서만 부른다 —
  이 둘이 `krx_surge` 의 **유일한 쓰기 경로**라 크론 2곳과 화면 lazy 재계산까지 한 곳으로 덮인다.
  **크론 파일은 한 줄도 안 고쳤다.**
- **확정분(`src='k'`)만** 넣는다 — 잠정 신호는 다음날 KRX 확정값에 밀려 사라질 수 있고, 넣으면 지울 수 없는 유령 이벤트가 된다. 수명은 신호일 기준이라 하루 늦어도 손해가 없다.
- 비용 실측: 전체 백필 **0.18초** · 일상 동기화는 하루 12~31행 = 밀리초.

### 3.2 ★★ 왜 별도 테이블인가 (v0.3 계획에서 바뀐 핵심)

`krx_surge` 는 테이블처럼 보이지만 **파생 캐시**다:
- `invalidateSurge()` 가 `DELETE FROM krx_surge WHERE d >= ?` 로 **범위 삭제**하고, `cron/dart_collect.php:412` 가 확정값 들어오는 날마다 부른다.
- `surgeCached()` 는 창 길이(`win`)가 바뀌면 그 날을 통째로 다시 만든다.

⇒ 여기에 `tracking_*` 를 두면 **매일 지워진다**. 그것도 하필 active 이벤트(정의상 최근 45일)가 위험 구간이다.

★ **`invalidateSurge()` 에 `krx_surge_event` 삭제를 넣지 말 것** — 코드에 경고 주석으로 박아 뒀다.

★ `pf_position.surge_event_d` 와 `krx_surge_event` 는 **독립**이다. 포지션은 편입 시점에 `(d, code)` 값만 복사하므로 이벤트 행이 아직 없어도 상관없다.

### 3.3 추적 상태를 읽는 규칙 ✅ (M6)

**`KrxAmt::surgeEventStates(array $sigs, ?string $asOf)`** — key `"code|d"` → `['until','state','left']`.

★★ **이 표에서만 알 수 있는 것은 `superseded`** 다 — 「그 뒤에 같은 종목의 새 신호가 떴다」는 **다른 행을 봐야 아는 사실**이라 신호 하나만 들고는 계산할 수 없다. (`tracking_until` 은 `d + TRACK_DAYS` 라 인라인으로도 나오지만 정의를 한 자리에 두려고 함께 낸다.)

★★ **날짜 우선 판정** — `state` 는 배치가 찍는 값이라 연휴 뒤 며칠 늦을 수 있다. 읽는 쪽은 `left < 0` 이면 `closed` 로 **덮는다**. 배치가 늦어도 판정이 안 틀린다. 현재 지연 실측 **0건**이지만 구조로 막아 뒀다.

★ **경계는 배치와 같아야 한다** — 배치 UPDATE 가 `tracking_until < CURDATE()` 로 닫으므로 **만료일 당일은 아직 추적 중**(`left = 0`). 처음에 `left <= 0` 으로 두어 화면만 하루 먼저 닫혔다(실측 3건).

⇒ 크론 파일은 고치지 않았다. `syncSurgeEvents()` 가 `rebuildSurge()`·`warmSurgeAll()` 끝에 붙어 있고, 그 둘이 사실상 매 거래일 돈다.

### 3.4 ⊗ `accBoxes` 이벤트 기반 전환 — 하지 않는다

v0.3 §3.1 의 per-event 전환을 `accBoxes` 에까지 적용하려다 **되돌렸다.**
`tracking_state` 는 「오늘」의 스냅샷인데 퀀트 화면은 **과거 날짜 조회**를 지원한다(드롭다운).
이벤트 기반으로 바꾸면 어느 날짜를 골라도 오늘의 active 집합이 나와 과거 조회가 깨진다.

무회귀 실측: `2026-07-31 → 7건 · 07-30 → 7 · 07-29 → 7 · 07-28 → 7 · 07-27 → 11` (날짜마다 다름 = 정상).

⇒ 추적 수명은 **표시**로만 쓴다(추적 카드의 「D-N · 교체 · 종료」 열).

### 3.5 절단 고지 ✅ (M6) — no silent caps

`accBoxes()` 가 총건수를 out-param(`&$total`)으로 낸다. 화면은 **세 층**을 각각 밝힌다:

```
※ 조회 52건 중 최신 40건만 가져옴 · 같은 종목의 옛 박스 8건 접음 · 종목 21개 중 15개 표시
```

★ **LIMIT 을 없애지 않는다** (v0.3 「제거」에서 정정) — 무제한이면 `boxStatusMany` 가 행수에 비례해 봉을 읽는다. 어닝 탭에서 무제한 호출이 **256MB 를 넘긴 실측**이 있다. **자르되 잘랐다고 말한다.**
집계 동작 검증: `cap=40 → 5/5 잘림 없음` · `cap=3 → 5건 중 3건, 잘림 표시 O`.

### 3.6 45일 기산점 ✅ 달력일 유지

`TRACK_DAYS = 45` **달력일**. 거래일로 바꾸지 않는 이유: 이 레포는 창 단위가 이미 섞여 있고(`momMany` 20·40 거래일 · `boxStatusMany` 경과 거래일), 여기만 바꾸면 잣대가 더 갈린다. 바꾸려면 화면 문구·검증 탭 정의를 함께 고쳐야 한다.


---

## 4. 포트폴리오 섹션

### 4.1 판단 체계 (유지)

③ 행동(룰셋 차수표 → 매수/잔여매수/매도/대기 + 신뢰도) · ④ 경보(장기물림 · N차 지연 · 계단관통↓).

### 4.2 화면 노출 ✅ 확정

종목 상세 상태판의 층 구성:

```
전:  시장 · SUE · [퀀트 : 최고 거래대금] · 행동 · 포트폴리오
후:  시장 · SUE · [편입 당시]           · 행동 · 포트폴리오
```

- 20·40일 모멘텀 칩(`momNow`)은 **남는다** — 오늘 기준으로 계산한 가격 지표라 ① 시장 공통층(§1.1).
- 「기준 박스 없음」은 **상세에만**.

### 4.3 사다리 방식별 차등 (유지)

계단관통↓ 기준 박스는 **방식 무관 공통**(편입 박스). 나머지는 v0.3 §4.3 그대로.
★ `quant_ladder` 의 `delay_days`(`[0,0,20,20,20]`)·목표수익률(`[0.15…0.35]`)은 여전히 **하드코딩** — ⊸ M5 2차 대상.

### 4.4 룰셋 차수표 우선 ✅ 문서화 완료

**정본 = 현행조사보고서 §2.** 두 모드가 `pf_position_calc()` 진입 직후 한 곳에서만 갈라지고 하류를 공유한다.
핵심 식: `w_n = [p_n·Q_{n−1} − C_{n−1}(1+b_n)] / b_n` · `b_i = beLast × x^k` · **목표수익률은 입력이 아니라 파생**(`t_i = e_i / a_i − 1`).

### 4.5 신뢰도 라벨

임계 전부 `assumed` — 검증 탭에 항목 자체가 없다. 레지스트리 「근거」 칸에 그대로 적혀 있다.
★ `pf_signal_confidence()` 의 변수명이 뜻과 반대다(`$over`=과매도 · `$under`=과매수). 지금 고치면 분기를 전부 뒤집어야 해서 **주석으로 밝혀만 뒀다**.

---

## 5. 임계 — `classes/Thr.class` ✅

### 5.1 왜 테이블이 아닌가

만들어 보면 **정본이 둘**이 된다(코드 기본값 ↔ DB 행). 그리고 이 값들은 사용자가 화면에서 바꾸는 설정이 아니라 **백테스트가 낳은 연구 결과**다 — 바꾸려면 근거를 다시 재고 회귀 테스트를 돌려야 하니 배포와 함께 가는 편이 옳고, 변경 이력은 git 이 갖는다.
⇒ 레지스트리의 **목적**(단일 정본 + evidence + 문서 화면이 읽어 갈 곳)은 지키고 저장소만 코드로 뒀다. DB 가 필요해지면 `Thr::all()` 을 그대로 적재하면 된다.

★ 클래스인 이유: 오토로더가 어디서든 읽어 준다 — 화면·api·lib·`KrxAmt.class`·크론까지. include 파일이면 `classes/ → stock/` 역참조가 생긴다.

### 5.2 현황 — 25키

| evidence | 개수 | 뜻 |
|---|---|---|
| `measured` | **11** | 검증 탭에 표·표본·기간분할이 있다 |
| `assumed` | **11** | 사용자 지정 — 백테스트 근거 없음 |
| `operational` | 3 | 판정이 아니라 운영상 선택 |

★★ **지정값이 실측만큼 많다**는 것이 이 표의 알맹이다. 그 사실을 가리면 「전부 백테스트로 정했다」는 틀린 인상을 준다.

### 5.3 통합한 사본 ✅

| 무리 | 전 → 후 |
|---|---|
| 매집형/불꽃형 | **4 → 1** (`pf_surge_badge`·`pf_alert_badge`·스냅샷·`accBoxes` **SQL**) |
| SUE ±1 | **6 → 1** (배지·어닝규칙·어닝표·스크리너·알림×2·**차트 JS**) |
| 급등락 3σ/15% | 2 → 1 |
| 거래량 2배 | 2 → 1 |
| floors ≥ 3 | 2 → 1 |
| 52주 | 2 → **2 (의도적)** — `*_BADGE`(5/95) vs `*_CONF`(10/90). D4 「indicator × 용도」 실증 |
| 하한 100억 | 4 → 1 |

**값 동일성 증명**: `krx_surge` **11,464건 전수** 대조(옛 리터럴 로직 vs 새 상수 로직, 화면·알림 두 경로 동시) **불일치 0** · 스냅샷 200건 0 · `accBoxes` 결과 집합 동일 · calc 628 / sim 103 PASS.

### 5.4 첫 소비자 ✅

- **신호분석**(`mode=signal`)에 「임계 레지스트리」 표 신설 — 값·**근거**·뜻. 임계 문구 12곳을 상수 보간으로 교체.
- **검증 탭**(`mode=quantstat`)에서 두 종류 숫자를 갈랐다:
  **측정 조건**(창·하한) = `Thr`·`KrxAmt::SURGE_WIN` 에서 읽음 / **측정 결과**(표본·수익률) = 리터럴 고정.
  ⇒ 「조건이 바뀌었는데 결과가 그대로면 다시 재야 한다는 뜻」을 화면에 명시. 검증 탭이 스스로 불일치를 드러낸다.

★ 이 과정에서 **사실이 틀린 문장**을 잡았다: 공통 원칙 1 이 「임계값은 **전부** 실측에서 왔다」였는데 11개가 지정값이다. 정정 완료.

### 5.5 M5 2차 ✅ — 이름 붙이기

사본이 없는 단일 위치 리터럴이라 **드리프트 위험이 아니라 가독성** 문제였다. 값은 하나도 바꾸지 않았다.

★★ **두 갈래로 나눠 두는 것이 이번 판단의 핵심이다** — 전부 `Thr` 에 밀어 넣으면 신호분석의 임계표가
읽을 수 없게 부풀고, 「화면이 사람에게 설명해야 하는 기준」과 「알고리즘 내부값」이 뒤섞인다.

| 갈래 | 어디에 | 무엇 |
|---|---|---|
| **판정·설명이 필요한 값** | `classes/Thr.class` (+11키 → **36키**) | 유동성 등급 50/10/1억 · 저거래일 1억 · 표본 하한 21/200/250/20봉 · RSI 14 · SUE 이력 8/4 |
| **알고리즘 내부값** | `lib/calc.php` 이름있는 상수 | `PF_BOX_*` 9개(레벨 3~5 · 최소깊이 · ratio·곡률 스캔 · 비중 허용오차·1차 하한) · `PF_GEN_*` 14개(생성기 기본값·clamp·곡률 탐색 범위) |

레지스트리는 이제 **measured 11 · assumed 11 · operational 14** = 36키.
★ 「지정값이 실측만큼 많다」는 사실은 그대로다 — operational 이 늘어도 그 둘의 비는 안 바뀐다.

**값 동일성 실측**: 유동성 등급 14종목 **불일치 0** · 표본 경계 정확히 21봉(σ)/200봉(52주위치) ·
퀀트 사다리 해 3케이스 정상(전부 계단형) · 생성기 기본값 → 7단계·최종 BE **−35.00%**(= `PF_GEN_BE_LAST_DEF`) ·
calc 628 / sim 103 PASS.

★ 남긴 원칙: **표본이 모자라면 계산하지 않는다.** 0 이나 중립으로 채우면 「특이 없음」으로 읽히는데
사실은 「아직 모른다」다 — `BARS_52W_MIN` 의 설명에 그대로 적어 신호분석 표에 뜨게 했다.

---

## 6. 데이터 모델 — 실제 스키마

```
pf_position                                   -- +3컬럼 (M1)
  ladder_method ENUM('ruleset_pct','quant_ladder') NULL
  entry_trigger ENUM('breakout','step_support','none') NULL
  surge_event_d DATE NULL                     -- krx_surge(d,code) 의 d

krx_surge_event   (d, code) PK                -- 신설 (M1) · append-only · 11,464행
  tracking_until DATE NOT NULL
  tracking_state ENUM('active','closed','superseded')
  KEY (tracking_state, tracking_until) · KEY (code, d)

pf_entry_snapshot  position_id PK             -- 신설 (M4) · 신규분만
  (§2.5 참조) · positionDelete 가 함께 지운다(FK 대신 명시 삭제)

classes/Thr.class                             -- 임계 정본 (M5) · 25키
stock/lib/entry.php                           -- 편입 스냅샷 조립 (M4)
```

**주요 메서드** (M1~M6 신설)

| 메서드 | 역할 |
|---|---|
| `Pf::positionSurgeEventDate($id)` | 기준 신호일만 단일 테이블로 읽는다 (★`positionGet` 은 INNER JOIN 이라 못 쓴다 — §8-1) |
| `Pf::entrySnapshotSave/entrySnapshot` | 편입 기록 저장·조회 (`INSERT IGNORE` = 편입은 한 번) |
| `KrxAmt::syncSurgeEvents($from)` | 이벤트 upsert + 만료·교체 UPDATE. `rebuildSurge`·`warmSurgeAll` 끝에서만 부른다 |
| `KrxAmt::surgeEventStates($sigs, $asOf)` | 추적 상태 — `superseded` 는 여기서만 알 수 있다 (§3.3) |
| `KrxAmt::accBoxes(…, &$total)` | 매집형 추적 목록 + **총건수**(절단 고지용 · §3.5) |
| `pf_stair_alert_map($positions)` | 계단관통↓ — **포지션 id 키** (같은 종목이라도 포지션마다 기준 박스가 다르다) |
| `Thr::all() / counts() / pct() / num()` | 임계 레지스트리 + 문서 화면용 포맷터 |

**용량** — `krx_surge_event` 0.05 MB · `pf_entry_snapshot` ~0 · 컬럼 3개 × 19행.
전체 DB 1,023 MB 중 **0.005%**. 연 증가 ~0.4 MB (참고: `krx_amt` 는 연 90~95 MB).

---

## 7. 마이그레이션 상태

| 단계 | 내용 | 상태 |
|---|---|---|
| M1 | 컬럼 추가 + `krx_surge_event` + 백필 | ✅ 배포·검증 (0.27초) |
| M2 | 쓰기 경로 — 편입 3축 저장 | ✅ 4케이스 실측 PASS |
| M3 | 읽기 경로 교체 — 경보 의미 복원 | ✅ 경보 9→1 렌더 확인 |
| M4 | V1 제거 + `pf_entry_snapshot` | ✅ 렌더 8경로 확인 |
| M5 | 임계 정본 + 문서 화면 연결 | ✅ 11,464건 전수 대조 |
| M6 | 절단 3층 고지 + 추적 상태 표시(`surgeEventStates`) | ✅ 5경로 렌더 · 과거 날짜 무회귀 |
| M5-2 | 인라인 리터럴 2차 — 이름 붙이기(Thr 11 + calc 23) | ✅ 값 동일성 실측 |

**되돌리기**: 전부 add-only. `ALTER TABLE pf_position DROP COLUMN` ×3 + `DROP TABLE krx_surge_event, pf_entry_snapshot`.
백업: `D:\tmp\backup_pf_position_pre_M1.sql`(18행 · M1 이전).

---

## 8. 구현 중 실제로 밟은 함정 (재발 방지)

1. **`positionGet()` 은 INNER JOIN 이다** — `pf_stock`·`pf_portfolio`·`pf_rule_set` 중 하나라도 비면 **행 전체가 null**. 스냅샷 자리에서 그 일이 나면 `signal_date` 가 조용히 비어 「기준 박스 없는 편입」으로 잘못 기록된다. ⇒ `Pf::positionSurgeEventDate()`(단일 테이블) 신설.
2. **`uk_pf_pos(portfolio_id, stock_code)` 는 UNIQUE** — 같은 포트폴리오에 같은 종목을 두 번 넣으면 500. 시험 스크립트는 케이스마다 즉시 지울 것.
3. **`require_login()` 이 `ensureTables()` 보다 먼저** — 마이그레이션은 HTTP 로 트리거되지 않는다(302). 키가드 스크립트로 부른다.
4. **`pf_surge_badge()` 는 `index.php` 에 있다** — lib 에서 못 부른다. 그래서 `pf_alert_badge()` 라는 축약 사본이 생겼던 것이다.
5. **M1↔M2 배포 틈** — 그 사이에 만든 포지션은 `ladder_method` 가 NULL 로 남는다(실제로 1건 발생·같은 규칙으로 메움). 단계를 나눠 배포하면 틈을 의식할 것.
6. **`(a, b) IN ((?,?),…)` 은 열 순서와 인자 순서가 같아야 한다** — `(d, code)` 로 적고 `code, d` 를 넣어 **한 건도 조회되지 않았다**. 에러가 아니라 <b>빈 결과</b>로 나와서 렌더만 봐서는 못 잡는다(값을 찍어서 잡았다). 이 레포는 `(code, d)` 순으로 통일한다.
7. **상태 컬럼과 그것을 읽는 코드의 경계를 맞출 것** — 배치는 `until < CURDATE()` 로 닫는데 읽기가 `left <= 0` 이면 화면만 하루 먼저 닫힌다. 「배치가 늦어도 안 틀리게」 방어하는 것과 「배치와 다르게 판정하는 것」은 다르다.

---

## 9. 결정 3건 ✅ (2026-08-03 확정)

### ① `SURGE_BACKFILL_MAX_DAYS = 60` — **유지**

바꾸지 않는다. 이 상한은 **백필에만** 걸린다 — 신규 편입은 화면이 보여 준 신호를 그대로 받으므로 무관하다.
남는 것은 「옛 18건을 어떻게 볼 것인가」인데, 그 대부분은 **퀀트 도입 이전(2021~2022)에 시작한 포지션**이다.
273일 전 박스를 「내가 산 이유」라 부르는 것은 기록이 아니라 창작이다.
⇒ 경보가 9→1 로 준 것은 손실이 아니라 **거짓 경보 8건이 사라진 것**이다.
※ 60 은 「편입 두 달 안의 신호면 그 판단에 영향을 줬을 법하다」는 선이다. 바꾸려면 상수 하나 + UPDATE 한 번.

### ② 보유 종목의 트리거 알림 — **유지하되 「보유중」을 문구에 박는다** ✅ 구현

같은 종목을 다른 포트폴리오에 담는 길을 열어 둔 이상(§2.6) 그 신호는 **여전히 실행 가능하다** — 빼면 진짜 기회를 놓친다.
문제는 「새 후보」와 「추가 편입 기회」가 구별되지 않는 것이었으므로, **구별을 만들어 준다**:

```
🟢 한국주철관 <보유중> 돌파확인 — 매집형×돌파 (실측 +2.26%·57.9%)
```

중복 잡음은 `pf_alert_log` 가 `(code|d|종류)` 로 막아 **이벤트당 한 번**만 울린다.
실측: 보유 10 · 관심 7 · **겹침 2종목**(222420·252990) — 그 둘에만 표시가 붙는다.

### ③ `entry_snapshot` 에 계단 레벨 — **추가** ✅ 구현

`box_levels VARCHAR(120)` (내림차순·콤마). `floors_count`(개수)만으로는 **「어느 가격이 계단이었나」를 되짚을 수 없어서**,
결국 `krx_surge` 를 다시 읽어야 했다 — 그건 §2.1 이 금지한 「참조」다. 레벨을 값으로 들면 스냅샷이 **자기완결**이 된다.

- `KrxAmt::boxStatusMany()` 가 `levels` 를 값으로 낸다(기존 키에 추가 — 소비자 영향 없음).
- 종목 상세 「편입 당시」 툴팁이 `계단 3,840/3,630` 까지 말한다.
- 실측 검증: 413630(2026-07-28) → `floors=7 · levels=[3840,3630]` → 스냅샷 `box_levels='3840,3630'` ✔
  (`levels` 는 **매매용 계단 = 최근 박스 2개**라 최대 4개, `floors` 는 무제한 카운트 — 둘의 수가 달라도 정상)

---

## 10. 불변 규칙 ✅ CLAUDE.md 게재 완료 (2026-08-03)

> **`CLAUDE.md` 의 「/stock 포트폴리오 — ★불변 규칙」 절이 정본이다.** 아래는 그 사본이다.
> 규칙을 고치면 **두 곳을 함께** 고친다(이 문서가 규칙 3 을 스스로 어기지 않도록).

1. **포트폴리오 화면에 탐색 판정(2-a·b·c) 금지.** 예외는 계단관통↓(④ 결과)과 `pf_entry_snapshot`(기록) 둘뿐.
2. **`krx_surge` 를 포트폴리오 로직에서 읽을 때는 반드시 `pf_position.surge_event_d` 경유.** `SELECT MAX(d) FROM krx_surge` 패턴 신규 생성 금지.
3. **임계 리터럴 신규 생성 금지** — `classes/Thr.class` 경유. 문서 화면의 설명 문구도 상수에서 보간한다.
4. **`invalidateSurge()` 에 `krx_surge_event` 삭제 금지** — append-only 다.
5. **차수 지연 판정 적용 3곳**(`pf_load_calc`·종목상세·api payload) 동시 수정 — 하나만 고치는 커밋 금지.
6. **새 테이블은 접두어로 소속을 밝힌다** — 포트폴리오 `pf_` · KRX 원장 계열 `krx_`.

---

## 11. 한눈에 — 이번에 실제로 달라진 것

| 무엇 | 전 | 후 |
|---|---|---|
| 계단관통↓ 경보 | 최신 박스 기준 **9건** | 편입 박스 기준 **1건** |
| 보유 종목상세의 퀀트 판정 | 2-a·2-b 배지 | **제거** → 「편입 당시」 기록 |
| 편입 시점 사실 | `source` 하나 | `source` + `ladder_method` + `entry_trigger` + `surge_event_d` + 스냅샷 9필드 |
| 임계 사본 | 매집형 4벌 · SUE **6벌** · 급등락 2벌 … | **전부 1벌** (`Thr` 36키) |
| 임계 근거 | 「전부 실측에서 왔다」(**틀린 문장**) | 실측 11 · **지정값 11** · 운영 3 — 화면에 표로 |
| 추적 카드 절단 | 말없이 3층에서 잘림 | 층마다 **몇 건 잘렸는지** 고지 |
| 신호 수명 | 없음 (rolling 45일 창만) | `krx_surge_event` 11,464행 · active/closed/superseded |

**검증 총량**: `krx_surge` **11,464건 전수** 판정 대조(불일치 0) · calc **628** / sim **103** PASS ·
서버 렌더 최대 **11경로** PHP 오류 0 · 쓰기 경로 4케이스 실측 · 과거 날짜 5일 무회귀.

---

*v0.4 — 2026-08-03. M1~M6 · M5-2 구현 반영 · 결정 3건 확정 · 불변 규칙 CLAUDE.md 게재.
계획된 작업은 전부 끝났다. 남은 것은 커밋뿐이다.*
