# 선물 발송 관리 시스템 요건정의서

## 0. 개요

명절(설/추석) 등 연 2회 진행하는 선물 발송 업무를 관리하는 웹 시스템.
현재 엑셀(`선물.xlsx`)로 관리 중인 업무를 대체한다.

**핵심 흐름**

```
고객명단 관리 ──┐
                ├──> 회차 생성(작업중) ──> 발송 대상/물품/수량 편집 ──> [저장/확정]
물품 관리 ──────┘                                                         │
                                                                          ▼
                                                    이력(읽기전용) + 업체발송용 엑셀 다운로드
```

**기술 스택**

- PHP 8.x (기존 서버 환경에 맞춤), MySQL 8.x / MariaDB 10.x
- 프레임워크: 기존 서버 구성에 따름. 별도 지정이 없으면 순수 PHP + PDO 로 구현하되, `db/`, `lib/`, `pages/`, `api/` 로 디렉터리 분리
- 엑셀 생성: PhpSpreadsheet (composer)
- 우편번호: Daum(카카오) 우편번호 서비스 JS API — 무료, 키 불필요
- 문자셋: DB/테이블/커넥션 모두 `utf8mb4_unicode_ci`

---

## 1. 데이터 모델

### 1.1 customers (고객 명단)

기준 정보. 매 회차에서 재사용된다.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | BIGINT PK AI | |
| name | VARCHAR(100) NOT NULL | 고객명 (예: 박병주, 노르웨이의숲) |
| memo | VARCHAR(200) | 추가정보 — 직함·관계 메모 (예: 박회장, 서장님, 지인) |
| phone | VARCHAR(30) | 연락처 |
| zipcode | VARCHAR(10) | 우편번호 (5자리 신주소 기준) |
| address1 | VARCHAR(255) | 기본주소 (우편번호 API 반환값) |
| address2 | VARCHAR(255) | 상세주소 (직접 입력) |
| source_type | ENUM('manual','directory') DEFAULT 'manual' | 직접입력 / 기존 인명록에서 가져옴 |
| source_ref_id | VARCHAR(64) | 인명록 원본 PK (중복 등록 방지용) |
| is_active | TINYINT(1) DEFAULT 1 | 소프트 삭제 |
| created_at, updated_at | DATETIME | |

- INDEX: `name`, `phone`, `source_ref_id`
- 주소가 없는 고객이 다수 존재하는 것이 정상 (오프라인 직접 전달 대상)

### 1.2 products (물품)

회차마다 취급 물품이 바뀔 수 있으므로 독립 관리.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | BIGINT PK AI | |
| name | VARCHAR(150) NOT NULL | 물품명 (예: 우레짱아찌, 우레짱아찌+참기름) |
| unit_price | INT NOT NULL DEFAULT 0 | 단가(원) |
| description | VARCHAR(255) | 구성 설명 |
| is_active | TINYINT(1) DEFAULT 1 | 비활성 시 신규 선택 목록에서 제외 (기존 이력은 유지) |
| sort_order | INT DEFAULT 0 | |
| created_at, updated_at | DATETIME | |

### 1.3 batches (선물 회차)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | BIGINT PK AI | |
| title | VARCHAR(100) NOT NULL | 예: `26년 2월 설선물` |
| year | SMALLINT NOT NULL | 2026 |
| season | ENUM('설','추석','기타') NOT NULL | |
| send_date | DATE | 발송 예정일 |
| status | ENUM('draft','confirmed') DEFAULT 'draft' | 작업중 / 확정(이력) |
| memo | TEXT | |
| total_qty | INT DEFAULT 0 | 확정 시 집계 저장 |
| total_amount | BIGINT DEFAULT 0 | 확정 시 집계 저장 |
| confirmed_at | DATETIME | |
| created_at, updated_at | DATETIME | |

- UNIQUE: `(year, season)` — 동일 연도·명절 중복 생성 방지 (`기타`는 예외 처리)
- **`status='draft'` 회차는 동시에 1개만 존재**하도록 제한

### 1.4 batch_items (회차별 발송 라인) ★ 스냅샷 테이블

> **중요**: 확정된 회차의 내역은 이후 고객·물품 정보가 바뀌어도 절대 변하면 안 된다.
> 따라서 고객명/연락처/주소/물품명/단가를 **값으로 복사 저장**한다. (customer_id는 참조용으로만 보관)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | BIGINT PK AI | |
| batch_id | BIGINT NOT NULL FK→batches | ON DELETE CASCADE |
| seq | INT NOT NULL | 회차 내 순번 (엑셀 No 컬럼) |
| customer_id | BIGINT NULL FK→customers | ON DELETE SET NULL |
| customer_name | VARCHAR(100) NOT NULL | 스냅샷 |
| customer_memo | VARCHAR(200) | 스냅샷 |
| phone | VARCHAR(30) | 스냅샷 |
| zipcode | VARCHAR(10) | 스냅샷 |
| address1 | VARCHAR(255) | 스냅샷 |
| address2 | VARCHAR(255) | 스냅샷 |
| delivery_type | ENUM('택배','일괄','별도') NOT NULL | |
| product_id | BIGINT NULL FK→products | ON DELETE SET NULL |
| product_name | VARCHAR(150) NOT NULL | 스냅샷 |
| unit_price | INT NOT NULL | 스냅샷 |
| extra_qty | INT NOT NULL DEFAULT 0 | "추가" 수량 |
| total_qty | INT NOT NULL DEFAULT 1 | **= 1 + extra_qty (자동계산)** |
| amount | INT NOT NULL DEFAULT 0 | **= unit_price × total_qty (자동계산)** |
| note | VARCHAR(255) | 비고 |
| created_at, updated_at | DATETIME | |

- INDEX: `(batch_id, seq)`, `customer_id`

> **기존 엑셀과의 차이 주의**: 엑셀의 `단가` 컬럼은 실제로는 *합계 금액*(50,000×2=100,000)이 들어가 있었다.
> 시스템에서는 `unit_price`(순수 단가) / `total_qty` / `amount`(합계)를 명확히 분리한다.

---

## 2. 화면 및 기능 요건

### 2.1 대시보드 (`/`)

- 현재 작업중(draft) 회차 요약: 대상 인원 / 총 수량 / 총 금액 / 미완성 항목 수
- 최근 이력 5건
- 바로가기: 고객관리 / 물품관리 / 현재 회차 편집

---

### 2.2 고객 명단 관리 (`/customers`)

**목록**
- 컬럼: 고객명, 추가정보, 연락처, 우편번호, 주소, 등록출처, 수정/삭제
- 검색: 고객명·연락처·주소 통합 검색
- 필터: `주소 있음 / 주소 없음(오프라인 전달)`, 등록출처
- 페이징 (기본 50건)
- 정렬: 고객명, 등록일

**등록 — 두 가지 경로**

1. **인명록에서 가져오기** (모달)
   - 기존 PHP 서버의 인명록 테이블을 검색 → 체크박스 다중 선택 → 일괄 등록
   - `source_ref_id` 로 중복 검사, 이미 등록된 건은 목록에서 `등록됨` 뱃지 표시 후 선택 불가
   - 가져온 뒤에도 자유롭게 수정 가능 (customers는 인명록과 동기화하지 않는 독립 사본)
   - ⚠️ **인명록 테이블/컬럼 구조는 미확정 → 3장 확인사항 참조.**
     구현 시 `lib/DirectoryRepository.php` 로 접근 계층을 분리해, 실제 스키마 확정 시 이 파일만 수정하면 되도록 할 것

2. **직접 입력**
   - 고객명(필수), 추가정보, 연락처, 주소(우편번호 검색), 상세주소

**삭제**
- 소프트 삭제(`is_active=0`). 과거 이력은 스냅샷이므로 영향 없음

---

### 2.3 물품 관리 (`/products`)

- 목록 / 등록 / 수정 / 활성·비활성 토글
- 필드: 물품명, 단가, 설명, 노출순서, 사용여부
- 단가 수정 시: **이미 확정된 회차에는 영향 없음.** 작업중 회차에 대해서는
  "현재 작업중 회차의 해당 물품 단가도 갱신할까요?" 확인 후 일괄 반영(선택)
- 삭제는 미제공(비활성화만) — 이력 무결성 보호

---

### 2.4 메인 발송 리스트 (`/batch`) — 작업중 회차 편집

**헤더 영역**
- 회차명 / 발송예정일 / 상태 표시
- 집계: 대상 N명, 총 수량 N개, 총 금액 N원 (발송구분별 소계 포함)
- 버튼: `[대상 추가]` `[임시저장]` `[회차 확정 저장]`

**리스트 (인라인 편집 가능한 테이블)**

| No | 고객명 | 추가정보 | 연락처 | 우편번호 | 주소 | 발송구분 | 물품 | 단가 | 추가 | 총지급 | 금액 | 비고 | 삭제 |

- `물품`: 활성 물품 셀렉트박스 → 선택 시 단가 자동 입력
- `추가` 입력 시 `총지급 = 1 + 추가`, `금액 = 단가 × 총지급` 즉시 재계산
- `발송구분` 기본값 규칙: **주소가 있으면 `택배`, 없으면 `일괄`** (사용자가 언제든 변경 가능, `별도`는 수동 선택)
- 행 단위 인라인 저장(Ajax) 또는 전체 임시저장 — 새로고침해도 작업 내용 유지
- 필터/검색: 발송구분, 주소유무, 고객명
- 일괄 작업: 선택 행에 대해 `물품 일괄 지정`, `발송구분 일괄 변경`, `선택 삭제`

**대상 추가 모달**
- 고객 명단에서 검색 → 다중 선택 → 리스트에 추가
- `전체 고객 일괄 추가` 버튼 (이전 회차 대상 그대로 불러오기 옵션 포함: `이전 회차에서 복사`)
- 이미 리스트에 있는 고객은 중복 추가 방지

**회차 확정 저장**
- 확인 다이얼로그: "확정 후에는 수정할 수 없습니다"
- 처리: 유효성 검사 → `total_qty`/`total_amount` 집계 → `status='confirmed'`, `confirmed_at` 기록
- 확정 후 `/history/{id}` 로 이동
- **유효성 검사(확정 차단 조건)**
  - 물품 미지정 행 존재
  - 발송구분 `택배`인데 주소 또는 우편번호가 비어있는 행 존재
  - 대상 0건
  - → 해당 행을 하이라이트하여 안내

---

### 2.5 이력 (`/history`)

**목록**
- 카드 또는 테이블: `26년 2월 설선물` / 발송일 / 대상 N명 / 총 N개 / 총 N원 / 확정일시
- 최신순 정렬, 연도 필터
- 클릭 → 상세

**상세 (`/history/{id}`) — 읽기 전용**
- 회차 정보 헤더 + 집계(발송구분별·물품별 소계)
- 전체 라인 테이블 (편집 UI 없음)
- 버튼: `[업체 발송용 엑셀 다운로드]` `[전체 내역 엑셀 다운로드]` `[인쇄]`
- **수정·삭제 기능 없음.** (관리자 전용 삭제가 필요하면 별도 권한으로 분리)

---

### 2.6 엑셀 다운로드

PhpSpreadsheet 사용. 파일명: `{회차명}_발송명단_{YYYYMMDD}.xlsx`

**① 업체 발송용** (택배 건만, 배송 라벨 기준)

| No | 받는분 | 연락처 | 우편번호 | 주소 | 상품명 | 수량 | 비고 |

- `주소` = address1 + " " + address2 결합
- `발송구분='택배'` 행만 포함
- 헤더 굵게/배경색, 열 너비 자동, 틀 고정(1행)

**② 전체 내역용**
- 화면 리스트 전 컬럼 + 하단 합계 행
- 발송구분별로 시트 분리 (`택배`, `일괄`, `별도`)

---

## 3. 확인이 필요한 사항 (구현 전 답변 필요)

1. **기존 인명록**: 테이블명, 주요 컬럼(이름/연락처/주소/우편번호/PK), 같은 DB인지 별도 DB인지, DB 직접 접근 가능한지 아니면 API인지
2. **로그인/권한**: 기존 서버의 인증 체계를 재사용하는지, 신규 구현인지. 단일 관리자면 간단한 세션 로그인으로 충분
3. **우편번호 API**: Daum(카카오) 우편번호 서비스 사용 권장 — 무료·키 불필요·주소검색 팝업 제공.
   (네이버/카카오 **지도** API는 좌표 변환용이라 우편번호 검색 용도로는 부적합. 지도 표시가 별도로 필요한지 확인)
4. **선물업체 엑셀 양식**: 업체가 지정한 고정 양식이 있으면 해당 양식 파일 제공 필요 (컬럼 순서·명칭 맞춤)
5. **기존 `선물.xlsx` 데이터 마이그레이션**: 고객 60명을 초기 데이터로 임포트할지 여부 (임포트 스크립트 필요 시 별도 작성)
6. **동시 사용자 수**: 1명 단독 사용이면 잠금 처리 불필요

---

## 4. 비기능 요건

- **문자셋**: utf8mb4 (DB, 테이블, PDO DSN `charset=utf8mb4`, HTML meta 모두)
- **보안**
  - 모든 쿼리 PDO Prepared Statement (문자열 연결 금지)
  - 출력 시 `htmlspecialchars()` — 고객명/주소에 특수문자 가능성
  - 폼 전송 시 CSRF 토큰
  - 연락처·주소는 개인정보 → 로그인 없이 접근 불가
- **반응형**: 데스크톱 우선, 태블릿까지 대응. 리스트 테이블은 가로 스크롤 허용
- **금액 표시**: 천단위 콤마, 정수 원 단위 (소수점 없음)
- **에러 처리**: DB 오류 시 사용자에게는 일반 메시지, 상세는 로그 파일로

---

## 5. 구현 우선순위

| 단계 | 범위 |
|---|---|
| 1 | DB 스키마 + 마이그레이션 SQL, DB 접속/공통 레이아웃 |
| 2 | 물품 관리 CRUD |
| 3 | 고객 관리 CRUD + 우편번호 검색 연동 (인명록 연동은 인터페이스만 만들고 후순위) |
| 4 | 회차 생성 + 메인 발송 리스트 편집 + 자동 계산 |
| 5 | 회차 확정 → 이력 목록/상세 |
| 6 | 엑셀 다운로드 2종 |
| 7 | 인명록 연동 (스키마 확정 후) |

---

## 부록 A. 참고 — 현행 엑셀 데이터 특성

- 총 60명, 물품은 전량 `우레짱아찌` 단일
- 발송구분: 택배 39 / 일괄 19 / 별도 2
- 총 수량 83개, 총 금액 4,150,000원 (기본 단가 50,000원)
- 우편번호 컬럼은 전량 공란 → 시스템에서 우편번호 검색으로 보강 필요
- 다수 지급 사례: 노르웨이의숲 11개, 강남고려병원 4개, 박영숙·서울대교수 각 3개 등
- 시트 우측에 물품 후보 목록 존재: 우레짱아찌 / 우레짱아찌+참기름 / 우레짱아찌(소) / 우레짱아찌(소)+참기름 / 참기름 / 고추가루 / 우레짱아찌+소고기 / 우레짱아찌(소)+소고기
  → **products 테이블 초기 데이터로 활용 가능**
