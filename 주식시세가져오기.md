# 주식시세 가져오기 — 데이터 지도

**작성 2026-07-31** · 서버 DB 실측(125개 테이블 전수 조사) + 소비처 grep 기준

이 문서의 목적: **새 시세 데이터가 필요할 때 "이미 있는 걸 쓸까 / 새로 받을까"** 를 이 파일만 보고 판단한다.
같은 값을 두 번 받아 두 곳에 저장하는 일을 막는 것이 목표다.

---

## 0. 30초 요약

| 사실 | 내용 |
|---|---|
| 외부 소스는 **3곳** | 네이버(주가·일봉) · KRX 오픈API(공식 시세·상장주식수·**거래대금**) · DART(재무) |
| 일봉(OHLCV)을 담는 곳 | **`krx_amt`(전종목·장기 원장)** · `pf_daily`(장기·14종목·수정주가) · `pf_sim_data`(rows_json·20종목) · `analysis_stock`(레거시) · `stock_daily_range`(고·저만) |
| **거래대금**을 담는 곳 | `krx_daily`(전종목·최근 10일) → **`krx_amt`(전종목·장기)** |
| 전종목 현재가 | `all_stock_info` (하루 9회 갱신·TRUNCATE 방식). ★거래량 `stock_vol`(주)·거래대금 `stock_vol_cap`(**억원**)도 들어 있다 |
| ★ 차트 API 는 **저장하지 않는다** | `stock_analysis_api.php?action=daily` 는 요청마다 네이버를 크롤해 그대로 넘긴다(거래대금만 DB에서 붙임) |
| ★ 새 데이터가 필요하면 | **먼저 이 표에서 찾는다.** 없을 때만 새로 받는다 (§4 의사결정) |

---

## 1. 외부 소스 3곳

| 소스 | 무엇을 주나 | 못 주는 것 | 쓰는 곳 |
|---|---|---|---|
| **네이버** `fchart.stock.naver.com` · `api.finance.naver.com` | 일봉 OHLCV(수정주가)·분봉·현재가 | **거래대금 없음** | 차트 API·`pf_daily`·`pf_sim_data`·`stock_daily_range` |
| **KRX 오픈API** `data-dbg.krx.co.kr` (키: `env/krx.inc`) | **일자별 전종목** 시세·거래대금·상장주식수·시총 | 종목별 시계열 조회 불가 | `krx_daily` → `krx_amt` |
| **DART** (키: `env/dart.inc`) | 재무제표·공시 | 시세 아님 | `stock_financial` |

**★ 네이버 일봉은 수정주가다** — 액면분할·배당락이 소급 반영돼 과거 값이 KRX 원본과 다를 수 있다(가짜 급락은 없음).
**★★ KRX 는 반대로 <b>원본</b>이다(수정주가 아님)** — 액면분할이 있으면 그 날 거래량이 통째로 몇 배 뛴다.
  「거래량 60일 최고」 같은 판정에서 곧바로 오탐이 되므로, 같이 저장하는 `list_shrs`(상장주식수) 변동으로 가려낸다.
  **거래대금은 이 함정에 면역이다**(분할해도 금액은 그대로) — 그래서 급증 판정의 기준은 거래대금으로 잡는다.
**★ KRX 오픈API 는 일자별 전종목뿐** — 종목 하나의 4년치를 받으려면 985일을 훑어야 한다. 그래서 §5 를 참고할 것.

### ★★★ 보관 깊이 — 「730일」은 틀렸다 (2026-07-31 실측으로 정정)

여기저기에 「KRX 보관 730일」이라 적어 뒀는데, **2년 전까지만 찍어 보고 거기서 멈춘 결과**였다.
더 내려가 재 보니 **최소 11년까지 정상**으로 온다:

| 경과 | 기준일 | 유가 | 코스닥 |  | 경과 | 기준일 | 유가 | 코스닥 |
|---|---|---|---|---|---|---|---|---|
| 4년 | 2022-08-01 | 938 | 1,575 |  | 7년 | 2019-08-02 | 900 | 1,359 |
| 5년 | 2021-07-30 | 929 | 1,508 |  | 9년 | 2017-08-02 | 882 | 1,239 |
| 6년 | 2020-07-31 | 908 | 1,425 |  | **11년** | **2015-08-03** | **883** | **1,093** |

★ **0 행이 나와도 보관 만료가 아니다** — 2024-02-12 가 0 행이었는데 설 연휴 대체공휴일이었다.
  (재무분석 때 7/17 을 같은 이유로 오판했던 것과 **똑같은 함정**이다. 판정하려면 <b>이틀 이상</b>을 찍어 본다.)
⇒ 과거 데이터에 시한이 없다. **얕게 시작해서 필요하면 나중에 더 내려가면 된다.**

---

## 2. 테이블 지도 (실측 2026-07-31)

### 2.1 일봉·시세 — 지금 쓰는 것

| 테이블 | 내용 | 규모 | 채우는 주체 | 읽는 곳 |
|---|---|---|---|---|
| **`pf_daily`** | 일봉 OHLCV `(stock_code,d,o,h,l,c,v)` · **수정주가** | 18,774행 · **14종목** · 2021-02~ | `cron/dart_collect.php job=daily` (네이버) | `Pf.class` · `stock/index.php` · `lib/calc.php` (지표·MFE/MAE) |
| **`krx_amt`** | ★**전종목 일별 원장**(2026-07-31 확장) `(code,d,amt,vol,o,h,l,c,mktcap,list_shrs,mkt,src)` · **원본주가** | 전종목 2024-08~ (+ 옛 24종목 2022-04~) | `cron/krx_amt.php` (아래 §3) | `stock_analysis_api.php action=daily` → 차트 지표 · **거래대금 급증 스크리너** |
| **`krx_daily`** | KRX 전종목 일별 (종가·OHLC·거래량·**거래대금**·시총·상장주식수) | 8,462행 · 2,765종목 · **최근 10일만**(prune) | `cron/dart_collect.php job=krx` (13:05) | `Dart.class`(PER 분모) · `Pf.class` · `Krx.class` |
| **`all_stock_info`** | 전종목 **현재가 스냅샷** (per·pbr·eps 포함) | 2,759행 · 갱신형(TRUNCATE) | `cron/keyword_collector.php mode=stock_etf_news` (평일 9회) | 단타 목록(top30) · 포트폴리오 시세 |
| **`stock_daily_range`** | 일별 **고·저만** `(stock_code,bas_dd,hi,lo)` | 332,593행 · 2,766종목 · 6개월 | `NaverFinanceAPI` (job=eod) | 6개월 고점·저점 스크리너 |
| **`stock_price_range`** | 위의 **6개월 집계 캐시** (hi/lo/날짜) | 2,766행 | 같은 크론 | 재무분석 스크리너 |
| **`pf_sim_data`** | 시뮬레이터용 일봉 **통째 JSON**(`rows_json`) | 20행(=20종목) · 종목당 10년 | 시뮬레이터 화면 「자료 등록」 | `stock/lib/sim.php` 백테스트 |

### 2.2 레거시 (건드리지 말 것 · 새로 쓰지 말 것)

| 테이블 | 내용 | 상태 |
|---|---|---|
| `analysis_stock` | 일봉 OHLCV 19종목·880일 | `prj_yehior.php` 전용. **pf_daily 와 성격 중복** — 새 기능은 pf_daily 를 쓴다 |
| `all_stock_history_info` | 연 3회 전종목 스냅샷 | `daily_news.php` 구화면 |
| `tbl_daily_stock_vol` | 급등주 분석 스냅샷(2023~2026-05) | `daily_news.php` · 갱신 중단 상태 |
| `tbl_stock_analysis*` · `tbl_daily_thema*` · `tbl_prj_*` | 옛 테마·분석 화면 | 2024년 이후 갱신 없음 |

---

## 3. 거래대금·일별 원장은 어떻게 흐르나 (2026-07-31 전종목 확장)

```
15:50  dart_eod 크론                                  ← 당일 (T+0 · 잠정)
        └→ ★ all_stock_info 스냅샷 → krx_amt 오늘 행 (src='n' · API 0회 · 실측 2,628종목)
13:05  dart_krx 크론                                  ← 다음날 (T+1 · 확정)
        ├→ KRX 오픈API 전종목 2회 호출 ─→ krx_daily 저장
        ├→ ★ 어제 잠정치와의 괴리를 로그로 (덮기 전에만 잴 수 있다)
        ├→ ★ krx_amt 로 <b>전종목</b> 이관 (src='k' 확정 · API 0회)
        └→ prune: krx_daily 오래된 기준일 삭제
```

**★★ 크론을 새로 등록하지 않았다** — 두 단계 모두 기존 크론(`dart_eod`·`dart_krx`)에 얹었다.
잠정→확정 덮어쓰기는 `stock_daily_range`(당일 1줄 + 다음날 보정)와 같은 검증된 패턴이다.

- 순서가 중요하다: **저장 → 괴리측정 → 이관 → prune**. 이관을 prune 뒤에 두면 지워진 뒤라 옮길 게 없다.
- ★ `src` 가드: KRX 확정값(src='k')이 있는 행은 잠정치가 **절대 덮지 않는다**(IF 가드).
- ★ 휴장일 차단 공짜: 잠정 적재는 `uDate=오늘` 인 행만 쓰므로 휴장일엔 저절로 0건.
- 소비처: 퀀트 「거래대금 신고가」(`stock/index.php?mode=quant`) · 차트 지표(`stock_analysis_api.php action=daily`)
- `cron/krx_amt.php job=daily` 는 **예비 경로**다 — dart_krx 가 실패했을 때 손으로 한 번 돌린다(크론 등록 불필요).
- 용량 실측: 전종목 2년 = 1,344,461행 · 231MB · 행당 180바이트 (하루 +2,765행 ≈ 연 125MB)

### 지표 엔진에서의 사용
`stock_analysis_api.php?action=daily` 가 네이버 일봉에 `a`(실제 거래대금)를 **있는 날만** 붙여 보낸다.
`style/dailychart.js` 의 `AMT`(=`거래대금`)는 **실제값 우선 · 없으면 종가×거래량 근사**.

**근사 오차 실측(7,949표본 · VWAP 대비 절대중앙)**
| 방식 | 오차 | 두 날 대소비교 뒤집힘 |
|---|---|---|
| 종가×거래량 (폴백) | 0.99% | 0.79% |
| (고+저)/2×거래량 | 0.89% | 0.42% |
| (고+저+종)/3×거래량 | 0.57% | 0.49% |
| **KRX 실제값** | 0% | 0% |

---

## 4. 새 데이터가 필요할 때 — 의사결정

1. **전종목의 오늘 시세?** → `all_stock_info` (이미 하루 9회 갱신). 새로 받지 말 것.
2. **특정 종목의 일봉 4년?** → 차트라면 API 가 요청마다 네이버에서 받는다(저장 불필요).
   지표·백테스트로 **반복 계산**한다면 `pf_daily` 에 종목을 추가한다(`cron/dart_collect.php job=daily`).
3. **거래대금?** → `krx_amt`. 없는 종목이면 §5 로 추가한다.
4. **고·저 6개월 통계?** → `stock_price_range`(집계) / `stock_daily_range`(원본).
5. **상장주식수·시총?** → `krx_daily` (최근 10일). 과거치는 `stock_financial.shares_out`.
6. **재무?** → `stock_financial` (DART).

**★ 저장하기 전에 물을 것**: 이 값이 위 6개 중 하나에 이미 있나? 있으면 **그 테이블을 읽는다**.
크론을 새로 만들기 전에 `CRON.md` 를 먼저 본다 — 기존 크론에 얹을 수 있는 경우가 많다.

---

## 5. 거래대금에 종목을 추가하려면

| 방법 | 명령 | 비용 | 정확도 |
|---|---|---|---|
| **① 공공데이터포털**(권장) | `php cron/krx_amt.php job=item code=005930 years=4` | **종목당 1회 호출·수초** | KRX 원천 = 공식 |
| ② KRX 일자별 훑기 | `php cron/krx_amt.php job=backfill from=20240801 all=1 sec=600` | **6초/거래일**(전종목 저장 포함) · 2년 ≈ 50분 | 공식 |
| ③ 그냥 두기 | — | 0 | 종가×거래량 근사(0.99%) |

### ★★ backfill 은 「구간」으로 준다 — 커서에 막혔던 이야기 (2026-07-31)

원래 `backfill` 의 이어받기 커서는 **「저장된 min(d) 의 하루 전」** 하나뿐이었다.
선별 24종목을 채울 때는 충분했지만, 나중에 **전종목 원장**을 채우려 하니 곧바로 막혔다 —
min(d) 가 이미 **2022-04-05**(옛 24종목분)이라 커서가 그보다 **더 과거로만** 내려가서,
정작 비어 있는 2024~2026 전종목 구간에는 **영영 들어가지 못한다.**

⇒ `from=`(가장 오래된 날) · `to=`(시작점, 기본 어제) 로 **구간을 직접 준다.**
  이어받기는 커서 대신 **「그 날에 이미 몇 종목이 있나」**(`min=`, 기본 500)로 판정한다 —
  저장된 사실 자체가 진행 상황이라 커서 파일이 필요 없고, 중단돼도 같은 명령을 다시 치면 이어진다.
★ 교훈 — **커서를 "저장된 값의 극단"에서 뽑으면, 그 테이블의 용도가 넓어지는 순간 못 쓰게 된다.**

**①은 `env/datago.inc` 에 `DATA_GO_KEY` 가 있어야 한다** (공공데이터포털 → 금융위원회_주식시세정보 →
활용신청(즉시승인) → 마이페이지 → **Decoding 키**). ⚠2026-08-02 까지 포털 시스템 점검이라 그 이후 발급 가능.

- 오늘 이후분은 크론이 자동으로 쌓으므로(§3), ①은 **과거 채우기 전용**이다.
- 단타의 「상위 종목」은 **대상 종목이 매일 바뀌어** 미리 채울 수 없다 → 근사 폴백으로 동작하며,
  전부 덮고 싶으면 `job=daily all=1` 로 전종목을 매일 쌓는다(연 60MB).

---

## 6. 알아 둘 함정

- **네이버 일봉 반환키는 `t/o/h/l/c/v`** (`time/open/…` 아님). 차트 API 응답도 같은 형식 + `a`(거래대금).
- **거래정지일**은 네이버가 `o/h/l/v = 0`, 종가만 유지해서 준다 → 지표에서 제외하지 않으면 가짜 급등락이 생긴다
  (`pf_bar_valid` · 차트 모듈은 종가 도지로 그린다).
- **krx_daily 는 T+1** — 다음 날 오전에야 올라온다(01:19 엔 없고 11:58 엔 있음, 실측). 그래서 13:05.
- **`likeSrtnCd` 는 부분일치** — 공공데이터포털에 `005380` 을 물으면 `00538005` 도 섞여 온다.
  `StockAmtGov` 가 `srtnCd` 완전일치로 거른다.
- **KRX 웹(data.krx.co.kr)의 통계 조회는 서버에서 막힌다**(`LOGOUT` 400). 종목별 시세추이를 그쪽으로 받으려 하지 말 것.
- **다음 금융**(`finance.daum.net`)은 종목별 거래대금을 주지만 **거래량 집계 기준이 KRX 와 달라**
  22.8%의 날이 1% 넘게 어긋난다(실측). 채택하지 않았다.
