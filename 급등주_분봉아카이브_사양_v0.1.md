# 급등주 분봉 아카이브 & 퀀트 분석 — 구현 사양 v0.1

> 작성 2026-08-05 · 기준 문서 「분봉(1분봉) 상세 수집 로직 — 한계 정리」(2026-08-05)
> **§11 실측 항목은 2026-08-05 전부 채웠다** — 아래 값은 추정이 아니라 서버 실측이다.

---

## 0. 한 줄 요약

최근 두 달간 **하루 10% 이상 오른 종목**을 `krx_amt` 에서 뽑아, 그 종목의 **급등일 전후 10거래일(D-4~D+5) 1분봉**을 키움에서 천천히 받아 **지워지지 않는 아카이브**(`qm_bar`)에 쌓고, 이벤트당 한 행짜리 피처 표(`qm_feat`)를 만들어 패턴을 분석한다.

3분봉은 **저장하지 않는다** — 조회 시 리샘플한다.

---

## 1. 범위 / 비범위

### 포함
- 새 표 4개 (`qm_event`, `qm_task`, `qm_bar`, `qm_feat`)
- 새 CLI/크론 `cron/qm_collect.php` (job: `probe`/`schema`/`events`/`work`/`verify`/`feat`/`status`)
- `Dt::resample()` 마지막 그룹 계산 **일반화** (§10-①)
- `Dt::UNITS` **[1,5,15] → [1,3]** 및 그에 딸린 화면·API·테스트 수정 (§10-②~④)
- `Kiwoom::minute()` 에 **`base_dt` 인자 추가** (§11-② 실측 결과)

### ⛔건드리지 않는 것 (회귀 사고 방지)
- `dt_min` / `dt_pool` / `dt_min_log` / `dt_token` **스키마와 데이터**
- `cron/dt_min.php` 의 5단계(수집→삭제→치유→분할→감시) 로직
- `stock/api.php` 의 `series` 동작 (unit 목록만 바뀐다)
- 갤러리·단타 미리보기가 쓰는 기존 `fetchMinute()` 경로
- 일봉 경로(`stock_analysis_api.php`)

`qm_*` 는 `dt_*` 와 **조인하지 않는다.** 종목코드 문자열만 공유한다.

---

## 2. 전제 / 제약

| 항목 | 값 | 이 사양에 준 영향 |
|---|---|---|
| SAPI | apache2handler · 응답 타임아웃 **30초** | `job=work` 는 **bg 자기호출**로 돈다 |
| 크론 예산 | 900초 | 차수당 예산 **600초** |
| 키움 | 1 req/s · **IP 화이트리스트** | ★HTTP는 **반드시 서버에서**. 로컬 PC 는 `8050` |
| 키움 분봉 보관 | 약 1년 | 두 달 수집은 안전 범위 |
| 거래일 정의 | `krx_amt` 에 행이 있는 날 | 새 휴장일 표를 만들지 않는다 |
| 종목코드 | 항상 6자리 저장 · `_AL`/`_NX` 는 요청 때만 | `Dt::cleanCode()` 그대로 |
| 거래량 기준 | **KRX 단독** (`KIWOOM_NO_SOR=true`) | 아카이브도 동일 |
| 봉 시각 | **시작(start)** | `Kiwoom::TS_BASE` 그대로 |

### ⛔소스는 키움 하나뿐 — 네이버 폴백 금지

`dt_min` 은 네이버를 폴백으로 쓰지만 **아카이브는 쓰지 않는다.** 이유 셋:
1. 네이버는 **가장 최근 거래일 하루치**만 준다. 두 달 전 구간엔 무용하다.
2. 폴백이 성공하면 `bars > 0` 이 되어 **목표 날짜가 안 채워졌는데도 성공으로 보인다**.
3. 한 종목 안에 소스가 섞이면 거래량 기준이 흔들린다.

키움이 실패하면 **그 (종목, 날짜)는 실패로 기록하고 `tries+1`** 한다. 대체하지 않는다.

---

## 3. 표본 정의

### 이벤트 조건
그날 종가가 **전 거래일 종가 대비 +10.00% 이상** & **거래대금 100억 이상**

### 제외 필터 — ✅2026-08-05 실측 (최근 40거래일 · 1차 통과 1,094건)

| 필터 | 판정 방법 | 실측 제외 |
|---|---|---|
| 거래대금 100억 미만 | `krx_amt.amt < 10,000,000,000` (★**원** 단위) | 1차 조건에 포함 |
| **우선주** | 종목코드 끝자리 ≠ `0` | **19건** |
| **스팩** | 종목명에 `스팩` 포함 | 0건 |
| **ETF** | `all_etf_info.etf_code` (473종목) | 0건 |
| **ETN** | 종목명에 `ETN` 포함 | 0건 |
| **관리종목** | ⛔**판정 불가** — 아래 참조 | **미적용** |
| **분할·병합일** | 이벤트일 `list_shrs` 가 전일 대비 ±20% 초과 | 1건 |
| **신규 상장** | 이벤트일 기준 `krx_amt` 행이 60거래일 미만 | 29건 |

⛔**관리종목 필터는 걸 수 없다.** `information_schema` 로 economist73 의 127개 표를 전수 조사했으나
`admin`/`mgmt`/`manage`/`warn`/`halt`/`susp`/`stat`/`flag` 어디에도 관리종목 지정 여부를 담은 컬럼이 없다.
**미적용으로 명시하고 진행한다** — 추측으로 걸러 표본을 조용히 왜곡시키지 않는다.

★**끝자리 `0` 규칙은 5번째 자리의 문자를 배제하지 않는다.** `0004V0`(엔비알모션)·`0009K0`(에임드바이오)·
`0126Z0`(삼성에피스홀딩스)는 전부 **보통주**다(신규상장·인적분할 코드). 실측으로 확인했고 남긴다.

### 수집 구간
이벤트일 **D 기준 D-4 ~ D+5 = 10거래일** (달력 아님, `krx_amt` 기준 거래일)
- 한 종목에 이벤트가 여러 번이면 **구간 합집합**을 취한다 (중복 수집 금지)
- D+5 가 아직 미도래면 그 날짜는 task 에 넣지 않는다. 지나고 `job=events` 를 다시 돌리면 채워진다

### ✅규모 실측 (2026-08-05 · 최근 40거래일)

| | |
|---|---|
| 이벤트 | **1,045건** |
| 유니크 종목 | **439개** |
| (종목,날짜) 과제 | **7,301개** (합집합으로 3,149개 절약) |
| 거래일당 봉 수 | **351봉** (`dt_min` 실측 · 정규장 381분이 상한) |
| `qm_bar` 행당 | **106바이트** (`dt_min` 실측) |
| **`qm_bar` 추정 용량** | **259 MB** |
| 키움 콜 | **약 4,260콜 ≈ 1.3시간** |

거래대금 분포(억): 최소 100 · 25% 175 · 중앙 **362** · 75% 1,017 · 최대 195,130

---

## 4. 데이터 모델

정본은 `cron/qm_collect.php` 의 `qm_ensure_tables()` 다. 아래는 요약이다.

- **`qm_event`** — 급등 이벤트 원장. `krx_amt` 에서 SQL 로 추출하므로 **API 콜 0**.
  `PRIMARY KEY (code, d)` · 이벤트일 `list_shrs` 스냅샷을 함께 담는다(사후 분할 감지용).
- **`qm_task`** — ★**(종목, 날짜) 단위**. 종목 단위로 만들지 않는다.
  `state` 0대기 1진행 2완료 9포기 · `lock_at` 은 stale 회수용.
- **`qm_bar`** — 1분봉 아카이브. `dt_min` 과 컬럼 동일 · ⛔**prune 대상 아님**.
  `fetched_at` 은 «어느 수정주가 기준으로 받았나»를 남긴다.
- **`qm_feat`** — 이벤트당 한 행. 분석은 이것만 읽는다.
  `q_bars_full` / `q_halted` / `q_split_after` 는 ⛔**제외하지 말고 남긴다**.

★**`q_bars_full` 은 «날짜가 다 있나»지 «봉이 375개 넘나»가 아니다.** 봉 수로 재면 거래가 얇은 종목이
통째로 「분석 불가」가 되어 표본이 조용히 대형주 쪽으로 기운다 — §9 가 금지한 생존편향과 같은 것이다.
(실측: 한화손해보험 2026-06-08 은 345봉인데 정상 거래일이다.) 얇음 자체는 `q_halted` 가 따로 신호한다.

---

## 5. 단계 A — 이벤트 추출 `job=events`

1. 최근 40거래일을 `krx_amt` 에서 구한다
2. 전 거래일 종가 대비 등락률 ≥10% & 거래대금 ≥100억 종목을 뽑는다
3. §3 제외 필터를 적용한다
4. `qm_event` UPSERT · 각 이벤트의 D-4~D+5 를 펼쳐 `qm_task` 에 **`INSERT IGNORE`**
5. 이벤트 수 / 유니크 종목 수 / 신규 task 수 / 필터별 제외 건수를 보고

**멱등해야 한다.** 두 번 돌려도 완료된 task 가 대기로 되돌아가지 않는다.

★`job=probe` 와 `job=events` 는 **같은 함수 `qm_scan_events()` 하나**를 본다 — 조사에서 본 숫자와
실제로 담기는 숫자가 갈리면 조사가 거짓말이 된다. (실측에서 둘 다 1,045건 / 7,301과제로 일치했다.)

★종목명은 `all_stock_info` 를 **SQL 조인하지 않고** 따로 읽어 붙인다 —
그 표는 `char(6) utf8mb3` 라 `krx_amt`(utf8mb4)와 조인하면 콜레이션이 어긋나 BNL 로 떨어진다(실측 15배).

---

## 6. 단계 B — 수집 워커 `job=work`

### 실행 형태
- **bg 자기호출**로 돈다 (`&bg=1`) · 차수 예산 **600초**
- `job=work n=1` — 디버깅용 1종목 실행

### 처리 단위 — ★여기가 핵심
**지시는 종목 단위, 판정은 날짜 단위.**

```
1. state=1 이면서 lock_at < now-30min 인 행을 state=0 으로 회수한다 (stale)
2. state=0 인 task 중 가장 오래된 날짜의 종목 하나를 고른다
3. 그 종목의 state=0 task 를 전부 집어 state=1, lock_at=now 로 «원자적으로» 마킹한다
   (UPDATE … WHERE code=? AND state=0 의 rowCount 가 0 이면 남이 먼저 잡은 것 — 다시 고른다)
4. 그 종목의 min(d) ~ max(d) 를 한 번에 키움에서 받는다 (base_dt=max(d) 로 «그 구간에 바로 뛴다»)
5. 받은 봉을 Dt::normalize() → Dt::byDay() 로 날짜별로 나눈다
6. ★지시된 날짜 각각에 대해:
     - 봉이 1개 이상 → state=2, bars 기록 (375 미만이면 err='partial')
     - 봉이 0        → tries+1, state=0 (tries>=5 면 state=9)
7. 종목 단위로 예외가 났으면 지시된 날짜 전부 tries+1, state=0 으로 되돌린다
```

6번이 **성공 오판 방지**다. 종목 단위로 성공을 판정하면 빈 날짜 구멍이 영원히 안 메워지면서 `tries` 도 안 오른다.

`partial` 판정 주의: 작은 종목은 원래 봉이 적다. **봉 시각이 적다는 이유로 재시도하지 마라** — 0봉만 실패다.

### ★저장은 «과제 날짜만»
한 종목에 이벤트가 여럿이면 `min(d)~max(d)` 사이에 **과제가 아닌 날**이 딸려 온다.
그것까지 저장하면 용량이 추정을 훌쩍 넘는다 → 저장 직전에 `qm_task` 에 있는 날짜만 남긴다.
(실측 검산: `bars_outside_tasks = 0`)

### 키움 호출 규칙
- `ka10080` · `tic_scope="1"` · `upd_stkpc_tp="1"` · **`TR_MAP` 화이트리스트 유지**
- ★**`base_dt` 를 반드시 넣는다** — §11-② 참조. 없으면 콜이 네 배 든다
- 콜 사이 **1초**. 종목 사이에도 1초
- 연속조회 중단 조건: `cont-yn≠Y` · `next-key` 없음 · 가장 오래된 봉 날짜 < 목표 min(d)
- 콜 상한 **MAX_CALLS=35/종목**. 초과하면 그 종목 중단하고 다음 차수로 넘긴다
- `429` → 지수 백오프 2회 · `401` → 토큰 재발급 후 1회 재시도 · **HTTP 200 + `return_code≠0` 도 실패**

### ⛔부분 재수집 금지
`upd_stkpc_tp=1` 은 **받는 시점 기준 수정주가**다. 나중에 일부 날짜만 다시 받으면 한 종목 안에
기준이 다른 봉이 섞인다 → 다시 받을 때는 **그 종목의 `qm_bar` 를 통째로 지우고 구간 전체를 다시 받는다.**

### 용량 가드
차수 시작 시 `qm_bar` 크기를 조회한다. **`QM_MAX_MB`(=300) 를 넘으면 수집을 중단**한다.
조용히 계속 쓰면 **같은 스키마의 다른 126개 표가 같이 죽는다.**

### 토큰 동시 발급
`dt_token` 을 **공유**한다. `qm_` 용 별도 토큰 표를 만들지 않는다.

---

## 7. 단계 C — 검증 `job=verify`

| 검사 | 기대 |
|---|---|
| `state=0` 잔여 | 0 |
| `state=1` 잔여 | 0 (있으면 stale 회수 실패) |
| `state=9` 목록 | 사람이 눈으로 확인. 상장폐지·거래정지면 정상 |
| 이벤트일 봉 존재율 | 이벤트일 D 에 봉이 0인 이벤트는 **분석 불가** → `q_bars_full=0` |
| 이벤트일 고가 검산 | `max(qm_bar.h)` ≥ `qm_event.close_prc` |
| 종가 재검산 | 분봉 15:30 종가 == `krx_amt` 종가. **불일치면 분할 의심** |
| 표본 크기 | 이벤트 수 · 유니크 종목 수 · 총 행 수 · 테이블 용량 |

✅실측(000370 한화손해보험 10일): **10/10 날짜 전부 종가 오차 0.000%**.

---

## 8. 단계 D — 피처 계산 `job=feat`

DB만 읽는다. API 콜 0. 멱등(재실행 시 덮어쓰기).

- VWAP 는 `Σ(종가×거래량)/Σ거래량` (분봉에 거래대금이 없으므로 **근사**임을 주석으로 남긴다)
- `f_hit10_hm` — 그 분의 고가가 `prev_prc × 1.10` 이상이 된 첫 봉의 시각
- `f_rebreak_n` — 10% 위로 올라갔다가 종가가 아래로 이탈한 뒤 다시 올라간 횟수
- ⛔**봉이 없는 구간을 0으로 채우지 않는다.** NULL 로 두고 `q_bars_full=0`

✅실측 교차검증(000370 · 2026-06-09 +15.38%):
`f_close_vs_high −1.98`(6,450/6,580−1) · `f_nd_high_ret 5.12`(06-10 고가 6,780) ·
`f_d5_ret 10.85`(06-16 종가 7,150) · `f_post_mdd −6.82`(06-11 저가 6,010) — 전부 `krx_amt` 일봉과 일치.

---

## 9. 단계 E — 분석 ✅완료 (`job=analyze` · 2026-08-05)

`qm_feat` 만 읽어 검정한다. 재현은 `php cron/qm_collect.php job=analyze`.

### ★★먼저 — 「D+5」가 아직 D+5 가 아닌 건이 있다

마지막 이벤트가 **어제(2026-08-04)** 라 사후 5거래일이 **아직 오지 않은** 이벤트가 있다.
`job=events` 는 미도래 날짜를 task 에 넣지 않으므로 그 건의 `f_d5_ret` 은 **1~4거래일치인데 이름만 D+5** 다.

| 사후 거래일(`n_post`) | 0 | 1 | 2 | 3 | 4 | **5** |
|---|---|---|---|---|---|---|
| 이벤트 | 54 | 24 | 116 | 11 | 7 | **833** |

**212건(20.3%)이 잘려 있다.** 사전(D-4)은 1,045건 전부 온전하다.
→ 그래서 분석은 **세 집합을 나란히** 찍는다. 잘린 창은 손실을 덜 담아 **성과를 부풀린다**(아래 참조).
→ 2026-08-11 이후 `job=events` → `job=work` 를 다시 돌리면 채워진다.

### ✅결과 — 가설 넷 다 «주장할 수 없다»

| 가설 | 전체 1,045 | 사후완전 833 | 사후완전+정제 758 | 판정 |
|---|---|---|---|---|
| 1. 상단 마감이면 익일 시가가 좋다 | t=0.72 | **t=2.26** | t=1.94 | **부분집합마다 뒤집힌다** |
| 2. +10% 도달이 이를수록 후행이 좋다 | t=1.55 | t=0.54 | t=0.99 | **기각** — 단조성이 사라진다 |
| 3. 사전 거래량 배수가 클수록 나쁘다 | t=1.90 | t=0.65 | t=0.61 | **기각** — 단조성이 사라진다 |
| 4. 이른 몰림이 클수록 사후 낙폭이 크다 | t=0.92 | **t=2.65** | t=1.96 | **부분집합마다 뒤집힌다** |

★**넷 다 |t|가 2 언저리에서 부분집합에 따라 넘나든다.** 어느 것도 견고하지 않다.
특히 가설2·3 의 「단조 경향」은 **전체본에서만 보였고, 사후창을 온전한 것으로 맞추자 사라졌다**
(09시대 −5.16%→−8.62% 로 내려앉으며 뒤 구간과 나란해진다). **잘린 창이 만든 착시였다.**

### ★가장 견고한 사실 — 급등 «다음»은 평균적으로 음(−)이고, 창이 온전할수록 더 나쁘다

| 결과변수 | 전체 1,045 | 사후완전 833 | 사후완전+정제 758 |
|---|---|---|---|
| 익일 **시가** | +1.00% (양 45.6%) | +1.23% (양 49.1%) | +0.78% (양 46.4%) |
| 익일 종가 | −1.71% (양 35.3%) | −2.42% (양 30.8%) | −2.80% (양 29.6%) |
| **D+5 종가** | −6.13% (양 33.2%) | **−8.72%** (양 **24.1%**) | **−9.36%** (양 24.1%) |
| 사후 최대낙폭 | −15.43% | −17.49% | −17.53% |

세 집합에서 **부호와 크기의 순서가 한결같다**. 익일 시가만 0 근처이고 그 뒤로는 계속 음(−)이다.

### ⛔결과를 읽을 때의 한계 (반드시 함께 말한다)

1. **한 국면 40거래일뿐이다**(2026-06-09 ~ 08-04). 이 구간은 SK하이닉스 +29.95%·삼성전자 +26.81%(둘 다 07-31)가
   나온 이례적 장이다. **다른 국면으로 일반화할 수 없다.** 8년치 검정이 아니다.
2. **★사후창이 20% 잘려 있다**(위 표). 잘린 건은 손실을 덜 담아 성과를 부풀린다 —
   실제로 D+5 가 −6.13% → −8.72% 로 내려앉고 가설2·3의 단조성이 사라졌다.
   **시간이 지나야 채워진다**(데이터가 모자란 것이 아니다).
3. **`f_post_mdd` 는 사실상 축퇴돼 있다** — 양(+)인 경우가 2~3%뿐이다.
   5거래일 중 «어느 한 시점의 저가»는 거의 언제나 종가보다 낮아서, 이 변수는 무엇과도 잘 갈리지 않는다.
4. **관리종목 필터가 미적용**이다(§3). 관리종목이 표본에 섞여 있다.
5. **거래비용·슬리피지·호가 공백이 반영돼 있지 않다.** 「익일 시가 +1.23%」는 그 시가에 실제로 살 수 있다는 뜻이 아니다.
6. **네 가설 다 |t|가 2 언저리에서 부분집합에 따라 넘나든다.** 표본을 조금만 바꿔도 결론이 뒤집히는 크기다 —
   어느 것도 「찾았다」고 말할 수 없다.

### ⛔분석 규율 (코드가 이걸 지킨다)
- **생존편향 금지** — 거래정지·기준 어긋남을 조용히 빼지 않는다. `job=analyze` 는 **전체본과 정제본을 나란히** 찍는다
- **표본 수를 항상 함께 적는다.** 구간 n이 30 미만이면 줄 끝에 `←n<30 결론 보류` 가 붙는다
- **없는 숫자를 만들지 않는다.** 각 가설 머리에 근거 컬럼과 필터 조건을 함께 출력한다
- **투자 의견을 쓰지 않는다.** 통계적 사실만 보고한다

### ⛔분석 규율
- **생존편향 금지** — 거래정지·상폐 종목을 조용히 빼지 않는다. `q_halted=1` 로 남기고,
  포함/제외 두 경우의 결과를 **둘 다** 보고한다
- **표본 수를 항상 함께 적는다.** 구간별 n 이 30 미만이면 결론을 내지 않는다
- **없는 숫자를 만들지 않는다.** 계산 근거가 된 컬럼과 필터 조건을 결과와 함께 출력한다
- **투자 의견을 쓰지 않는다.** 통계적 사실만 보고한다

---

## 9-B. ★8년 일봉 검증 (`qm_dday` · `job=dbuild` → `job=danalyze`) ✅2026-08-05

분봉 아카이브는 **키움이 1년만 보관**해서 구조적으로 한 국면에 갇힌다. 같은 이벤트 정의를
`krx_amt`(2019~) 에 던지면 코로나 폭락·2022 약세장까지 넘나드는 표본이 나온다. **API 콜 0.**

⛔`qm_event`/`qm_task` 에 넣지 않는다 — 거기 4만 건을 넣으면 `job=work` 가 18만 과제의
**분봉**을 받으러 간다(4.4GB·며칠). `qm_dday` 는 분봉과 무관한 별도 표다(8.5MB).

**표본 39,339건 / 2,917종목 / 2019~2026 (연도별 2,061~5,759건 고르게)**

★**상장폐지 종목을 빼지 않았다.** 이름이 `all_stock_info` 에 없는 359종목(1,812건)은 대부분 상장폐지인데,
빼면 「끝이 나쁜 것」만 사라져 결과가 좋은 쪽으로 기운다. `q_noname=1` 로 표시만 한다.
(실측: 상장폐지 종목만 보면 D+5 **−2.66%** vs 전체 −0.85% — 확실히 더 나쁘다.)

### A. 국면별 일관성 — 이 표가 답이다

| 연도 | n | 익일 시가 | 익일 종가 | D+5 | D+20 | D+5 양(+) |
|---|---|---|---|---|---|---|
| 2019 | 2,061 | **+0.99%** | −0.28% | −2.09% | −3.84% | 34.4% |
| 2020 | 5,759 | **+1.31%** | +0.20% | **+0.74%** | +3.83% | 44.7% |
| 2021 | 5,362 | **+0.96%** | +0.20% | −0.25% | −0.75% | 39.2% |
| 2022 | 3,859 | **+0.64%** | −0.35% | −2.25% | −5.53% | 34.0% |
| 2023 | 4,976 | **+0.99%** | −0.22% | −0.94% | −1.53% | 38.4% |
| 2024 | 4,695 | **+0.75%** | −0.29% | −1.34% | −4.53% | 38.3% |
| 2025 | 4,777 | **+1.10%** | +0.18% | −0.46% | −0.72% | 40.1% |
| 2026 | 5,263 | **+1.28%** | −0.13% | −1.59% | −3.82% | 39.3% |

- **익일 시가는 8년 전부 양(+)** (+0.64 ~ +1.31%) — 국면을 넘어 재현된다
- **D+5 는 7/8년 음(−)** (2020 코로나 V자 반등만 예외) · 양(+) 비율은 **한 해도 50%를 못 넘는다**
- 40거래일 분봉 표본에서 본 모양과 **같다**. 그 결과가 국면 탓이 아니었음이 확인됐다.

### B. ★가설1은 8년에서 압도적으로 확인됐다

`f_close_vs_high ≥ −3%`(상단 마감) → 익일 시가:

| | 2019 | 2020 | 2021 | 2022 | 2023 | 2024 | 2025 | 2026 | **8년 전체** |
|---|---|---|---|---|---|---|---|---|---|
| 차이(%p) | +2.44 | +1.86 | +2.07 | +1.74 | +1.79 | +1.58 | +1.81 | +1.23 | **+1.78** |
| Welch t | 10.82 | 13.02 | 14.80 | 10.41 | 12.68 | 11.43 | 12.72 | 8.44 | **33.54** |

**8개 연도 전부 t>8. 단 한 해도 예외가 없다.**
분봉 표본 1,045건에서 t=2.26 으로 흔들리던 것이 39,339건에서는 확실해졌다 — **표본 부족이 원인이었다.**
절대값으로는 상단 마감 ≈ **+2.01%** · 그밖 ≈ +0.23%.

### C. 가설3은 기각됐다
사전 거래량 배수와 D+5 는 **관계 없다**(양 끝 t=**0.49** · 단조도 아니다).
40일 표본에서 보였던 단조 경향은 **잘린 사후창이 만든 착시**였음이 확인됐다.

### D. ★분봉이 답해야 할 질문이 뚜렷해졌다
- 사후 **최대상승 +14.29%**(양 **91.7%**) vs **최대낙폭 −9.83%**(양 5.8%) vs D+5 종가 −0.85%
  → 5일 안에 오르는 순간은 거의 언제나 온다. **종가까지 들고 있으면 사라진다.**
  「언제 파나」가 핵심이고, 그건 일봉으론 못 본다.
- 익일 시가 +1%는 **갭**이다. 그 시가에 실제로 체결되는지는 분봉만 답한다.
- ⛔거래비용·슬리피지는 여전히 미반영이다. **+1.78%p 는 매매 규칙이 아니라 통계적 사실이다.**

---

## 10. 기존 코드 변경 — `UNITS [1,5,15] → [1,3]` ✅완료

### ① `Dt::resample()` 마지막 그룹 일반화
```php
// 기존: if ($gm > 915) $gm = 915;        ← 5·15분에서만 맞는 값
$lastGm = 540 + intdiv(379, $unit) * $unit;
if ($gm > $lastGm) $gm = $lastGm;
```
unit별 결과: 1→919 · 3→918 · 5→915 · 15→915.
**5·15분 결과는 기존과 동일하므로 회귀가 없다.** 3분만 새로 맞는다.
이 줄이 없으면 `unit=3` 에서 15:18·15:19 는 15:18 그룹인데 15:30 봉만 15:15 로 떨어져 **라벨이 역행**한다.

### ② 검증 기준값 ✅
하루 381봉 → **1분 381 · 3분 127 · 5분 76 · 15분 26**, 거래량 합은 넷 다 동일.

### ③ `stock/api.php` — 조용히 빈 응답 주의 ✅
`in_array($unit, Dt::UNITS)` 실패 시 `$out([])` 다. `unit` 을 보내는 곳은 `subUnitOf()` 하나뿐임을
grep 으로 확인하고 함께 고쳤다.

### ④ `stock/index.php` `mode=short` ✅
보조 차트 5분 → **3분**. 메인 1분은 그대로.

### ⑤ `stock/tests/dt_test.php` ✅
3분 케이스로 교체 + **`$lastGm` 일반화가 기존 5·15분 기대값을 그대로 통과하는지 확인하는 회귀 방어**를 남겼다.
**26건 → 37건 전부 통과** · `calc_test` 655/0 무회귀.

---

## 11. ✅실측 표 (2026-08-05 · 전부 채움)

### ① DB 여유 용량

| | |
|---|---|
| economist73 **전체** | **1,031.2 MB** |
| 그중 `krx_amt` | 643.6 MB (62%) · `nw_contract_image` 103 · `place` 62.5 |
| `qm_bar` 추정 | **259 MB** |
| 적재 후 합계 | 약 **1,290 MB** |

→ 사용자 확인 결과 **여유 있음**. 거래대금 하한 100억 그대로 진행.
→ 가드 `QM_MAX_MB = 300` (추정 259MB + 15%).

축소가 필요해질 때의 순서(쓰지 않았다):
1. 거래대금 하한 100억 → 300억 (이벤트 596건 = 57%)
2. 1분봉은 D-1~D+2만, 나머지 6일은 3분봉
3. 분석 종료 후 `qm_bar` 를 CSV 로 덤프하고 DB 에는 `qm_feat` 만 남긴다

### ② `ka10080` 기준일 파라미터 — **있다: `base_dt`**

```
① 인자 없음   → 900봉 · 2026-07-31 15:16 ~ 2026-08-05 11:09 · cont-yn=Y   (늘 «오늘»부터)
② base_dt=20260623 → 900봉 · 2026-06-19 12:36 ~ 2026-06-23 15:35   ★먹는다
   base_dtm / strt_dt / qry_dt / end_dt → 전부 무시 (①과 같은 구간)
③ 인자 없이 2026-06-01 까지 되짚기 → 20콜 · 17,062봉 · 19.9초
```

**`base_dt=YYYYMMDD` 는 «그 날 마감까지의 900봉»을 준다.** 과거 구간을 받을 때 반드시 넣는다 —
없으면 언제나 오늘부터 되짚어야 해서 콜이 **네 배** 든다. `Kiwoom::minute()` 의 5번째 인자로 넣었다.

실측 결과: 10거래일 구간 = **5콜 · 5.4초** (000370). 이벤트가 여럿이라 구간이 40거래일이면 19콜.

### ③ ETF·관리종목 판정법
- **ETF** — `all_etf_info`(473종목) 재사용 **가능**. 다만 실측 표본에서 제외 건수는 **0건**이었다
  (거래대금 100억 + 등락률 10% 조건을 ETF 가 거의 통과하지 못한다).
- **ETN** — 종목명 `ETN` 포함. 실측 0건.
- **관리종목** — ⛔**판정 불가.** §3 참조. **미적용으로 명시**하고 진행했다.

---

## 12. 실행 대본

```
[0] 조사   php cron/qm_collect.php job=probe        ✅완료 (§11)
[1] 스키마 php cron/qm_collect.php job=schema       ✅완료
[2] 선행   Dt::resample() 일반화 → dt_test 통과     ✅완료 (37/0 · calc 655/0)
[3] 추출   php cron/qm_collect.php job=events       ✅완료 (1,045건 · 7,301과제)
[4] 수집   php cron/qm_collect.php job=work         ✅완료 (2,666,638봉 · 177.8MB)
[5] 검증   php cron/qm_collect.php job=verify       ✅전 항목 통과
[6] 피처   php cron/qm_collect.php job=feat         ✅1,045건
[7] 분석   php cron/qm_collect.php job=analyze      ✅완료 (§9)

     job=diag  code=…   한 종목이 왜 「봉 0」 이었나를 실호출로 캔다
     job=reset          검증이 잡아낸 손상·오판 과제를 대기로 되돌린다
```

### ★수집 중에 드러난 함정 넷 (전부 실측으로 값을 치렀다)

1. **「종목 통째 삭제」가 이미 끝난 날짜의 봉을 지웠다**(179건). 삭제는 종목 단위인데 잠금은 대기 날짜뿐이라,
   지운 뒤 다시 넣은 것이 대기분뿐이었다 → **`state=2` 인 채 봉만 사라졌다.**
   고침: 종목을 잡을 때 **`state=2` 도 함께 잠근다**(구간 전체를 한 기준으로 다시 쓴다).
2. **빈 응답을 「그 날짜엔 봉이 없다」로 오판했다**(124건). 키움은 장애 때 예외 없이 **HTTP 200 + 빈 목록**
   (`return_code=0`)을 준다. 고침: **하나도 못 받았으면 실패**(재시도), **일부만 비면 진짜**(거래정지).
   가르는 자는 **`krx_amt.vol`** — 그 날 거래가 있었으면 분봉도 있어야 한다.
3. **실패한 종목이 곧바로 다시 뽑혀 5연속으로 죽었다.** 선택이 `ORDER BY d` 라 실패 종목이 늘 가장 오래된
   날짜를 쥐고 있었다. 고침: **`ORDER BY tries, d`** — 실패한 것은 뒤로 보낸다.
4. **INT UNSIGNED 끼리 빼면 음수에서 터진다**(SQLSTATE 22003). `CAST(… AS SIGNED)` 로 뺀다.

크론 등록(선택 · 수집이 끝나면 **내린다**):
```
cron_job.php?task=qm&k=econ-cron-j7k2&job=work     '*/30 17-23,0-7 * * 1-5'
```
★`TASKS['qm']` 은 `job` 을 레지스트리에 넣지 않았다 — 그래야 `&job=` 으로 골라 부를 수 있다
(레지스트리 값은 사용자 인자보다 **우선**한다).
과제가 (종목, 날짜) 단위라 크론과 수동 실행이 겹쳐도 안전하다. **단 `state=1` 마킹은 원자적으로** 한다.

★SSH 에서 돌릴 때 `php` 는 PATH 에 없다 — **`/usr/local/php84/bin/php`** 를 쓴다.

---

## 13. 완료 판정

- `qm_task` 에 `state IN (0,1)` 이 **0건**
- `job=verify` 전 항목 통과
- `qm_feat` 행 수 == `qm_event` 행 수
- `qm_bar` 용량이 `QM_MAX_MB` 이내
- `dt_test.php` **전건 통과** (기존 26건 + 3분 케이스 + 5·15분 회귀 방어 = 37건)
- `dt_min` 행 수와 `dt_pool` 내용이 작업 전후로 **변하지 않았음**

---

## 14. 절대 하지 말 것

1. `qm_*` 데이터를 `dt_min` 에 넣지 않는다 → **prune 이 집어삼킨다**
2. `qm_bar` 에 prune·만료 삭제를 붙이지 않는다 → 아카이브는 불변이다
3. 아카이브에 **네이버 폴백을 쓰지 않는다** (§2)
4. 봉이 없는 분을 **0으로 채우지 않는다** → 없던 봉이 생기면 지표가 거짓말을 한다
5. **부분 재수집을 하지 않는다** → 종목 단위로 지우고 다시 받는다 (§6)
6. 성공/실패를 **종목 단위로 판정하지 않는다** → 날짜별로 판정한다 (§6)
7. **주문 API를 부르지 않는다** → `TR_MAP` 화이트리스트 밖은 예외
8. `_AL`/`_NX` 코드로 저장하지 않는다 → 항상 6자리
9. 키움 호출을 **로컬 PC에서 하지 않는다** → IP 화이트리스트
10. 분봉과 일봉을 한 시계열로 합치지 않는다
11. **`q_bars_full` 을 봉 수로 판정하지 않는다** → 날짜 기준 (§4)
12. **과제가 아닌 날짜를 저장하지 않는다** → 용량이 추정을 넘는다 (§6)
13. **★분봉 가격을 `krx_amt` 가격과 견주지 않는다.** 분봉은 `upd_stkpc_tp=1` 이라 **지금 기준 수정주가**이고
    `krx_amt` 는 **그 날 있던 값**이다. 이벤트 뒤 무상증자·액면분할이 있으면 상수배로 어긋난다
    (실측 가온전선 ×1.7995 · RF머트리얼즈 ×1.9960 · 1,045건 중 **57건**).
    기준가는 **직전 거래일의 마지막 분봉 종가**에서 얻는다 — 그래야 시가갭·10% 도달·재돌파·사전수익률이
    한 기준 안에 머문다. 어긋난 건은 `q_px_basis=1` 로 **표시하고 남긴다**(빼지 않는다).
14. **「봉 0」 을 곧바로 「그 날 봉이 없다」로 적지 않는다** → §12 함정 2
