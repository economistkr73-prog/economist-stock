# PER·PBR 밴드 차트 — 설계·구현 기록 (2026-08-03 · ✅배포)

재무분석 종목상세(`/stock/index.php?mode=fund&code=…`) 맨 아래에 **밴드 차트**.
FnGuide 기업정보의 Band Chart 와 같은 그림이되, **재료도 계산도 이 레포 안에서 끝난다**(외부 API 0회).

> 이 문서는 처음 설계안 그대로가 아니다 — **실측으로 틀린 것을 고친 결과**가 반영돼 있다.
> 뒤집힌 항목은 §9 에 따로 모았다.

---

## 0. 한 줄 요약

> 과거 **시가총액** 시계열 위에, **이익·자본 × 배수** 계단선 다섯 개를 얹는다.
> 배수는 그 종목 과거 밸류에이션의 분위수이고, **선이 꺾이는 날은 결산기가 아니라 공시일**이다.

---

## 1. 파일

| 파일 | 역할 |
|---|---|
| `stock/lib/band.php` | **계산 단일본** — `pf_band_series($pdo,$code,$years)` · `pf_band_filings()` · `pf_band_quantile()` |
| `stock_analysis_api.php` | `module=band&action=series` — 창구일 뿐 (계산 없음) |
| `style/bandchart.js` | 그림. `DailyChart.create()/addLine()` 위의 **얇은 층** (`?v=1`) |
| `stock/index.php` `pf_fund_detail_band()` | 종목상세 카드 |
| `chart_gallery.php` | 구성 ⑥ 「밴드형」 |

**`style/dailychart.js` 는 한 글자도 고치지 않았다** → 캐시버스트(`?v=`) 불필요, 다른 화면 무영향.
`addLine()` 이 이미 ⒜계단선(`stepped`) ⒝`null` 끊김 ⒞가격축 참여를 다 해 준다. 밴드에 필요한 건 그게 전부였다.

---

## 2. ★★ 설계의 중심 — 주당(원)이 아니라 **시가총액**

밴드를 「원」으로 그리려면 과거 5년치 **수정주가**가 필요한데 우리에겐 없다. 그래서 내부 단위를 시총으로 둔다.

```
PER = 시총 ÷ 순이익(TTM)          밴드선(시총) = 순이익(TTM) × 배수
PBR = 시총 ÷ 자본총계             밴드선(시총) = 자본총계   × 배수
```

**주식수가 식에서 사라진다.**

| | 주당(원) 방식 | 시총 방식 |
|---|---|---|
| 과거 주가 | 수정주가를 만들어야 함 | `krx_amt.mktcap` 그대로 |
| EPS 분모 | 「지금 상장주식수」로 통일 | **분모 자체가 없음** |
| 액면분할 | 보정 로직 필요 | **자동으로 무해** |

화면에 「원」으로 보일 때만 `÷ 지금 상장주식수` — **계산이 아니라 단위 환산**이라 밴드와 주가선이 같은 비율로 움직여 그림이 안 깨진다.

### ★ 결정적 검증 — 대한제분 001130

`krx_amt.list_shrs` 이력을 훑어 **5년 안에 상장주식수가 바뀐 날이 딱 하루**임을 찾았다: **2026-05-18, 1,690,000 → 16,900,000 (10:1)**.

| 날짜 | 주가 | 시총 | 주당 환산 |
|---|---|---|---|
| 2026-05-15 | 143,000 | 2,645억 | 15,650원 |
| **2026-05-18 (분할일)** | **14,300** | **2,417억** | **14,300원** |
| 2026-05-22 | — | 2,160억 | 12,780원 |

주가는 1/10 로 **절벽**이 생기지만 시총은 −8.6% 로 매끄럽게 이어진다(그날의 실제 하락분).
⇒ 주당 방식이었다면 밴드가 여기서 통째로 깨졌을 것이다.

> ⚠ memory `session_재무분석.md` 의 「대한제분 **2024년** 10:1 액면분할」은 **시점이 틀렸다**. 실제는 2026-05-18.
> (주식수 16,900,000 은 맞다. PER 분모 함정의 교훈 자체는 그대로 유효하다.)

---

## 3. ★★★ 계단이 꺾이는 날은 **공시일**

남들은 밴드를 결산기에 맞춰 매끄럽게 잇는다. 그러면 3월에 나온 사업보고서의 이익이 전년 12월 자리에 이미 반영되어 **선견 편향**이 된다.

이 레포는 그 문제를 이미 풀었다 — 종목상세의 「공시일」 칸과 차트의 ▲▼ SUE 마커가 모두 `dart_rcept` 접수일(원본 `MIN`)을 본다. **밴드도 같은 날 꺾인다.**

**실측 일치** (공시일 → 계단 전환일 = 다음 거래일):

| 종목 | 분기 | 공시(접수일) | 계단 |
|---|---|---|---|
| 삼성전자 | 26.1Q | 2026-05-15 | **2026-05-18** |
| 삼성전자 | 25.4Q | 2026-03-10 | **2026-03-11** |
| SK하이닉스 | 25.4Q | 2026-03-17 | **2026-03-18** |
| 조광피혁 | 24.4Q | 2025-03-20 | **2025-03-21** |

★ 주 단위로만 줄이면 전환일이 그 주 금요일까지 **최대 4일 밀린다**(실측: 삼성 26.1Q 가 5/22 에 섰다).
그래서 주봉 축약 후 **계단 전환일만은 정확한 날짜로 다시 끼워 넣는다**(`px` 262점 → 279점).

---

## 4. 재료 — 전부 이미 있었다 (P0 실측)

| 필요한 것 | 어디 | 실측 결과 |
|---|---|---|
| 과거 일별 시가총액 | `krx_amt.mktcap` | **2019-01-02~2026-08-03 · 4,716,321행 · 3,137종목 · 결측 0건** |
| 분기 누적 재무 | `stock_financial` | 2016~ · `Dart::quarterly()` → TTM |
| 공시 접수일 | `dart_rcept` | 100,994행 · 2,639종목 · 2016-01-06~ |
| 지금 상장주식수 | `krx_amt.list_shrs` | `krx_daily.list_shrs` 와 **표본 5/5 일치** |

연도별 `mktcap` NULL 이 **전 구간 0**이라 설계 전제가 그대로 통과했다.
(2026년 `list_shrs` 결측 2,623행은 당일 네이버 잠정행(`src='n'`)이고 `mktcap` 은 있으므로 밴드에 무해.)

---

## 5. 계산 (`pf_band_series`)

```
① 시총 시계열   krx_amt (code, d ≥ 오늘−N년, mktcap>0)
② 분기 재무     stock_financial → Dart::quarterly() → 당분기
                TTM 순이익 = 그 분기 포함 4개 합   ·   자본총계 = 그 분기말 시점값(더하지 않음)
③ 계단          각 분기 TTM 을 「그 분기 보고서 접수일 다음 거래일」에 세운다
④ 배수          per_d = mktcap_d ÷ 그날 유효한 TTM  →  10·30·50·70·90% 분위수
⑤ 표시          주봉 축약(+계단 전환일) · 화면에서 ÷ 상장주식수
```

**저장하지 않는다** — 비율 무저장 원칙 그대로. 재무·시세가 갱신되면 밴드가 저절로 따라온다.

**성능**: 종목당 **6~13ms** · JSON **27~32KB** · 종목상세 렌더 **89ms**(밴드는 비동기라 서버 렌더에 부담 없음).

---

## 6. ★★ 값이 없는 이유는 셋이고, 셋 다 다르게 말한다

`gap = {early, stale, neg}` 로 세어 **가장 많은 사유 하나**를 화면에 적는다.

| 사유 | 뜻 | 화면 문구 |
|---|---|---|
| `early` | 그 이전에 분기 재무가 없다 | 「그 이전에는 분기 재무가 없습니다」 |
| `stale` | 계단이 400일 넘게 안 바뀜 = **수집 구멍** | 「오래 갱신되지 않았습니다 (DART 수집 구멍)」 |
| `neg` | 값이 0 이하 | 「0 이하입니다 (적자·자본잠식)」 |

### ★★★ `stale` 가드가 없었으면 조용히 거짓말을 할 뻔했다

현대차 005380 은 `stock_financial.net_income` 이 **2018.3Q~2023.4Q 통째로 NULL**이다(매출·영업이익은 정상).
가드 없이 두면 **2018년 2분기의 이익이 2024년 말까지 6년간 유효한 값인 척** 이어져 그 구간 PER 밴드가 완전한 허구가 된다.
**화면에는 아무 이상이 없어 보인다 — 그래서 더 나쁘다.**

⇒ `PF_BAND_MAX_AGE = 400일`. 넘으면 선을 **끊는다**. 현대차 PER 은 cover 100% → **50%** 로 떨어져 밴드가 꺼졌다(정직해짐).

### ⊕ 곁들여 드러난 별개 문제 — DART 순이익 결측

밴드와 무관하게 **스크리너의 순이익률·ROE·PER 에도 영향**을 준다.

```
매출은 있는데 순이익이 NULL 인 행: 연 40~126건 · 영향 233종목
시총 상위: 현대차(21행) 삼성바이오로직스(3) HD현대중공업(1) SK(2) HD한국조선해양(7) SK이노베이션(11) LG화학(1) SK텔레콤(2)
```

---

## 7. ★ 밴드 성립률 — 생각보다 낮다 (시총 상위 40 실측)

```
PER 24/40 (60%)      PBR 30/40 (75%)
```

| 미성립 사유 | 종목 |
|---|---|
| `early` (분기 재무 이력 부족) | **금융주 전체** — KB금융·신한지주·하나금융·우리금융·삼성생명·삼성화재·메리츠금융·미래에셋증권 · SK스퀘어 |
| `neg` (적자) | 한국전력·한화오션·삼성중공업·HD현대중공업·SK이노베이션 |
| `stale` (수집 구멍) | 현대차 |
| 재무 없음 | 삼성전자우 (**우선주는 DART corp_code 없음**) |

★ 금융주는 `stock_financial` 에 **2023년 이전 분기 행이 아예 없다**(KB금융은 2024년부터 4개 분기가 갖춰짐).
접수일 원장은 2015년부터 다 있는데 재무만 없다 — `accountMap()` 이 금융업 계정을 버리는 것과 같은 뿌리로 보인다.
**밴드의 결함이 아니라 재무 수집의 한계**이고, 화면은 그 사실을 정직하게 말한다.

---

## 8. 화면

- 종목상세 **맨 아래** 카드. 순서: 연도별 재무 → 분기 추이 → 6개월 고저 → 일봉 → **밴드**
  (재무를 읽고 → 주가를 보고 → **그 둘의 비율**을 본다)
- 좌우 2단 `PER Band │ PBR Band`, 900px 아래에서 세로 1단
- 범례 = 수정 시총 + 배수 5개 + 「빈 구간 N%」
- 밴드가 없으면 **빈 차트를 그리지 않고 그 자리에 이유를 적는다**(빈 패널 금지)

### 갤러리 구성 ⑥ 「밴드형」

| 구성 | 테마 | 특징 |
|---|---|---|
| ⑥ 밴드형 | light | 캔들·거래량 없음 · 계단선 5 + 주가선 · **밴드선이 가격축에 참여** |

★ 구성 ①~⑤ 는 「지표선을 가격축에서 **제외**」가 규칙인데 밴드는 **정반대**다(밴드가 안 보이면 그림이 무의미).
그래서 같은 차트에 얹지 않고 **차트를 따로 둔다.**

★ **`ChartFeat` 「화면별 구성」에는 올리지 않았다** — 이 차트에는 켜고 끌 도구모음이 하나도 없다
(지표·기간바·SUE 마커 전부 없음). 등록하면 차트설정에 **빈 카드**만 하나 생긴다.
대신 갤러리 ⑥ 의 설명에 그 사실을 적었다.

---

## 9. ★ 처음 설계에서 **뒤집힌 것**

| 처음 계획 | 실측 후 |
|---|---|
| 「밴드 최신점 PER == 스크리너 PER 열」로 검산 | **틀린 기대.** 밴드는 **TTM**, 스크리너 기본은 **연간**이라 기준이 다르다. 삼성 밴드 PER 16.63(EPS 14,404) vs 2025 연간 EPS 7,757 — 2026 1Q 순이익 47.2조가 TTM 에 들어와서다. 화면 각주에 명시 |
| 「연결/별도」 토글 | **불가능.** 같은 (종목·연도·보고서)에 CFS·OFS 가 함께 있는 경우 **0건**. 종목당 한 쪽만 저장돼 있다 |
| `dailychart.js` 에 밴드 레이어 추가 + `?v=37` | **불필요.** `addLine({stepped:true})` 로 충분 — 공용 모듈 무수정 |
| 대한제분 = 2024년 분할 검증 사례 | 실제 분할일은 **2026-05-18** (메모리 기록이 틀렸음) |
| min/max 배수 | 10/30/50/70/90 분위수 (적자 직전 이상치가 밴드를 밖으로 밀어낸다) |

### 구현 중 잡은 내 실수 둘

1. **TTM 을 배열 순서로 더했다** — `$qs[$i+1]`. 빠진 분기가 있으면 조용히 **다섯 분기 전**을 더한다.
   반드시 `(연도·분기)` 키로 짚어야 한다(`$byQ[$qk-$k]`). 메모리에 이미 적혀 있던 함정을 그대로 밟았다.
2. **묵은 계단을 이어 그렸다** — §6 의 `stale` 가드.

---

## 10. 안 하는 것 (명시적 비목표)

- **컨센서스 추정 밴드**(그림 오른쪽 미래 절반) — DART 는 실적치만 준다. **재현 불가**.
- **배당수익률 밴드** — DPS 는 `alotMatter` 종목별 호출이라 14종목뿐.
- **전 종목 밴드 스크리닝** — 종목당 5년 시계열이라 2,500종목이면 무겁고, 캐시 표를 두면 비율 무저장 원칙과 부딪힌다.
- **수정주가 자체를 만들기** — §2 에서 회피했다.
- **배수 분위수의 백테스트** — 관례값이다. 화면 각주에 「근거 없음」을 처음부터 적어 두었다.

---

## 11. 남은 것

- 금융주 분기 재무 결측(2023 이전) — 밴드 성립률을 60% → 80%대로 올릴 수 있는 유일한 항목
- 현대차형 순이익 NULL 233종목 — 스크리너 PER·ROE 에도 영향
- 브라우저에서 **눈으로** 본 검증은 아직 (JS 문법·API 노출·서버 렌더까지만 확인)

---

## 참고

- `CLAUDE.md` 「차트 — ★업무 규칙」
- memory `session_재무분석.md`(PER 분모 함정·콜레이션 15배 사고) · `session_이익모멘텀SUE.md`(`dart_rcept`) · `session_차트화면별구성.md`
